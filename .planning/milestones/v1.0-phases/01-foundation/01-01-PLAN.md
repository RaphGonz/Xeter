---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/docker-compose.yml
  - .env.example
  - Makefile
autonomous: true
requirements:
  - INFR-01

must_haves:
  truths:
    - "`docker compose up` starts all six services with no errors"
    - "All infrastructure services pass their healthchecks before application services start"
    - "Application services hot-reload on code changes via volume mounts (no image rebuild)"
    - "`make up`, `make seed`, `make reset`, `make test` all exist as Makefile targets"
  artifacts:
    - path: "deploy/docker-compose.yml"
      provides: "Full stack: ClickHouse, PostgreSQL, Redis, MinIO, analyser stub, presenter stub, view stub"
      contains: "condition: service_healthy"
    - path: ".env.example"
      provides: "All required env vars with dev defaults"
      contains: "DATABASE_URL"
    - path: "Makefile"
      provides: "Developer commands"
      contains: "make seed"
  key_links:
    - from: "presenter service"
      to: "postgres, clickhouse, redis, minio"
      via: "depends_on condition: service_healthy"
      pattern: "condition: service_healthy"
    - from: "services"
      to: "source code"
      via: "volume mounts + uvicorn --reload"
      pattern: "uvicorn.*--reload"
---

<objective>
Create the Docker Compose stack that starts all six services (ClickHouse, PostgreSQL, Redis, MinIO, backend stubs, frontend stub) in the correct startup order, with hot reload for backend code.

Purpose: Every subsequent plan assumes `docker compose up` produces a running, healthy infrastructure. This is the physical foundation.
Output: deploy/docker-compose.yml, .env.example, Makefile with standard targets
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker Compose stack with healthcheck-gated startup</name>
  <files>deploy/docker-compose.yml</files>
  <action>
Create `deploy/docker-compose.yml` defining all six infrastructure and application services.

Infrastructure services (must define healthchecks):
- `postgres`: image postgres:16-alpine, port 5432:5432, volume pg_data, healthcheck `pg_isready -U xeter -d xeter`, env POSTGRES_DB=xeter POSTGRES_USER=xeter POSTGRES_PASSWORD=xeter_dev_password
- `clickhouse`: image clickhouse/clickhouse-server:25.3, ports 8123:8123 and 9000:9000, volume ch_data, healthcheck `wget --spider -q http://localhost:8123/ping`
- `redis`: image redis:7-alpine, port 6379:6379, healthcheck `redis-cli ping`
- `minio`: image minio/minio:latest, ports 9100:9000 and 9101:9001, volume minio_data, env MINIO_ROOT_USER=xeter MINIO_ROOT_PASSWORD=xeter_dev_password, command `server /data --console-address ":9001"`, healthcheck `curl -f http://localhost:9000/minio/health/live` (use curl, not mc — mc is not available in the standard minio image)

Application stub services (Phase 1: minimal Dockerfiles, not yet functional):
- `analyser`: build from services/analyser Dockerfile, port 4318:4318, volumes mount ../services/analyser:/app/services/analyser and ../shared:/app/shared, command `uvicorn services.analyser.main:app --host 0.0.0.0 --port 4318 --reload`, depends_on postgres+clickhouse+redis+minio all condition: service_healthy, env DATABASE_URL, CLICKHOUSE_HOST, REDIS_URL, MINIO_ENDPOINT
- `presenter`: build from services/presenter Dockerfile, port 8000:8000, volumes mount ../services/presenter:/app/services/presenter and ../shared:/app/shared, command `uvicorn services.presenter.main:app --host 0.0.0.0 --port 8000 --reload`, depends_on postgres+clickhouse+redis+minio all condition: service_healthy, same env vars
- `view`: build from services/view Dockerfile, port 3000:3000, volume mounts ../services/view:/app, no backend dependencies required (frontend is static dev server)

All healthcheck intervals: interval 10s, timeout 5s, retries 5, start_period 30s (except redis which needs no start_period).

Named volumes at bottom: pg_data, ch_data, minio_data.

Create stub Dockerfiles for each service so `docker compose build` succeeds in Phase 1:
- services/analyser/Dockerfile: FROM python:3.12-slim, WORKDIR /app, RUN pip install fastapi "uvicorn[standard]", CMD stub
- services/presenter/Dockerfile: same pattern
- services/view/Dockerfile: FROM node:20-alpine, WORKDIR /app, expose 3000

Create stub main.py files so uvicorn can start:
- services/analyser/main.py: minimal FastAPI app with a single GET /healthz returning {"status": "ok"}
- services/presenter/main.py: minimal FastAPI app with a single GET /healthz returning {"status": "ok"}

Create services/view/package.json stub (empty Next.js shell) and a simple index.html so the container starts without crashing.

Note: The actual service logic is built in Plans 03 and 04. These are intentionally minimal stubs.
  </action>
  <verify>
    <automated>cd C:/Users/raphg/Desktop/Xeter/Xeter && docker compose -f deploy/docker-compose.yml config --quiet && echo "compose config valid"</automated>
  </verify>
  <done>
`docker compose -f deploy/docker-compose.yml config` validates without errors. All six services defined with correct ports. All four infrastructure services have healthchecks. All application services have `condition: service_healthy` depends_on for all infra services.
  </done>
</task>

<task type="auto">
  <name>Task 2: Environment config and Makefile</name>
  <files>.env.example, Makefile</files>
  <action>
Create `.env.example` at repo root with all required environment variables and safe dev defaults:

```
# PostgreSQL
DATABASE_URL=postgresql+asyncpg://xeter:xeter_dev_password@localhost:5432/xeter
POSTGRES_URL=postgresql://xeter:xeter_dev_password@localhost:5432/xeter

# ClickHouse
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=8123
CLICKHOUSE_DB=default
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=

# Redis
REDIS_URL=redis://localhost:6379

# MinIO / S3
MINIO_ENDPOINT=http://localhost:9100
MINIO_ACCESS_KEY=xeter
MINIO_SECRET_KEY=xeter_dev_password
MINIO_BUCKET=xeter-spans

# Dev bootstrap
DEV_API_KEY=dev-api-key-local
DEV_TENANT_NAME=dev-tenant
DEV_USER_EMAIL=dev@example.com
DEV_USER_PASSWORD=dev_password_local

# App
SECRET_KEY=dev-secret-key-change-in-production
```

Create `Makefile` at repo root with these targets:
- `up`: `docker compose -f deploy/docker-compose.yml up --build`
- `down`: `docker compose -f deploy/docker-compose.yml down`
- `logs`: `docker compose -f deploy/docker-compose.yml logs -f`
- `build`: `docker compose -f deploy/docker-compose.yml build`
- `seed`: `cd xeter && python scripts/seed.py` (path TBD, placeholder for Plan 04)
- `reset`: `cd xeter && python scripts/reset.py` (path TBD, placeholder for Plan 04)
- `test`: `cd xeter && python -m pytest tests/ -v`
- `migrate`: `cd xeter && alembic upgrade head`

Add `.PHONY` declaration for all targets. Add a brief comment above each target explaining what it does.

Also create a `xeter/` Python package root directory with:
- `xeter/__init__.py` (empty)
- `xeter/shared/__init__.py` (empty — package scaffold for DAL in Plan 03)
- `xeter/shared/dal/__init__.py` (empty)
- `xeter/shared/db/__init__.py` (empty)
- `xeter/services/__init__.py` (empty)
- `xeter/services/analyser/__init__.py` (empty)
- `xeter/services/presenter/__init__.py` (empty)
- `xeter/migrations/.gitkeep` (placeholder)
- `xeter/tests/__init__.py` (empty)
- `xeter/tests/dal/__init__.py` (empty)
- `xeter/scripts/.gitkeep` (placeholder)
- `pyproject.toml` or `requirements.txt` at xeter/ root with Phase 1 deps:
  fastapi==0.135.2, "uvicorn[standard]>=0.32", pydantic==2.12.5, sqlalchemy==2.0.48, asyncpg==0.31.0, alembic==1.18.4, clickhouse-connect==0.15.0, "passlib[bcrypt]>=1.7", python-dotenv, structlog, httpx, pytest, "pytest-asyncio==1.3.0", anyio
  </action>
  <verify>
    <automated>test -f C:/Users/raphg/Desktop/Xeter/Xeter/.env.example && test -f C:/Users/raphg/Desktop/Xeter/Xeter/Makefile && grep -q "make seed" C:/Users/raphg/Desktop/Xeter/Xeter/Makefile && echo "env and Makefile exist"</automated>
  </verify>
  <done>
`.env.example` exists with all required env vars. `Makefile` exists with `up`, `down`, `seed`, `reset`, `test`, `migrate` targets. Python package scaffold under `xeter/` exists with all `__init__.py` files. `pyproject.toml` or `requirements.txt` lists all Phase 1 dependencies.
  </done>
</task>

</tasks>

<verification>
docker compose -f deploy/docker-compose.yml config --quiet
grep -r "condition: service_healthy" deploy/docker-compose.yml | wc -l  # should be >= 8 (2 app services × 4 infra deps)
grep "uvicorn.*--reload" deploy/docker-compose.yml  # confirms hot reload
grep "DEV_API_KEY" .env.example  # confirms dev key present
</verification>

<success_criteria>
- `docker compose -f deploy/docker-compose.yml config` passes with no errors
- All 4 infra services (postgres, clickhouse, redis, minio) have healthcheck blocks
- Both app services (analyser, presenter) have `depends_on` with `condition: service_healthy` on all 4 infra services
- `.env.example` contains DATABASE_URL, CLICKHOUSE_HOST, REDIS_URL, MINIO_ENDPOINT, DEV_API_KEY
- `Makefile` contains `seed`, `reset`, `test` targets
- Python package scaffold under `xeter/` with `shared/`, `services/`, `tests/`, `migrations/`, `scripts/` directories
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
