---
phase: 07-wrong-args-rewrite
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - xeter/scripts/calibrate.py
autonomous: true
requirements:
  - CAL-01
  - CAL-02

must_haves:
  truths:
    - "Running calibrate.py --flag-type wrong_tool completes and reports P/R metrics only for wrong_tool"
    - "Running calibrate.py --flag-type on a binary type skips the numeric sweep and reports binary detection"
    - "Running calibrate.py without --flag-type runs all flag types (unchanged global behavior)"
    - "wrong_tool_args is present in FLAG_TYPES; the stale exclusion NOTE in stdout is gone"
  artifacts:
    - path: "xeter/scripts/calibrate.py"
      provides: "Calibration harness with --flag-type arg and BINARY_FLAG_TYPES set"
      contains: "BINARY_FLAG_TYPES"
  key_links:
    - from: "calibrate.py main()"
      to: "active_flag_types list"
      via: "cli_args.flag_type filter"
      pattern: "active_flag_types"
---

<objective>
Extend calibrate.py with two infrastructure upgrades needed by the entire v1.1 milestone:

1. CAL-02: --flag-type CLI argument so each phase can calibrate its method in isolation
2. CAL-01: BINARY_FLAG_TYPES set that bypasses hill-climbing for non-numeric detectors

Also move wrong_tool_args from the exclusion comment back into FLAG_TYPES — the Phase 7 rewrite makes it calibratable.

Purpose: Every phase in v1.1 ends with `calibrate.py --flag-type <method>`. CAL-01 is an empty placeholder now but Phase 9 tool_use_violation requires it.

Output: Updated xeter/scripts/calibrate.py with parse_args(), BINARY_FLAG_TYPES set, filtered main loop, binary-safe summary printer, stale NOTE removed.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-wrong-args-rewrite/07-RESEARCH.md

Relevant current state of xeter/scripts/calibrate.py:

FLAG_TYPES list (lines 46-52) — wrong_tool_args commented out:
  FLAG_TYPES = ["wrong_tool", "no_tool", "excessive_tool", "parsing_error", "response_anomaly"]

Stale NOTE to remove in main() (lines 309-312):
  print("\\nNOTE: wrong_tool_args excluded — low_confidence by design ...")

Hill-climb loop (line 318):
  for flag_type in FLAG_TYPES:

Summary loop (lines 335-345) — assumes best_precision is never None

plot_pr_curves() — assumes every result has a non-empty history list
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BINARY_FLAG_TYPES, parse_args(), --flag-type filter to calibrate.py</name>
  <files>xeter/scripts/calibrate.py</files>
  <action>
Make the following surgical edits to xeter/scripts/calibrate.py. Do NOT rewrite the file — make targeted changes.

CHANGE 1 - Module docstring: Remove the sentence about wrong_tool_args exclusion from the docstring at the top of the file.

CHANGE 2 - Imports: Add "import argparse" after "import json" in the stdlib imports block.

CHANGE 3 - FLAG_TYPES: Uncomment wrong_tool_args and add it to the list. Remove the inline exclusion comment. Result:
    FLAG_TYPES = [
        "wrong_tool",
        "wrong_tool_args",
        "no_tool",
        "excessive_tool",
        "parsing_error",
        "response_anomaly",
    ]

CHANGE 4 - Add BINARY_FLAG_TYPES set immediately after FLAG_TYPES:
    BINARY_FLAG_TYPES: set[str] = {
        # "tool_use_violation",  # added in Phase 9
    }

CHANGE 5 - Add parse_args() function just before main():
    def parse_args():
        parser = argparse.ArgumentParser(
            description="Calibrate ToolCallAnalyzer thresholds"
        )
        parser.add_argument(
            "--flag-type",
            dest="flag_type",
            default=None,
            help="Calibrate only this flag type in isolation (e.g. wrong_tool_args)",
        )
        return parser.parse_args()

CHANGE 6 - Update main(): call parse_args() at the very start of main(), then after loading spans determine the active flag lists:
    cli_args = parse_args()
    (... existing embedder/span loading code ...)
    if cli_args.flag_type:
        known = set(FLAG_TYPES) | BINARY_FLAG_TYPES
        if cli_args.flag_type not in known:
            print(f"ERROR: Unknown flag type: {cli_args.flag_type!r}. Known: {sorted(known)}")
            sys.exit(1)
        active_flag_types = [cli_args.flag_type] if cli_args.flag_type in FLAG_TYPES else []
        active_binary = ({cli_args.flag_type} if cli_args.flag_type in BINARY_FLAG_TYPES
                         else set())
    else:
        active_flag_types = FLAG_TYPES
        active_binary = BINARY_FLAG_TYPES

CHANGE 7 - Remove the stale NOTE print from main(). Delete the entire print() call block that says "wrong_tool_args excluded — low_confidence by design".

CHANGE 8 - Update the hill-climb loop: replace "for flag_type in FLAG_TYPES:" with "for flag_type in active_flag_types:" and add binary detection handling at the top of the loop body:
    for flag_type in active_flag_types:
        if flag_type in active_binary:
            print(f"\\n  [{flag_type}] binary flag — skipping numeric sweep")
            results[flag_type] = {
                "best_threshold": 1.0,
                "best_precision": None,
                "best_recall": None,
                "history": [],
                "steps": 0,
                "binary": True,
            }
            continue
        best_threshold, best_precision, best_recall, history = hill_climb(
            flag_type, spans, embedder, calibrated
        )
        calibrated[flag_type] = best_threshold
        results[flag_type] = {
            "best_threshold": best_threshold,
            "best_precision": best_precision,
            "best_recall": best_recall,
            "history": history,
            "steps": len(history),
        }

CHANGE 9 - Update summary loop to handle binary results (best_precision is None). Add a guard at the top of the loop body:
    for flag_type, res in results.items():
        if res.get("binary"):
            print(f"  {flag_type:<25s}  binary detection — no P/R sweep")
            continue
        met = res["best_precision"] >= 0.80
        (... rest of existing print logic unchanged ...)

CHANGE 10 - Update plot_pr_curves(): compute n as count of non-empty-history results only. Skip entries with empty history inside the loop. If n == 0, return early without plotting.

Invariant preserved: calibrated = dict(DEFAULT_THRESHOLDS) is computed before the loop. When --flag-type is used, only that flag type's threshold is updated in calibrated.
  </action>
  <verify>
    <automated>cd /c/Users/raphg/Desktop/Xeter/Xeter && python -c "import sys; sys.argv=['c','--flag-type','wrong_tool_args']; from xeter.scripts.calibrate import parse_args,FLAG_TYPES,BINARY_FLAG_TYPES; a=parse_args(); assert a.flag_type=='wrong_tool_args'; assert 'wrong_tool_args' in FLAG_TYPES; assert isinstance(BINARY_FLAG_TYPES,set); print('PASS')"</automated>
  </verify>
  <done>parse_args() function exists and returns flag_type correctly; BINARY_FLAG_TYPES is a set; wrong_tool_args is in FLAG_TYPES; import succeeds without errors</done>
</task>

</tasks>

<verification>
Run: pytest xeter/tests/ -x -q (all existing tests still pass — no test touches calibrate.py directly).
Static check: grep -n "wrong_tool_args" xeter/scripts/calibrate.py shows it in FLAG_TYPES, not in a comment.
Static check: grep -n "NOTE:" xeter/scripts/calibrate.py returns nothing (stale print removed).
Static check: grep -n "BINARY_FLAG_TYPES" xeter/scripts/calibrate.py shows the set definition.
</verification>

<success_criteria>
- BINARY_FLAG_TYPES set defined after FLAG_TYPES (empty for now, comment for Phase 9)
- parse_args() function exists using argparse, returns Namespace with flag_type attribute
- main() calls parse_args() and filters to active_flag_types when --flag-type is given
- Binary flags skip hill_climb and are reported as "binary detection" in summary
- plot_pr_curves() handles empty history gracefully
- All 16 existing worker tests still pass
- wrong_tool_args is in FLAG_TYPES (not commented out)
- Stale NOTE print about wrong_tool_args exclusion is removed
</success_criteria>

<output>
After completion, create .planning/phases/07-wrong-args-rewrite/07-01-SUMMARY.md
</output>
