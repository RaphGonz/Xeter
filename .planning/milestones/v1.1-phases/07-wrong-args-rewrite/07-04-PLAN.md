---
phase: 07-wrong-args-rewrite
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - xeter/services/worker/detection_patterns.yml
autonomous: false
requirements: []

must_haves:
  truths:
    - "detection_patterns.yml exists on disk with negation_motifs and tool_triggering_terms sections"
    - "User has reviewed and explicitly approved the schema before Phase 9 begins"
    - "The file is valid YAML parseable by yaml.safe_load"
  artifacts:
    - path: "xeter/services/worker/detection_patterns.yml"
      provides: "Initial schema draft for NOTOOL-04 and NOTOOL-06"
      contains: "negation_motifs"
  key_links:
    - from: "Phase 9 _check_tool_use_violation()"
      to: "xeter/services/worker/detection_patterns.yml"
      via: "yaml.safe_load at init"
      pattern: "detection_patterns.yml"
---

<objective>
Create the initial detection_patterns.yml schema file that Phase 9 needs for windowed proximity detection in _check_tool_use_violation(). This file stores negation motifs and tool-triggering terms as external YAML — editable without a code rebuild (NOTOOL-04).

User must review and approve the draft before Phase 9 implementation begins (NOTOOL-06, the current STATE.md blocker). Creating the draft now (Wave 1, parallel with code work) avoids holding up Phase 8 or 9.

Note: requirements field is empty here because NOTOOL-04 and NOTOOL-06 are Phase 9 requirements. This plan is preparatory work that satisfies a Phase 9 prerequisite.

Output: xeter/services/worker/detection_patterns.yml created; user approval obtained.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

NOTOOL-03 (Phase 9): _check_tool_use_violation uses windowed proximity detection.
Finds tool-triggering terms in prompt, checks if a negation motif appears 1-4 words
before it. Score based on proximity distance (1 word = highest confidence).

NOTOOL-04: Detection patterns stored in external YAML, editable without code rebuild.
NOTOOL-06: Schema reviewed and approved by user before implementation begins.

The file will be loaded by Phase 9 implementation as:
  import yaml
  with open("xeter/services/worker/detection_patterns.yml") as f:
      patterns = yaml.safe_load(f)
  negation_motifs = patterns["negation_motifs"]
  tool_triggering_terms = patterns["tool_triggering_terms"]
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create detection_patterns.yml draft</name>
  <files>xeter/services/worker/detection_patterns.yml</files>
  <action>
Create xeter/services/worker/detection_patterns.yml with the following content.

The file serves NOTOOL-03: windowed proximity detection. The proximity algorithm scans
the prompt for a tool_triggering_term, then checks if a negation_motif appears 1-4 words
before it.

Content to write:

  # Detection patterns for _check_tool_use_violation (Phase 9)
  # Editable without code rebuild (NOTOOL-04).
  # Reviewed and approved by user before Phase 9 implementation (NOTOOL-06).

  negation_motifs:
    - "do not"
    - "don't"
    - "never"
    - "avoid"
    - "without"
    - "skip"
    - "no"
    - "refrain from"
    - "instead of"
    - "rather than"

  tool_triggering_terms:
    - "use"
    - "call"
    - "invoke"
    - "run"
    - "execute"
    - "fetch"
    - "search"
    - "query"
    - "look up"
    - "retrieve"
    - "check"
    - "get"
  </action>
  <verify>
    <automated>cd /c/Users/raphg/Desktop/Xeter/Xeter && python -c "import yaml; d=yaml.safe_load(open('xeter/services/worker/detection_patterns.yml')); assert 'negation_motifs' in d; assert 'tool_triggering_terms' in d; assert len(d['negation_motifs'])>=5; assert len(d['tool_triggering_terms'])>=5; print('PASS: schema valid')"</automated>
  </verify>
  <done>detection_patterns.yml exists; yaml.safe_load parses it; both sections have at least 5 entries</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User review and approval of detection_patterns.yml</name>
  <action>Present detection_patterns.yml to user for review. No code changes needed.</action>
  <what-built>
Created xeter/services/worker/detection_patterns.yml with:
- negation_motifs: 10 entries (do not, don't, never, avoid, without, skip, no, refrain from, instead of, rather than)
- tool_triggering_terms: 12 entries (use, call, invoke, run, execute, fetch, search, query, look up, retrieve, check, get)

This file will be loaded at init by _check_tool_use_violation() in Phase 9. Approving now satisfies NOTOOL-06 (schema reviewed before implementation begins) and resolves the STATE.md blocker.
  </what-built>
  <how-to-verify>
1. Open xeter/services/worker/detection_patterns.yml
2. Review negation_motifs: do these phrases cover "do not call tools" instructions in your data?
3. Review tool_triggering_terms: do these words signal a tool should be called in your prompts?
4. Consider additions: "must not", "please avoid", "should not" for negation; "lookup", "find" for triggering terms
5. The proximity algorithm fires when a negation_motif appears 1-4 words before a tool_triggering_term
  </how-to-verify>
  <resume-signal>Type "approved" if the schema looks correct, or describe changes needed (e.g. "add must not to negation_motifs")</resume-signal>
  <verify>User has typed "approved" or specified changes which were applied</verify>
  <done>User has explicitly approved the schema; NOTOOL-06 blocker resolved</done>
</task>

</tasks>

<verification>
File exists and parses: python -c "import yaml; yaml.safe_load(open('xeter/services/worker/detection_patterns.yml'))" exits 0
User checkpoint passed.
</verification>

<success_criteria>
- detection_patterns.yml exists and is valid YAML
- negation_motifs and tool_triggering_terms both present with entries
- User explicitly approved the schema content via checkpoint
- STATE.md blocker NOTOOL-06 is resolved
</success_criteria>

<output>
After completion, create .planning/phases/07-wrong-args-rewrite/07-04-SUMMARY.md
Note: NOTOOL-06 blocker resolved — user approved schema on [date].
Note any content changes the user requested.
</output>
