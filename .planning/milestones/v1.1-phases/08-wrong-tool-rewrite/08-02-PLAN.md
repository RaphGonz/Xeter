---
phase: 08-wrong-tool-rewrite
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified: []
autonomous: false
requirements:
  - WTOOL-01
  - WTOOL-02
  - WTOOL-03
  - WTOOL-04

must_haves:
  truths:
    - "User has reviewed the rewritten _check_wrong_tool logic and approved it (or raised issues to fix)"
    - "The three-branch algorithm is understood and accepted before calibration runs"
  artifacts:
    - path: "xeter/services/worker/tool_call_analyzer.py"
      provides: "Approved _check_wrong_tool implementation"
      contains: "wrong_tool_called"
  key_links:
    - from: "checkpoint:human-verify"
      to: "08-03-PLAN.md"
      via: "approval gate — calibration only runs after user approves algorithm"
      pattern: "n/a"
---

<objective>
Pause for user review of the rewritten `_check_wrong_tool` before calibration runs. The algorithm is non-trivial (three cases with a single threshold) and a human review at this point catches logic errors before they affect the calibration fixture.

Purpose: Structural review point. Every Phase 7–10 plan follows the pattern: implement → review → calibrate. This is the review step.

Output: User approval (or issue list for 08-01 fix) before 08-03 runs.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-wrong-tool-rewrite/08-CONTEXT.md
@.planning/phases/08-wrong-tool-rewrite/08-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Rewritten `_check_wrong_tool` in `xeter/services/worker/tool_call_analyzer.py` with three-branch logic:

- Branch 0 (guard): `span.tool_name is None` → return [] (method only applies when a tool was called)
- Branch 1 (immediate): `available_tools` is None or empty → flag immediately with `metric: no_available_tools`, no embedding (WTOOL-03)
- Branch 2 (threshold): hybrid score all available tools; take top1
  - Case A: `top1_name == called_tool AND top1_score >= threshold` → no flag (correct)
  - Case B: `top1_name != called_tool AND top1_score >= threshold` → flag (better tool existed)
  - Case C: `top1_score < threshold` → flag (no tool was appropriate)

Single threshold key `wrong_tool_called` (renamed from `wrong_tool`) in all 5 files.
Reported score is `top1_score` (hybrid). 4 new unit tests added.
  </what-built>
  <how-to-verify>
1. Read `xeter/services/worker/tool_call_analyzer.py` — find `_check_wrong_tool`. Confirm:
   - First branch checks `span.tool_name is None` → return [].
   - Second branch checks `not span.available_tools` → immediate Flag with `metric: no_available_tools`.
   - Hybrid scoring loop uses `bow_score` + `hybrid_score` (not cosine-only).
   - Single `self._thresholds["wrong_tool_called"]` lookup (no other threshold key in this method).
   - Correct case returns [] (not flagged). All other cases return [Flag(...)].
   - `flag.score = top1_score` (the hybrid score, not a gap or name score).

2. Check the old bug is gone: the old code flagged ONLY when `called_tool != top_tool AND top_score < threshold`. Confirm this inverted AND gate is NOT present in the new code.

3. Run tests: `python -m pytest xeter/tests/worker/test_tool_call_analyzer.py -v`
   Confirm all pass including the 4 new tests:
   - `test_wrong_tool_immediate_flag_no_available_tools`
   - `test_wrong_tool_immediate_flag_empty_available_tools`
   - `test_wrong_tool_score_is_top1_hybrid`
   - `test_wrong_tool_no_flag_correct_tool_above_threshold`

4. Spot check the threshold rename: `grep -rn "wrong_tool_called" xeter/services/worker/main.py xeter/scripts/calibrate.py deploy/docker-compose.yml fixtures/calibrated_thresholds.json`
   Should see 1+ hits in each file.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to calibration, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
User approval received. No issues flagged, or all flagged issues resolved.
</verification>

<success_criteria>
- User has reviewed the three-branch algorithm and confirmed it matches CONTEXT.md decisions
- All 4 new tests pass in user's local environment
- No logic errors identified in the correct-case / Case B / Case C branching
- Threshold rename confirmed in all 5 files
</success_criteria>

<output>
After completion, create `.planning/phases/08-wrong-tool-rewrite/08-02-SUMMARY.md`
</output>
