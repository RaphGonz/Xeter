---
phase: 08-wrong-tool-rewrite
plan: "03"
type: execute
wave: 3
depends_on:
  - "08-02"
files_modified:
  - fixtures/labelled_spans.jsonl
  - fixtures/calibrated_thresholds.json
autonomous: true
requirements:
  - WTOOL-06

must_haves:
  truths:
    - "calibrate.py --flag-type wrong_tool completes without errors and prints P and R"
    - "The wrong_tool_called key appears in calibrated_thresholds.json with a calibrated value"
    - "Calibration fixture covers all three detection branches (Case B original 11 spans + new WTOOL-03 spans + Case C spans)"
    - "Recall is maximized (false negatives minimized); both P and R are reported"
  artifacts:
    - path: "fixtures/labelled_spans.jsonl"
      provides: "Augmented fixture with WTOOL-03 and Case C spans"
      min_lines: 1
    - path: "fixtures/calibrated_thresholds.json"
      provides: "Calibrated wrong_tool_called threshold value"
      contains: "wrong_tool_called"
  key_links:
    - from: "fixtures/labelled_spans.jsonl"
      to: "xeter/scripts/calibrate.py"
      via: "labelled spans consumed by hill-climb"
      pattern: "anomaly_type.*wrong_tool"
    - from: "xeter/scripts/calibrate.py"
      to: "fixtures/calibrated_thresholds.json"
      via: "hill-climb writes best threshold"
      pattern: "wrong_tool_called"
---

<objective>
Augment the calibration fixture with missing branch coverage, run `calibrate.py --flag-type wrong_tool`, and write the calibrated `wrong_tool_called` threshold to `calibrated_thresholds.json`.

Purpose: WTOOL-06 requires a per-method calibration run that passes the P/R benchmark before Phase 9 begins. The existing 11 wrong_tool spans only cover Case B (better tool existed). Adding WTOOL-03 and Case C spans makes calibration meaningful across all detection paths.

Output: Augmented `labelled_spans.jsonl`, calibrated `wrong_tool_called` threshold in JSON.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-wrong-tool-rewrite/08-CONTEXT.md
@.planning/phases/08-wrong-tool-rewrite/08-01-SUMMARY.md
@.planning/phases/08-wrong-tool-rewrite/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Augment fixture with WTOOL-03 and Case C spans</name>
  <files>
    fixtures/labelled_spans.jsonl
  </files>
  <action>
Append new labelled spans to `fixtures/labelled_spans.jsonl`. Each span is a JSON object on its own line (JSONL format). All new spans must have `"anomaly_type": "wrong_tool"` so the calibration hill-climb counts them as positive examples.

**Group 1 — WTOOL-03: tool called with no available_tools (4 spans)**

Add 4 spans with `available_tools: null` (or `[]`) and a non-null `tool_name`. These exercise Branch 1 (immediate flag). The `wrong_tool` flag fires unconditionally here, so threshold sweep does not affect them — they are always TP. Include varied prompt/tool combinations:
- Span A: prompt asks to "look up the weather", tool_name="weather_api", available_tools=null
- Span B: prompt asks to "calculate compound interest", tool_name="calculator", available_tools=null
- Span C: prompt asks to "find the capital of France", tool_name="search_web", available_tools=[]
- Span D: prompt asks to "send an email to Alice", tool_name="send_email", available_tools=null

**Group 2 — Case C: no tool was appropriate for the prompt (4 spans)**

Add 4 spans where the prompt is semantically distant from all available_tools. These exercise "top1_score < threshold" path. The available_tools should be plausible tools but unrelated to the prompt:
- Span E: prompt="What is 2+2?", tool_name="search_web", available_tools=[{"name": "database_query", "description": "Query SQL databases"}, {"name": "file_reader", "description": "Read files from disk"}]
- Span F: prompt="Tell me a joke", tool_name="calculator", available_tools=[{"name": "calculator", "description": "Performs mathematical calculations"}, {"name": "translator", "description": "Translates text between languages"}]
- Span G: prompt="Recite a poem about autumn", tool_name="web_scraper", available_tools=[{"name": "web_scraper", "description": "Scrapes web pages"}, {"name": "image_classifier", "description": "Classifies images using ML"}]
- Span H: prompt="What is the meaning of life?", tool_name="weather_api", available_tools=[{"name": "weather_api", "description": "Fetch current weather data"}, {"name": "stock_ticker", "description": "Get stock prices"}]

**Format for each span** — use the same schema as existing spans in the file. Required fields: `span_id` (unique), `tenant_id`, `trace_id`, `agent_name`, `agent_model`, `tool_name`, `prompt`, `anomaly_type: "wrong_tool"`. Optional: `tool_description`, `tool_arguments`, `tool_output`, `available_tools`, `response`.

Read the first 2–3 existing wrong_tool spans from the file to confirm exact field names and format before appending.

Assign unique `span_id` values (e.g., `"wtool-03-a"` through `"wtool-03-d"` and `"wtool-casec-a"` through `"wtool-casec-d"`).
  </action>
  <verify>
    <automated>python -c "
import json
spans = [json.loads(l) for l in open('fixtures/labelled_spans.jsonl') if l.strip()]
wt = [s for s in spans if s.get('anomaly_type') == 'wrong_tool']
no_avail = [s for s in wt if not s.get('available_tools')]
print(f'Total wrong_tool spans: {len(wt)} (was 11, expect >= 19)')
print(f'No available_tools spans: {len(no_avail)} (expect >= 4)')
"</automated>
  </verify>
  <done>Fixture contains >= 19 wrong_tool spans. At least 4 have available_tools=null or []. At least 4 new spans have semantically mismatched tools (Case C). All spans are valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Run calibration and write calibrated threshold</name>
  <files>
    fixtures/calibrated_thresholds.json
  </files>
  <action>
**Prerequisites:** The live embedder container must be running. Start it if needed:
```bash
docker compose -f deploy/docker-compose.yml up embedder -d
```
Wait ~10 seconds for the container to become ready before running calibration.

**Run calibration:**
```bash
python xeter/scripts/calibrate.py --flag-type wrong_tool
```

This runs the hill-climb over the `wrong_tool_called` threshold. It will:
1. Score all labelled_spans against the live embedder.
2. Print a table of threshold → P, R, F1 per step.
3. Write the best threshold to `fixtures/calibrated_thresholds.json` under the `wrong_tool_called` key.

**Recall-first threshold selection (per locked CONTEXT.md decision):**
After the calibration run, inspect the printed P/R table. The hill-climb maximizes precision by default. For recall-first:
- Identify the lowest threshold that achieves recall >= 0.80 (or the highest recall achievable).
- If the auto-selected threshold gives lower recall than a lower threshold at acceptable precision, manually update `fixtures/calibrated_thresholds.json` to use the recall-maximizing threshold.
- Log the chosen threshold and the P/R values in the summary.

**Confirm the key is written correctly:**
```bash
python -c "import json; d=json.load(open('fixtures/calibrated_thresholds.json')); print(d.get('wrong_tool_called', 'MISSING'))"
```

**If the calibration crashes** with KeyError or import error, check:
1. `grep -n "wrong_tool_called" xeter/scripts/calibrate.py` — must show the renamed key.
2. `grep -n "wrong_tool_called" xeter/services/worker/main.py` — must show the env var read.
3. The embedder container is healthy: `docker ps | grep embedder`.
  </action>
  <verify>
    <automated>python -c "import json; d=json.load(open('fixtures/calibrated_thresholds.json')); v=d.get('wrong_tool_called'); print(f'wrong_tool_called threshold: {v}'); assert v is not None, 'MISSING key'; assert 0.0 < v < 1.0, f'threshold out of range: {v}'"</automated>
  </verify>
  <done>`wrong_tool_called` key exists in `calibrated_thresholds.json` with a float value between 0.0 and 1.0. Calibration completed without errors. Both P and R were reported. Recall-first threshold selected (recall >= 0.80 if achievable, or highest achievable recall noted).</done>
</task>

</tasks>

<verification>
1. Fixture coverage: `python -c "import json; spans=[json.loads(l) for l in open('fixtures/labelled_spans.jsonl') if l.strip()]; wt=[s for s in spans if s.get('anomaly_type')=='wrong_tool']; print(len(wt), 'wrong_tool spans')"`
2. Threshold written: `python -c "import json; d=json.load(open('fixtures/calibrated_thresholds.json')); print(d.get('wrong_tool_called'))"`
3. Full test suite still passes: `python -m pytest xeter/tests/worker/ -q`
</verification>

<success_criteria>
- `fixtures/labelled_spans.jsonl` contains >= 19 wrong_tool spans (11 original Case B + 4 WTOOL-03 + 4 Case C)
- `fixtures/calibrated_thresholds.json` has `wrong_tool_called` key with a calibrated float value
- Calibration run completed without error; P and R both reported
- Recall-first selection applied (threshold chosen to maximize recall, with P as high as possible given that goal)
- Full worker test suite passes
- Phase 8 complete — WTOOL-06 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/08-wrong-tool-rewrite/08-03-SUMMARY.md`
</output>
