---
milestone: v1.4
name: Trace Hierarchy + TraceAnalyzer Foundation
audited: 2026-05-15T17:00:00Z
status: tech_debt
scores:
  requirements: 12/12
  phases: 4/4
  integration: 11/12
  flows: 3/4
gaps: []
tech_debt:
  - phase: 21-trace-ui
    items:
      - "human_needed: 3 runtime behaviors unverified — span auto-open on back-navigation (?span= deep-link), collapsible tree chevron/row propagation, empty-state /traces page"
      - "disputed: useSearchParams() at traces/[trace_id]/page.tsx:21 called without explicit Suspense boundary — integration checker flagged as production build risk; 21-04 decision documented that dynamic rendering via use(params) makes Suspense unnecessary; requires human browser and/or next build verification"
  - phase: 19-traceanalyzer-scaffold-db-migration
    items:
      - "doc gap: 19-01-SUMMARY.md frontmatter requirements-completed field is [] — TANA-02 was claimed and verified by VERIFICATION.md but omitted from SUMMARY frontmatter; code correct, tracking gap only"
---

# Milestone v1.4 — Audit Report

**Milestone:** Trace Hierarchy + TraceAnalyzer Foundation
**Audited:** 2026-05-15
**Status:** ⚡ tech_debt — all requirements met, no critical blockers, items below need review

---

## Score Summary

| Dimension | Score | Notes |
|---|---|---|
| Requirements | 12/12 | All satisfied per 3-source cross-reference |
| Phases | 4/4 | All VERIFICATION.md files present; phases 18/19/20 passed, phase 21 human_needed |
| Integration | 11/12 | All cross-phase wiring verified; 1 disputed item (useSearchParams/Suspense) |
| E2E flows | 3/4 | 3 flows fully wired; 1 partially unverified (UI-03 runtime behavior) |

---

## Phase Verification Summary

| Phase | Status | Score | Requirements |
|---|---|---|---|
| 18: Cleanup + BaseAnalyzer Refactor | passed | 8/8 | CLEAN-01, CLEAN-02, CLEAN-03, TANA-01 |
| 19: TraceAnalyzer Scaffold + DB Migration | passed | 10/10 | TANA-02, TANA-03, TANA-04 |
| 20: Trace API | passed | 15/15 | TRACE-01, TRACE-02 |
| 21: Trace UI | human_needed | 8/8 | UI-01, UI-02, UI-03 |

All four phases have VERIFICATION.md files. No unverified phases.

---

## Requirements Coverage (3-Source Cross-Reference)

| Requirement | VERIFICATION.md | SUMMARY Frontmatter | REQUIREMENTS.md | Final Status |
|---|---|---|---|---|
| CLEAN-01 | Phase 18: SATISFIED | 18-01-SUMMARY: listed | [x] Complete | **satisfied** |
| CLEAN-02 | Phase 18: SATISFIED | 18-01-SUMMARY: listed | [x] Complete | **satisfied** |
| CLEAN-03 | Phase 18: SATISFIED | 18-01-SUMMARY: listed | [x] Complete | **satisfied** |
| TANA-01 | Phase 18: SATISFIED | 18-02-SUMMARY: listed | [x] Complete | **satisfied** |
| TANA-02 | Phase 19: SATISFIED | 19-01-SUMMARY: **missing** | [x] Complete | **partial** (doc gap only) |
| TANA-03 | Phase 19: SATISFIED | 19-03-SUMMARY: listed | [x] Complete | **satisfied** |
| TANA-04 | Phase 19: SATISFIED | 19-02-SUMMARY: listed | [x] Complete | **satisfied** |
| TRACE-01 | Phase 20: SATISFIED | 20-01-SUMMARY: listed | [x] Complete | **satisfied** |
| TRACE-02 | Phase 20: SATISFIED | 20-01-SUMMARY: listed | [x] Complete | **satisfied** |
| UI-01 | Phase 21: SATISFIED | 21-01-SUMMARY: listed | [x] Complete | **satisfied** |
| UI-02 | Phase 21: SATISFIED | 21-02-SUMMARY: listed | [x] Complete | **satisfied** |
| UI-03 | Phase 21: SATISFIED | 21-03-SUMMARY: listed | [x] Complete | **partial** (runtime unverified) |

**Orphaned requirements:** None.

**FAIL gate:** No requirement is `unsatisfied`. No critical blockers.

### TANA-02 documentation gap
`19-01-SUMMARY.md` frontmatter has `requirements-completed: []` — TANA-02 is absent. The Phase 19 VERIFICATION.md marks TANA-02 as SATISFIED with full evidence (`trace_analyzer.py` exists, `class TraceAnalyzer(BaseTraceAnalyzer)`, 5 tests pass). This is a tracking-only gap with zero code impact.

### UI-03 partial status
Code wiring is complete: `SpanDetailPanel.tsx:259` breadcrumb href includes `?span=${detail.span_id}`; `traces/[trace_id]/page.tsx:21–31` reads the `?span` param via `useSearchParams()` and initialises `selectedSpanId`. The `useState(spanFromUrl)` initialiser causes `SpanDetailPanel` to open on mount when the param is present. Three items remain unverified:
1. The `?span=` auto-open is a runtime behavior (Sheet animation) that cannot be verified statically
2. Collapsible tree chevron vs row click event propagation cannot be observed statically
3. Empty state on `/traces` requires a tenant with no trace data

---

## Cross-Phase Integration

### Phase 18 → 19: BaseTraceAnalyzer inheritance
**CONNECTED.** `trace_analyzer.py:12` imports `BaseTraceAnalyzer` from `base.py`; `class TraceAnalyzer(BaseTraceAnalyzer)` at line 15 satisfies the abstract contract. `ToolCallAnalyzer(BaseSpanAnalyzer)` wired correctly in Phase 18 — TANA-01 fully consumed by Phase 19.

### Phase 19 → Worker: TraceAnalyzer in main loop
**CONNECTED.** `worker/main.py:136` instantiates `TraceAnalyzer(embedder, THRESHOLDS)`; flush-timeout loop at lines 162–187 calls `trace_analyzer.analyze()` and writes `write_flags(None, ...)` for non-empty results. `flag_writer.py:52` signature `span_id: str | None` matches nullable schema from migration 005.

### Phase 19 → 20: Nullable span_id consumed by Presenter
**CONNECTED.** `routers/traces.py:343` correctly branches on `if flag.span_id is None` to route trace-level vs span-level flags. The no-spans-yet path (lines 260–307) handles `Flag.span_id.is_(None)` correctly.

### Phase 20 → 21: TypeScript types vs Pydantic models
**CONNECTED — 100% field match.** All 25 fields across `TraceListItem`, `TraceListResponse`, `TraceObject`, `SpanInTrace`, `TraceFlagItem`, `ScoreItem` match between `routers/traces.py` Pydantic models and `api.ts` TypeScript interfaces. No type mismatches.

### Phase 21 → 20: API call URL patterns
**CONNECTED.** `api.ts` `listTraces()` fetches `/api/traces` → Next.js rewrite (`next.config.ts:6–9`) → `GET /traces`. `getTraceDetail()` fetches `/api/traces/${traceId}` → `GET /traces/{trace_id}`. Router registration in `main.py:55` confirmed.

### Disputed: useSearchParams without Suspense (UI-03)
**DISPUTED — not a blocker.** Integration checker flagged `useSearchParams()` at `page.tsx:21` as lacking a Suspense boundary (Next.js 15+ requirement). However, 21-04 SUMMARY documents an explicit conscious decision: *"No Suspense boundary added — page is already dynamically rendered via use(params); useSearchParams is safe without extra wrapper."* The reasoning is that `use(params)` makes the page fully dynamic, so `useSearchParams` is safe in this context per Next.js docs. This should be verified with `next build` before production deployment. Tracked as tech debt.

---

## E2E Flow Verification

| Flow | Status | Notes |
|---|---|---|
| **Trace List**: login → NavBar "Traces" → `/traces` → `listTraces()` → `GET /traces` → TraceTable | COMPLETE | Full chain wired; auth guard in `traces/layout.tsx` |
| **Trace Detail**: click trace row → `/traces/{id}` → `getTraceDetail()` → `GET /traces/{id}` → SpanTree renders hierarchy | COMPLETE | Full chain wired; parent_span_id tree construction verified |
| **Worker → Flags**: spans ingested → trace buffer → flush timeout → `TraceAnalyzer.analyze([])` → no flags (scaffold) → span-level flags still written normally | COMPLETE | Scaffold correctly returns []; existing span-level flag path unaffected |
| **Span back-navigation**: span detail breadcrumb → `/traces/{id}?span={span_id}` → SpanDetailPanel auto-opens | PARTIAL | URL construction and param read wired statically; runtime behavior (Sheet open) needs human verification |

---

## Tech Debt by Phase

### Phase 19: TraceAnalyzer Scaffold + DB Migration
- **TANA-02 doc gap:** `19-01-SUMMARY.md` frontmatter `requirements-completed: []` — TANA-02 should be listed. No code impact. Fix: update frontmatter to `[TANA-02]`.

### Phase 21: Trace UI
- **Human testing required (3 items):** span auto-open on back-navigation, collapsible tree interaction, empty state on `/traces` — all require a running browser with real data.
- **useSearchParams Suspense (disputed):** Verify with `next build` before production deployment. If the build fails, add a `<Suspense>` wrapper around the `useSearchParams` read by extracting it into a child component.

### Inherited tech debt (from v1.3 notes, now resolved)
- ✓ Dead `verify_session_token()` in `diagnosticer/main.py` — **resolved in Phase 18 (CLEAN-01)**
- ✓ Stale "NO RLS" comment in `spans.py` — **resolved in Phase 18 (CLEAN-02)**

---

## Conclusion

All 12 v1.4 requirements are satisfied. All four phases have verifications. Cross-phase wiring is correct at every boundary. No critical blockers exist.

The three tech debt items are:
1. `19-01-SUMMARY.md` frontmatter documentation gap (trivial)
2. Phase 21 runtime behaviors needing human browser testing (non-blocking)
3. `useSearchParams` Suspense question (disputed, intentional decision, verify with `next build`)

**Recommended next step:** Run `next build` to confirm or dispel the Suspense concern, do a quick browser smoke test of the 3 human-needed behaviors, then proceed to `/gsd:complete-milestone 1.4`.

---

_Audited: 2026-05-15_
_Auditor: Claude (gsd-integration-checker + orchestrator)_
