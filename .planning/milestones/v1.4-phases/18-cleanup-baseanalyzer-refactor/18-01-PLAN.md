---
phase: 18-cleanup-baseanalyzer-refactor
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - xeter/services/diagnosticer/main.py
  - xeter/services/presenter/routers/spans.py
  - xeter/tests/diagnosticer/test_diagnose_endpoint.py
  - xeter/services/analyser/batch.py
  - xeter/services/analyser/s3.py
  - xeter/services/diagnosticer/context_assembly.py
  - xeter/services/diagnosticer/providers/__init__.py
  - xeter/services/diagnosticer/providers/ollama.py
  - xeter/services/presenter/main.py
  - xeter/services/presenter/routers/spans.py
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03

must_haves:
  truths:
    - "verify_session_token() is absent from diagnosticer/main.py and the file still imports cleanly"
    - "The diagnosticer test suite passes with no import error from test_diagnose_endpoint.py"
    - "Stale 'NO PostgreSQL RLS' / 'no RLS' comments in spans.py accurately reflect post-migration-004 reality"
    - "Every environ.get() default across all services is documented inline with its safety status"
  artifacts:
    - path: "xeter/services/diagnosticer/main.py"
      provides: "Diagnosticer app without dead verify_session_token function"
      contains: "InternalApiKeyMiddleware"
    - path: "xeter/services/presenter/routers/spans.py"
      provides: "spans router with accurate RLS comments"
    - path: "xeter/tests/diagnosticer/test_diagnose_endpoint.py"
      provides: "test file not importing the deleted function"
  key_links:
    - from: "xeter/tests/diagnosticer/test_diagnose_endpoint.py"
      to: "xeter/services/diagnosticer/main.py"
      via: "import of app and get_ch_client only (verify_session_token removed)"
      pattern: "from xeter.services.diagnosticer.main import app, get_ch_client"
    - from: "xeter/services/diagnosticer/main.py"
      to: "InternalApiKeyMiddleware"
      via: "app.add_middleware — sole auth boundary"
      pattern: "app.add_middleware\\(InternalApiKeyMiddleware\\)"
---

<objective>
Remove v1.3 tech debt: delete the dead `verify_session_token()` function from
`diagnosticer/main.py`, correct stale "NO PostgreSQL RLS" comments in `spans.py`,
and annotate all `os.environ.get()` defaults across services with their safety status.

Purpose: Clean state before the v1.4 hierarchy refactor lands. Dead code causes confusion
about which auth boundary actually protects Diagnosticer. Stale comments about RLS create
false impressions after migration 004 added it. Undocumented env defaults are a latent
security risk flagged in the pending todos since 2026-04-24.

Output: Three source files with dead code removed/corrected, multiple files with
annotated env var defaults, all existing tests still pass.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Key facts extracted from codebase for executor. No codebase exploration needed. -->

From xeter/services/diagnosticer/main.py (current state):
- Line 53-54: SECRET_KEY and INTERNAL_API_KEY are required (KeyError if unset) — SAFE
- Line 78-94: verify_session_token() — defined but NEVER used as Depends() in any route
- Line 71: InternalApiKeyMiddleware is the SOLE auth boundary (app.add_middleware line 71)
- The /diagnose endpoint (line 139) does NOT use verify_session_token as Depends()
- Imports at top: `from jose import JWTError, jwt` — used only by verify_session_token

From xeter/tests/diagnosticer/test_diagnose_endpoint.py:
- Line 32: `from xeter.services.diagnosticer.main import app, get_ch_client, verify_session_token`
- verify_session_token is imported but NEVER referenced in the test body
- After deletion: import line must drop verify_session_token

From xeter/services/presenter/routers/spans.py (RLS comment locations):
- Line 9 (docstring): "span_scores has RLS (migration 004)" — ALREADY CORRECT
- Line 161 (list docstring): "no RLS — explicit tenant_id filter" — STALE; span_scores HAS RLS since migration 004
- Line 247 (inline comment): "NO RLS — must include tenant_id filter" — STALE; both RLS and explicit filter exist
- Line 442 (inline comment): "span_scores has RLS (migration 004) + explicit WHERE tenant_id — both layers required" — ALREADY CORRECT
- Correct text to use: "span_scores has RLS (migration 004) + explicit tenant_id filter — both layers enforce isolation"

Env var defaults audit — full list across all services:

SAFE (infrastructure defaults matching docker-compose):
  analyser/batch.py:   CLICKHOUSE_HOST="clickhouse", CLICKHOUSE_PORT="8123", CLICKHOUSE_DB="default", CLICKHOUSE_USER="default", XETER_BATCH_SIZE="100", XETER_FLUSH_INTERVAL="5"
  analyser/batch.py:   CLICKHOUSE_PASSWORD="" — DOCUMENT as "empty default; override in production"
  analyser/s3.py:      S3_BUCKET="xeter-payloads" — SAFE (bucket name, not a secret)
  diagnosticer/context_assembly.py: S3_BUCKET="xeter-payloads", S3_ENDPOINT_URL="http://minio:9000" — SAFE docker defaults
  diagnosticer/context_assembly.py: S3_ACCESS_KEY, S3_SECRET_KEY via os.environ.get() with no default — returns None silently (not KeyError); DOCUMENT as "should use os.environ[] in production"
  diagnosticer/providers/__init__.py: DIAGNOSTICER_PROVIDER="anthropic", DIAGNOSTICER_MODEL per-provider defaults — SAFE
  diagnosticer/providers/ollama.py: OLLAMA_HOST="http://ollama:11434" — SAFE docker default
  presenter/main.py:   DIAGNOSTICER_URL="http://diagnosticer:8001" — SAFE docker default
  presenter/main.py:   ENVIRONMENT="dev" — DOCUMENT as "must be 'production' in prod"
  presenter/main.py:   CORS_ALLOW_ORIGINS="http://localhost:3000" — DOCUMENT as "must be restricted in prod"
  presenter/routers/spans.py: S3_ENDPOINT_URL="http://minio:9000", S3_ACCESS_KEY="", S3_SECRET_KEY="" — DOCUMENT S3_ACCESS_KEY/SECRET_KEY empty defaults as "must be set in production"
  worker/main.py: EMBEDDER_URL="http://embedder:8002" — SAFE docker default
  worker/main.py: threshold floats — SAFE calibration values
  presenter/diagnosis_service.py: DIAGNOSTICER_TIMEOUT_SECONDS=60.0 — SAFE
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead verify_session_token from diagnosticer</name>
  <files>
    xeter/services/diagnosticer/main.py
    xeter/tests/diagnosticer/test_diagnose_endpoint.py
  </files>
  <action>
    In xeter/services/diagnosticer/main.py:
    1. Delete the entire verify_session_token() function (lines 78-94, the function defined with Header default, JWTError try/except, and return tenant_id).
    2. Remove the jose imports that were exclusively used by that function:
       - Remove `from jose import JWTError, jwt`
       - Remove `ALGORITHM = "HS256"` constant (used only in verify_session_token)
       - Remove `SECRET_KEY = os.environ["SECRET_KEY"]` — used only in verify_session_token; the diagnosticer auth is purely INTERNAL_API_KEY via middleware
    3. Keep INTERNAL_API_KEY and InternalApiKeyMiddleware intact — they are the real auth boundary.
    4. Add a brief comment above InternalApiKeyMiddleware: "# Sole auth boundary for Diagnosticer — all routes require X-Internal-Api-Key"

    In xeter/tests/diagnosticer/test_diagnose_endpoint.py:
    1. Remove verify_session_token from the import line (line 32):
       Change: `from xeter.services.diagnosticer.main import app, get_ch_client, verify_session_token`
       To:     `from xeter.services.diagnosticer.main import app, get_ch_client`
    2. Do NOT change any other line — verify_session_token was imported but never used in the test body.
  </action>
  <verify>
    <automated>cd "C:/Users/raphg/Desktop/Xeter/Xeter" &amp;&amp; python -c "from xeter.services.diagnosticer.main import app, get_ch_client; print('import ok')" &amp;&amp; python -m pytest xeter/tests/diagnosticer/ -x -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    verify_session_token is absent from diagnosticer/main.py; module imports without error;
    diagnosticer tests pass (7 cases: 200, 401x2, 404, 502, 422, healthz).
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct stale RLS comments in spans.py</name>
  <files>
    xeter/services/presenter/routers/spans.py
  </files>
  <action>
    In xeter/services/presenter/routers/spans.py, update two stale comments that say
    span_scores has NO RLS (migration 004 added RLS in Phase 14):

    Location 1 — line 161, inside the GET /spans docstring step list:
      Old: "Query PostgreSQL span_scores for those span_ids (no RLS — explicit tenant_id filter)."
      New: "Query PostgreSQL span_scores for those span_ids (RLS via migration 004 + explicit tenant_id filter)."

    Location 2 — line 247, inline comment before the span_scores SQL query:
      Old: "# --- Step 3: span_scores query (NO RLS — must include tenant_id filter) ---"
      New: "# --- Step 3: span_scores query (RLS via migration 004 + explicit tenant_id filter) ---"

    Lines 9 and 442 already have correct text — do not touch them.

    The explicit tenant_id WHERE clause must remain in both SQL queries — both layers
    (RLS + explicit filter) are intentional belt-and-suspenders.
  </action>
  <verify>
    <automated>grep -n "NO RLS\|no RLS" "C:/Users/raphg/Desktop/Xeter/Xeter/xeter/services/presenter/routers/spans.py" 2>&amp;1; echo "exit:$?"</automated>
  </verify>
  <done>
    grep finds zero matches for "NO RLS" or "no RLS" in spans.py (ignoring auth.py line 143
    which is a correct comment about tenant bootstrap having no RLS — that one is NOT changed).
    All four comment sites in spans.py accurately describe the current state.
  </done>
</task>

<task type="auto">
  <name>Task 3: Annotate env var defaults with safety status</name>
  <files>
    xeter/services/analyser/batch.py
    xeter/services/analyser/s3.py
    xeter/services/diagnosticer/context_assembly.py
    xeter/services/diagnosticer/providers/__init__.py
    xeter/services/diagnosticer/providers/ollama.py
    xeter/services/presenter/main.py
    xeter/services/presenter/routers/spans.py
    xeter/services/presenter/diagnosis_service.py
    xeter/services/worker/main.py
  </files>
  <action>
    Add inline comments to each os.environ.get() call to document safety status.
    Use one of three tags: [safe-default], [must-set-in-prod], [empty-ok].

    Rules for each file:

    xeter/services/analyser/batch.py:
      CLICKHOUSE_HOST, PORT, DB, USER, BATCH_SIZE, FLUSH_INTERVAL → append "  # [safe-default] docker-compose value"
      CLICKHOUSE_PASSWORD="" → append "  # [must-set-in-prod] empty default; override with real password"

    xeter/services/analyser/s3.py:
      S3_BUCKET="xeter-payloads" → append "  # [safe-default] bucket name only, not a secret"

    xeter/services/diagnosticer/context_assembly.py:
      S3_BUCKET="xeter-payloads" → "  # [safe-default]"
      S3_ENDPOINT_URL="http://minio:9000" → "  # [safe-default] docker-compose value"
      os.environ.get("S3_ACCESS_KEY") with no default → append "  # [must-set-in-prod] returns None silently; use os.environ[] for fail-fast"
      os.environ.get("S3_SECRET_KEY") with no default → append "  # [must-set-in-prod] returns None silently; use os.environ[] for fail-fast"

    xeter/services/diagnosticer/providers/__init__.py:
      DIAGNOSTICER_PROVIDER="anthropic" → "  # [safe-default]"
      DIAGNOSTICER_MODEL per-provider defaults → "  # [safe-default]"

    xeter/services/diagnosticer/providers/ollama.py:
      OLLAMA_HOST="http://ollama:11434" → "  # [safe-default] docker-compose value"

    xeter/services/presenter/main.py:
      DIAGNOSTICER_URL="http://diagnosticer:8001" → "  # [safe-default] docker-compose value"
      ENVIRONMENT="dev" → "  # [must-set-in-prod] must be 'production' in production"
      CORS_ALLOW_ORIGINS="http://localhost:3000" → "  # [must-set-in-prod] restrict to actual frontend origin"

    xeter/services/presenter/routers/spans.py (S3 block ~lines 361-364):
      S3_BUCKET="xeter-payloads" → "  # [safe-default]"
      S3_ENDPOINT_URL="http://minio:9000" → "  # [safe-default] docker-compose value"
      S3_ACCESS_KEY="" → "  # [must-set-in-prod] empty default; must be set in production"
      S3_SECRET_KEY="" → "  # [must-set-in-prod] empty default; must be set in production"

    xeter/services/presenter/diagnosis_service.py:
      DIAGNOSTICER_TIMEOUT_SECONDS=60.0 → "  # [safe-default]"

    xeter/services/worker/main.py:
      EMBEDDER_URL="http://embedder:8002" → "  # [safe-default] docker-compose value"
      threshold floats → "  # [safe-default] calibration value; tune via calibration scripts"

    Do NOT change any os.environ[] (bracket) access — those already fail-fast on missing vars.
    Do NOT change SECRET_KEY, INTERNAL_API_KEY, REDIS_URL, DATABASE_URL — they are required.
  </action>
  <verify>
    <automated>grep -rn "\[must-set-in-prod\]" "C:/Users/raphg/Desktop/Xeter/Xeter/xeter/services/" 2>&amp;1 | grep -v __pycache__ | wc -l</automated>
  </verify>
  <done>
    At least 7 [must-set-in-prod] annotations exist across the service files
    (CLICKHOUSE_PASSWORD, S3_ACCESS_KEY x2, S3_SECRET_KEY x2, ENVIRONMENT, CORS_ALLOW_ORIGINS).
    All [safe-default] annotations applied. Full test suite still passes:
    `pytest xeter/tests/ -x -q --ignore=xeter/tests/validation -q` exits 0.
  </done>
</task>

</tasks>

<verification>
Run the full non-validation test suite to confirm no regressions:
  cd "C:/Users/raphg/Desktop/Xeter/Xeter" &amp;&amp; python -m pytest xeter/tests/ -x -q --ignore=xeter/tests/validation 2>&amp;1 | tail -10

Confirm dead code gone:
  python -c "import inspect; from xeter.services.diagnosticer import main; print([n for n in dir(main) if 'session' in n.lower()])"
  Expected: empty list (no verify_session_token)

Confirm stale comments gone:
  grep -n "NO RLS\|no RLS" xeter/services/presenter/routers/spans.py | wc -l
  Expected: 0
</verification>

<success_criteria>
1. verify_session_token() is absent from diagnosticer/main.py; module and all tests import cleanly
2. spans.py lines 161 and 247 now say "RLS via migration 004" (no "NO RLS" / "no RLS" remaining in spans.py)
3. All os.environ.get() defaults annotated with [safe-default] or [must-set-in-prod] across 9 service files
4. Full test suite (excluding integration/validation) exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/18-cleanup-baseanalyzer-refactor/18-01-SUMMARY.md`
</output>
