# Requirements Archive: Xeter v1.5 — Silent Failure Detection

**Archived:** 2026-05-30
**Status:** ✅ SHIPPED
**Core Value:** When a tool call fails, tell the developer whether it was the model, the architecture, or the prompt — and why.

---

## v1.5 Requirements (All Complete)

### INFRA — Infrastructure Prerequisites

- [x] **INFRA-01**: Worker processes pending trace flushes on BRPOP timeout — idle traces flushed and analyzed correctly — **Phase 22 ✓**
- [x] **INFRA-02**: TraceAnalyzer score data persisted — flush_scores() called; calibration dataset includes trace-level metrics — **Phase 22 ✓**
- [x] **INFRA-03**: calibrate.py supports multiple analyzer classes; hill-climb enforces R ≥ 0.10 — **Phase 23 ✓**
- [x] **INFRA-04**: Worker pyproject.toml includes jsonschema==4.26.0, tiktoken==0.13.0, rapidfuzz==3.14.5 — **Phase 23 ✓**
- [x] **INFRA-05**: SpanData has expected_output_schema field; ClickHouse column added; span_fetcher maps it — **Phase 23 ✓**
- [x] **INFRA-06**: SpanData has parent_span_id field; ClickHouse confirmed; span_fetcher maps it — **Phase 23 ✓**

### SCHEMA — Output/Schema Failure Checks

- [x] **SCHEMA-01**: output_schema_violation flag when model returns free text instead of structured output — **Phase 24 ✓**
- [x] **SCHEMA-02**: required_fields_missing flag when tool_arguments passes JSON parse but fails required validator — **Phase 24 ✓**
- [x] **SCHEMA-03**: output_truncated flag when model output is cut short — **Phase 24 ✓**
- [x] **SCHEMA-04**: type_coercion_error flag when tool_arguments has type violations — **Phase 24 ✓**

### CTX — Context/Content Checks

- [x] **CTX-01**: context_overflow flag when prompt token count exceeds threshold — **Phase 24 ✓**
- [x] **CTX-02**: stale_context flag (best-effort) when tool output appears reused without re-query — **Phase 25 ✓**
- [x] **CTX-04**: missing_details flag when response does not semantically cover items requested in prompt — **Phase 25 ✓**

### TRACE — Trace-Level Checks

- [x] **TRACE-01**: step_repetition flag when trace contains duplicate/near-duplicate tool calls — **Phase 25 ✓** (algorithm refined in Phase 28: binary exact-match)
- [x] **TRACE-02**: termination_loop flag when same tool called N+ times without exit — **Phase 25 ✓**
- [x] **TRACE-03**: context_propagation_failure flag when later span's prompt missing key information from earlier tool_output — **Phase 25 ✓**
- [x] **TRACE-04**: history_loss flag when later span's prompt semantically disconnected from trace centroid — **Phase 25 ✓** (P=0.5 accepted — architectural limitation)
- [x] **TRACE-05**: wrong_agent_handoff flag (best-effort) when agent-name sequence deviates from AGENT_ROUTING_GRAPH — **Phase 26 ✓**
- [x] **TRACE-06**: information_withholding flag when agent response contains key entities not passed to next span's prompt — **Phase 26 ✓** (algorithm refined Phase 28: binary check)
- [x] **TRACE-07**: conversation_reset flag (best-effort) when abrupt topic shift detected via embedding cosine drop — **Phase 26 ✓**
- [x] **TRACE-08**: clarification_skipped flag when span proceeds on ambiguous prompt without asking for clarification — **Phase 26 ✓**
- [x] **TRACE-09**: no_verification flag when completed trace has no verification-type tool call — **Phase 26 ✓**
- [x] **TRACE-10**: incomplete_verification flag when verification span covers fewer entities than produced — **Phase 26 ✓**

### CAL — Calibration

- [x] **CAL-01**: All 17 new flag types calibrated; recall floor R ≥ 0.10 enforced; full-suite mean precision ≥ 95% — **Phases 27+28 ✓** (mean precision 0.947; conditionally met — history_loss P=0.5 is accepted architectural exception)

---

## Requirement Outcomes

| Requirement | Outcome | Note |
|-------------|---------|------|
| INFRA-01..06 | Validated | All infra prerequisites in place |
| SCHEMA-01..04 | Validated | All 4 deterministic span checks shipping at P=1.0 |
| CTX-01 | Validated | context_overflow calibrated |
| CTX-02 | Validated | stale_context marked low_confidence |
| CTX-04 | Validated | missing_details shipping at P=1.0 after Phase 28 fix |
| TRACE-01..04 | Validated | 4 trace checks shipping; history_loss P=0.5 accepted |
| TRACE-05..10 | Validated | 6 best-effort proxy checks shipping |
| CAL-01 | Conditionally validated | Mean P=0.947; history_loss accepted exception |

## Dropped / Out of Scope

- **CTX-03 (prompt_injection)**: Permanently removed. Insufficient reliable OTel signal without LLM-in-the-loop evaluation.
- **E3 (prompt injection)**: Same reasoning as CTX-03.

## Future Requirements (carried to v1.6)

- python-jose → PyJWT migration (AUTH-F02)
- Refresh token revocation store (AUTH-F01)
- Rate limiting on Analyser ingestion (OPS-F01)
- TypeScript/Node.js SDK (SDK-F01)
- Per-tenant Redis queue keys (OPS-F02)

---

*For current requirements, see next milestone's REQUIREMENTS.md*
