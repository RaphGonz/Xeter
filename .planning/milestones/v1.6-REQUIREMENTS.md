# Requirements Archive — v1.6 Release

**Archived:** 2026-05-31
**Milestone:** v1.6 Release
**Status:** ✅ All requirements complete

---

## Milestone Goal

Ship Xeter publicly: apply a license, clean up dev artifacts, improve the diagnosticer prompt, and produce exhaustive documentation covering installation, deployment, the SDK, all 24 detection checks, calibration, pluggable LLM configuration, and performance optimization hints.

---

## v1.6 Requirements

### Licensing

- [x] **LICENSE-01**: A `LICENSE` file exists at repo root containing GPL-3.0 full text followed by a Commons Clause 1.0 addendum that explicitly prohibits selling Xeter as a service
- [x] **LICENSE-02**: SPDX license identifier header (`SPDX-License-Identifier: GPL-3.0-only WITH Commons-Clause-1.0`) added to all top-level Python source files across analyser, presenter, worker, diagnosticer, and SDK
- [x] **LICENSE-03**: README displays a license badge linked to the LICENSE file

### Assets & Housekeeping

- [x] **ASSETS-01**: `assets/` folder created at repo root; `logo+typo.png` moved from root to `assets/logo+typo.png`; `.gitignore` and any path references updated
- [x] **CLEAN-01**: `check_tier4.py` deleted from repo root (dead calibration dev script, no longer needed)
- [x] **CLEAN-02**: `VALIDATION-REPORT.md` deleted from repo root (stale dev artifact, not part of public release)

### Diagnosticer Prompt

- [x] **DIAG-01**: Inline prompt string extracted from `xeter/services/diagnosticer/context_assembly.py:_format_context()` into a dedicated file `xeter/services/diagnosticer/prompt.md`; `_format_context()` reads the file at import time and substitutes span data into it
- [x] **DIAG-02**: Extracted prompt rewritten with: (a) a system message section that frames the diagnosticer role, (b) a chain-of-thought scaffold that walks through each flag before reaching a verdict, (c) explicit decision criteria for the four verdicts (`model` / `architecture` / `prompt` / `unknown`), and (d) guidance on severity calibration

### Documentation

- [x] **DOCS-01**: README displays a banner image (`assets/logo+typo.png`) at the top followed by a one-sentence tagline and a license badge
- [x] **DOCS-02**: README contains a **Quick Start** section covering local install (Python env + Docker Compose) with exact commands: `generate-secrets.sh`, `docker compose up`, health-check verification
- [x] **DOCS-03**: README contains an **SDK** section showing how to instrument an agent: install, `@xeter_sdk.trace` decorator usage, env vars (`XETER_ENDPOINT`, `XETER_API_KEY`), and a minimal working example
- [x] **DOCS-04**: README contains a **Detection Checks** section with a table of all 24 flag types: flag type, analyzer class, description, threshold type (binary / tunable), and default threshold value where applicable
- [x] **DOCS-05**: README contains a **Calibration** section explaining how to reset thresholds, add fixture spans, run `calibrate.py --flag-type <type>` on real data, and apply the output to `docker-compose.yml` via `WORKER_THRESHOLD_*` env vars
- [x] **DOCS-06**: README contains a **Pluggable LLM** section listing the three supported providers (Anthropic, OpenAI, Ollama), the env vars to configure each (`DIAGNOSTICER_PROVIDER`, `DIAGNOSTICER_MODEL`, `ANTHROPIC_API_KEY` / `OPENAI_API_KEY` / `OLLAMA_BASE_URL`), and a note on using local models via Ollama
- [x] **DOCS-07**: README contains a **Performance** section covering the two real tuning levers: `WORKER_TRACE_FLUSH_TIMEOUT_S` tuning (flush latency vs context richness tradeoff) and embedding model swap guidance (model size vs latency/accuracy tradeoff). Redis queue sizing, disabling unused analyzers, and ClickHouse retention are explicitly out of scope (D-P-01, D-P-02, D-P-03).

---

## Traceability

| REQ-ID | Phase | Status | Outcome |
|--------|-------|--------|---------|
| LICENSE-01 | 29 | ✅ Complete | GPL-3.0 + Commons Clause 1.0 LICENSE file (35 KB) at repo root |
| LICENSE-02 | 29 | ✅ Complete | SPDX headers added to all 90 substantive Python source files |
| ASSETS-01 | 29 | ✅ Complete | assets/ directory created; logo+typo.png moved via git mv |
| CLEAN-01 | 29 | ✅ Complete | check_tier4.py deleted (was untracked, removed from disk) |
| CLEAN-02 | 29 | ✅ Complete | VALIDATION-REPORT.md removed via git rm |
| DIAG-01 | 30 | ✅ Complete | Prompt extracted to prompt.md; import-time read via Path(__file__).parent; format_map substitution |
| DIAG-02 | 30 | ✅ Complete | prompt.md rewritten: system message, four-verdict criteria, severity calibration, CoT scaffold |
| LICENSE-03 | 31 | ✅ Complete | GPL badge in README Section 1 + prose in Section 11 |
| DOCS-01 | 31 | ✅ Complete | Banner + tagline + badge at README top |
| DOCS-02 | 31 | ✅ Complete | Quick Start with 4-command sequence, seeded credentials, ports table |
| DOCS-03 | 31 | ✅ Complete | SDK section with install + @xeter.trace decorator + env vars + example |
| DOCS-04 | 31 | ✅ Complete | 24-row detection table from FLAG_TYPE_TO_ANALYZER_CLASS registry |
| DOCS-05 | 31 | ✅ Complete | Calibration section with 4-step workflow + WORKER_THRESHOLD_* application |
| DOCS-06 | 31 | ✅ Complete | Pluggable LLM section: 3 providers + env vars table + Ollama note |
| DOCS-07 | 31 | ✅ Complete | Performance section: 2 levers only (WORKER_TRACE_FLUSH_TIMEOUT_S + embedder swap) |

---

## Future Requirements (carried to next milestone)

- TypeScript/Node.js SDK for JS-based agents (SDK-F01) — deferred from v1.5
- Refresh token revocation store (AUTH-F01) — server-side blacklist
- python-jose → PyJWT migration (AUTH-F02)
- Rate limiting on Analyser ingestion (OPS-F01) — per-API-key sliding window, 429 + Retry-After
