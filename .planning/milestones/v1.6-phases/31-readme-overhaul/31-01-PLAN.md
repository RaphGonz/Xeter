---
phase: 31-readme-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/docker-compose.yml
autonomous: true
requirements:
  - DOCS-02

must_haves:
  truths:
    - "Running docker compose up applies migrations and seeds dev data automatically — no separate CLI commands needed"
    - "The migrate+seed step completes before worker, presenter, analyser, and diagnosticer services start"
    - "Re-running docker compose up on an already-initialized DB is safe (seed is idempotent)"
  artifacts:
    - path: "deploy/docker-compose.yml"
      provides: "db-init service definition"
      contains: "service_completed_successfully"
  key_links:
    - from: "db-init"
      to: "worker / presenter / analyser / diagnosticer"
      via: "depends_on condition: service_completed_successfully"
      pattern: "db-init"
---

<objective>
Add a `db-init` init container service to `deploy/docker-compose.yml` that runs Alembic migrations followed by the seed script before application services start.

Purpose: The Quick Start flow in the new README is `generate-secrets.sh && docker compose up` — no separate migration or seed steps. This init container makes that promise true.

Output: Updated `deploy/docker-compose.yml` with a `db-init` service and updated `depends_on` blocks for worker, presenter, analyser, and diagnosticer.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add db-init service and update depends_on blocks</name>
  <files>deploy/docker-compose.yml</files>
  <read_first>
    - deploy/docker-compose.yml — read the full file to understand current service structure, existing depends_on patterns (e.g., minio-init uses condition: service_healthy on minio), and where the new service fits
    - xeter/scripts/seed.py — confirm the module path is `xeter.scripts.seed` (not a subdirectory)
    - xeter/migrations/alembic.ini — confirm alembic.ini path for the -c flag
  </read_first>
  <action>
    Insert a new `db-init` service in the "Infrastructure services" block of deploy/docker-compose.yml, placed after the `minio-init` service and before the application services comment.

    The service definition:
    - image: mirror the build context from the `presenter` service (same Dockerfile: `services/presenter/Dockerfile`, context `..`) so no separate image is needed
    - depends_on: postgres with condition: service_healthy
    - environment: pass DATABASE_URL as `postgresql+asyncpg://xeter:${POSTGRES_PASSWORD}@postgres:5432/xeter` (same value used by presenter)
    - command: run two commands sequentially — `python -m alembic -c xeter/migrations/alembic.ini upgrade head` followed by `python -m xeter.scripts.seed`; use a shell entrypoint (`sh -c "... && ..."`) to chain them
    - restart: "no" — this is a one-shot init container; it must not restart

    Then update the depends_on block for each of these four services — worker, presenter, analyser, diagnosticer — adding:
    ```
    db-init:
      condition: service_completed_successfully
    ```
    Each of these services already has a depends_on block; add db-init as an additional entry alongside existing dependencies. Do not remove any existing depends_on entries.

    Do not add a healthcheck to db-init (it exits after completion; healthchecks are for long-running services).
  </action>
  <verify>
    <automated>cd C:\Users\raphg\Desktop\Xeter\Xeter && python -c "import yaml; d = yaml.safe_load(open('deploy/docker-compose.yml')); s = d['services']['db-init']; assert 'postgres' in s['depends_on']; assert s.get('restart') == 'no'; deps_worker = d['services']['worker']['depends_on']; assert 'db-init' in deps_worker; deps_presenter = d['services']['presenter']['depends_on']; assert 'db-init' in deps_presenter; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - deploy/docker-compose.yml contains a `db-init:` service entry
    - db-init depends_on includes `postgres` with `condition: service_healthy`
    - db-init command chains `alembic upgrade head` and `python -m xeter.scripts.seed` via shell
    - db-init has `restart: "no"`
    - worker, presenter, analyser, and diagnosticer each have `db-init: condition: service_completed_successfully` in their depends_on blocks
    - All existing depends_on entries for those four services are preserved (no removals)
    - `docker compose -f deploy/docker-compose.yml config` exits 0 (valid YAML)
  </acceptance_criteria>
  <done>db-init service present; four application services block on it; compose config is valid YAML</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| db-init → postgres | Init container connects using DATABASE_URL — same trust boundary as all other services |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01-01 | Tampering | db-init seed script | accept | Seed is idempotent (upsert semantics); re-running on existing data cannot corrupt state |
| T-31-01-02 | Denial of Service | db-init failure blocks all app services | mitigate | `restart: "no"` prevents infinite retry loops; failure surfaces immediately in compose logs |
| T-31-01-SC | Tampering | No new npm/pip/cargo installs in this plan | accept | No package manager operations; no legitimacy audit required |
</threat_model>

<verification>
- `docker compose -f deploy/docker-compose.yml config` exits 0 with no YAML errors
- db-init service present in parsed YAML
- worker, presenter, analyser, diagnosticer all list `db-init` in depends_on with `condition: service_completed_successfully`
</verification>

<success_criteria>
After docker compose up, migrations and seed run automatically before any application service accepts traffic. Running docker compose up a second time on an already-initialized DB completes without error.
</success_criteria>

<output>
Create `.planning/phases/31-readme-overhaul/31-01-SUMMARY.md` when done.
</output>
