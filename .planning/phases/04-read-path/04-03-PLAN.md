---
phase: 04-read-path
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - xeter/services/diagnosticer/__init__.py
  - xeter/services/diagnosticer/main.py
  - services/diagnosticer/__init__.py
  - services/diagnosticer/Dockerfile
  - deploy/docker-compose.yml
  - xeter/services/presenter/routers/diagnose.py
  - xeter/services/presenter/main.py
  - xeter/tests/presenter/test_diagnose.py
autonomous: true
requirements:
  - INFR-02

must_haves:
  truths:
    - "POST /diagnose on the Presenter proxies to the Diagnosticer service and returns 501 with placeholder body"
    - "Diagnosticer is a separate container with its own Dockerfile and docker-compose entry"
    - "Diagnosticer docker-compose entry has DB/S3 env vars wired (even though scaffold only returns 501)"
    - "POST /diagnose on the Presenter requires a valid session token (401 without)"
    - "Diagnosticer GET /healthz returns 200"
  artifacts:
    - path: "xeter/services/diagnosticer/main.py"
      provides: "Diagnosticer FastAPI scaffold returning 501"
    - path: "services/diagnosticer/Dockerfile"
      provides: "Diagnosticer Docker image"
    - path: "xeter/services/presenter/routers/diagnose.py"
      provides: "POST /diagnose proxy route on Presenter"
  key_links:
    - from: "xeter/services/presenter/routers/diagnose.py"
      to: "xeter/services/diagnosticer/main.py"
      via: "httpx.AsyncClient proxy on app.state.http_client"
      pattern: "app\\.state\\.http_client"
    - from: "deploy/docker-compose.yml"
      to: "services/diagnosticer/Dockerfile"
      via: "diagnosticer service definition"
      pattern: "diagnosticer:"
---

<objective>
Diagnosticer service scaffold and POST /diagnose proxy on the Presenter.

Purpose: Scaffolds the Diagnosticer as a separate service (own Dockerfile, own container) returning 501, wires it to the Presenter via HTTP proxy, and adds the docker-compose entry with all DB/S3 connections ready for v2 LLM integration.
Output: Diagnosticer scaffold, Presenter proxy route, docker-compose integration, unit tests.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-read-path/04-RESEARCH.md
@.planning/phases/04-read-path/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->

From xeter/services/presenter/deps.py:
```python
def verify_session_token(authorization: str = Header(...)) -> str:
    # Returns tenant_id string
```

From xeter/services/presenter/main.py (Plan 01):
```python
# Lifespan already sets app.state.ch_client
# Plan 03 adds app.state.http_client (httpx.AsyncClient) to lifespan
```

From deploy/docker-compose.yml:
```yaml
# Plan 01 already added S3/SECRET_KEY/DIAGNOSTICER_URL env vars to presenter
# Plan 03 adds the diagnosticer service entry
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnosticer scaffold service (FastAPI + Dockerfile + docker-compose)</name>
  <files>
    xeter/services/diagnosticer/__init__.py
    xeter/services/diagnosticer/main.py
    services/diagnosticer/__init__.py
    services/diagnosticer/Dockerfile
    deploy/docker-compose.yml
  </files>
  <action>
1. Create `xeter/services/diagnosticer/__init__.py` (empty).

2. Create `xeter/services/diagnosticer/main.py`:
   - Minimal FastAPI app: `app = FastAPI(title="Xeter Diagnosticer", version="0.1.0-scaffold")`
   - `DiagnoseRequest(BaseModel)`: `span_id: str`, `flags: list`
   - `POST /diagnose` handler with `status_code=501`:
     - Accepts DiagnoseRequest body
     - Returns `{"status": "not_implemented", "message": "Diagnosticer not yet available", "span_id": body.span_id}`
   - `GET /healthz` handler: returns `{"status": "ok"}`

3. Create `services/diagnosticer/__init__.py` (empty).

4. Create `services/diagnosticer/Dockerfile`:
   - Based on `python:3.12-slim` (same as presenter/analyser)
   - WORKDIR /app, ENV PYTHONPATH=/app
   - COPY and pip install xeter package (same pattern as presenter Dockerfile)
   - CMD: `uvicorn xeter.services.diagnosticer.main:app --host 0.0.0.0 --port 8001 --reload`

5. Add `diagnosticer` service to `deploy/docker-compose.yml`:
   ```yaml
   diagnosticer:
     build:
       context: ..
       dockerfile: services/diagnosticer/Dockerfile
     ports:
       - "8001:8001"
     volumes:
       - ../xeter:/app/xeter
     command: uvicorn xeter.services.diagnosticer.main:app --host 0.0.0.0 --port 8001 --reload
     depends_on:
       postgres:
         condition: service_healthy
       clickhouse:
         condition: service_healthy
       minio:
         condition: service_healthy
     environment:
       DATABASE_URL: postgresql+asyncpg://xeter:xeter_dev_password@postgres:5432/xeter
       CLICKHOUSE_HOST: clickhouse
       S3_ENDPOINT_URL: http://minio:9000
       S3_ACCESS_KEY: xeter
       S3_SECRET_KEY: xeter_dev_password
       S3_BUCKET: xeter-payloads
   ```

6. Add `diagnosticer` to presenter's `depends_on` in docker-compose (presenter needs diagnosticer running for proxy). Use `condition: service_started` (no healthcheck needed for scaffold).
  </action>
  <verify>
    <automated>cd C:/Users/raphg/Desktop/Xeter/Xeter && python -c "from xeter.services.diagnosticer.main import app; print('Diagnosticer app imported OK')"</automated>
  </verify>
  <done>Diagnosticer scaffold exists as separate service. Dockerfile builds. docker-compose entry wired with DB/S3 env vars.</done>
</task>

<task type="auto">
  <name>Task 2: POST /diagnose proxy on Presenter and httpx client in lifespan</name>
  <files>
    xeter/services/presenter/routers/diagnose.py
    xeter/services/presenter/main.py
    xeter/tests/presenter/test_diagnose.py
  </files>
  <action>
1. Create `xeter/services/presenter/routers/diagnose.py`:
   - `DiagnoseRequest(BaseModel)`: `span_id: str`, `flags: list`
   - `POST /diagnose` handler:
     - Depends on verify_session_token for tenant_id (auth required)
     - Gets httpx client from `request.app.state.http_client`
     - Forwards request body to Diagnosticer: `resp = await request.app.state.http_client.post("/diagnose", json=body.model_dump())`
     - Returns `Response(content=resp.content, status_code=resp.status_code, media_type="application/json")`
     - If httpx raises (connection refused, timeout): return 502 with `{"error": "diagnosticer_unavailable", "message": "Diagnosticer service is unavailable"}`

2. Update `xeter/services/presenter/main.py` lifespan:
   - Add `httpx.AsyncClient(base_url=os.environ.get("DIAGNOSTICER_URL", "http://diagnosticer:8001"), timeout=30.0)` stored on `app.state.http_client`
   - On shutdown: `await app.state.http_client.aclose()`
   - Wire diagnose router: `app.include_router(diagnose.router, prefix="", tags=["diagnose"])`

3. Create `xeter/tests/presenter/test_diagnose.py` with tests:
   - `test_diagnose_returns_501`: Mock httpx client to return Response(status_code=501, json={"status": "not_implemented", ...}). Assert Presenter returns 501 with the same body.
   - `test_diagnose_returns_401_without_token`: Call POST /diagnose without Authorization header. Assert 401.
   - `test_diagnose_proxies_request_body`: Mock httpx client, verify it receives the correct span_id and flags in the forwarded request.
   - `test_diagnose_returns_502_on_connection_error`: Mock httpx client to raise httpx.ConnectError. Assert 502 with diagnosticer_unavailable message.
   - Use `app.dependency_overrides` for auth. Mock `app.state.http_client` directly.
  </action>
  <verify>
    <automated>cd C:/Users/raphg/Desktop/Xeter/Xeter/xeter && python -m pytest tests/presenter/test_diagnose.py -x -q</automated>
  </verify>
  <done>POST /diagnose proxies to Diagnosticer and returns 501. Auth required. 4 tests pass.</done>
</task>

</tasks>

<verification>
cd C:/Users/raphg/Desktop/Xeter/Xeter/xeter && python -m pytest tests/presenter/ -x -q
All presenter tests pass (login + list + detail + diagnose).
</verification>

<success_criteria>
1. Diagnosticer service scaffold exists as separate container returning 501
2. POST /diagnose on Presenter proxies request to Diagnosticer
3. Diagnosticer docker-compose entry has DB/S3 env vars wired
4. POST /diagnose requires valid session token (401 without)
5. Diagnosticer GET /healthz returns 200
6. All 4 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-read-path/04-03-SUMMARY.md`
</output>
