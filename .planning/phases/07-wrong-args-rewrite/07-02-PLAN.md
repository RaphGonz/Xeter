---
phase: 07-wrong-args-rewrite
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - xeter/services/worker/base.py
autonomous: true
requirements:
  - HYBRID-01

must_haves:
  truths:
    - "bow_score('hello world', 'hello earth') returns the Jaccard token overlap (0.333...)"
    - "bow_score with an empty string returns 0.0 (no crash)"
    - "hybrid_score(0.8, 0.2) returns 0.5 (50/50 blend)"
    - "hybrid_score with weight=0.7 returns the weighted blend correctly"
    - "Both functions are importable from xeter.services.worker.base"
  artifacts:
    - path: "xeter/services/worker/base.py"
      provides: "bow_score() and hybrid_score() module-level functions"
      exports: ["bow_score", "hybrid_score"]
  key_links:
    - from: "xeter/services/worker/tool_call_analyzer.py"
      to: "xeter/services/worker/base.py"
      via: "from xeter.services.worker.base import bow_score, hybrid_score"
      pattern: "bow_score|hybrid_score"
---

<objective>
Add two pure module-level functions to xeter/services/worker/base.py:

- bow_score(text_a, text_b) -> float: Jaccard token overlap using set operations (stdlib only, no new deps)
- hybrid_score(cosine, bow, weight=0.5) -> float: 50/50 blend of cosine and BOW scores

These become the shared utility used by all four check methods in v1.1 (wrong_args phase 7, wrong_tool phase 8, no_tool+tool_use_violation phase 9, excessive_tool phase 10). Placing them in base.py makes them available to every current and future analyzer without import gymnastics.

Purpose: HYBRID-01 is the cross-cutting foundation for all similarity improvements in v1.1. Plan 07-03 depends on this.

Output: Two functions added to base.py; 16 existing tests still pass; new unit tests for both functions added.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-wrong-args-rewrite/07-RESEARCH.md

Relevant current state of xeter/services/worker/base.py:

BaseAnalyzer class provides embed(), compare(), log_score() — all instance methods.
The file currently has NO module-level utility functions (only dataclasses and the class).
No new dependencies: use stdlib set operations for BOW (confirmed in research).

Import pattern for downstream use in tool_call_analyzer.py:
  from xeter.services.worker.base import BaseAnalyzer, Flag, SpanData
  (add bow_score, hybrid_score to this import)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bow_score and hybrid_score functions to base.py</name>
  <files>xeter/services/worker/base.py</files>
  <action>
Add two module-level functions to the bottom of xeter/services/worker/base.py (after the BaseAnalyzer class). Do NOT modify existing code.

Add this block after the BaseAnalyzer class definition:

    # ---------------------------------------------------------------------------
    # Hybrid scoring utility (HYBRID-01)
    # Used by all similarity-based check methods in ToolCallAnalyzer.
    # ---------------------------------------------------------------------------

    def bow_score(text_a: str, text_b: str) -> float:
        """Jaccard token overlap between two strings. Returns value in [0, 1].

        Tokenizes by lowercased whitespace split. Returns 0.0 if either
        string is empty after splitting (avoids division by zero).

        Used as the BOW component of hybrid_score().
        """
        tokens_a = set(text_a.lower().split())
        tokens_b = set(text_b.lower().split())
        if not tokens_a or not tokens_b:
            return 0.0
        return len(tokens_a & tokens_b) / len(tokens_a | tokens_b)


    def hybrid_score(cosine: float, bow: float, weight: float = 0.5) -> float:
        """50/50 blend of cosine similarity and bag-of-words score.

        Returns weight * cosine + (1 - weight) * bow.
        Default weight=0.5 gives equal contribution (HYBRID-01 specification).

        Args:
            cosine: Cosine similarity from BaseAnalyzer.compare(), in [-1, 1].
            bow:    BOW score from bow_score(), in [0, 1].
            weight: Cosine weight. Default 0.5 (50/50 blend).
        """
        return weight * cosine + (1.0 - weight) * bow

These are pure functions with no side effects and no dependencies beyond Python builtins. No imports needed.
  </action>
  <verify>
    <automated>cd /c/Users/raphg/Desktop/Xeter/Xeter && python -c "from xeter.services.worker.base import bow_score, hybrid_score; assert abs(bow_score('hello world','hello earth') - 1/3) < 0.001; assert bow_score('','x') == 0.0; assert hybrid_score(0.8,0.2) == 0.5; assert hybrid_score(1.0,1.0,0.7) == 1.0; print('PASS')"</automated>
  </verify>
  <done>bow_score and hybrid_score importable from xeter.services.worker.base; bow_score returns Jaccard overlap; hybrid_score returns weighted blend; empty string returns 0.0</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for bow_score and hybrid_score</name>
  <files>xeter/tests/worker/test_tool_call_analyzer.py</files>
  <action>
Append new test functions at the bottom of xeter/tests/worker/test_tool_call_analyzer.py. These tests cover the HYBRID-01 utility functions directly. They do not require an embedder mock.

Add the following import at the top of the test file (alongside existing imports):
  from xeter.services.worker.base import bow_score, hybrid_score

Then append these tests at the end of the file:

    # ---------------------------------------------------------------------------
    # Tests 15–16: HYBRID-01 utility functions (bow_score, hybrid_score)
    # ---------------------------------------------------------------------------

    def test_bow_score_partial_overlap():
        # "hello world" vs "hello earth" — shared: {"hello"}, union: {"hello","world","earth"}
        score = bow_score("hello world", "hello earth")
        assert abs(score - 1/3) < 0.001, f"Expected ~0.333, got {score}"

    def test_bow_score_identical_strings():
        score = bow_score("search for python docs", "search for python docs")
        assert score == 1.0

    def test_bow_score_no_overlap():
        score = bow_score("apple banana", "cherry delta")
        assert score == 0.0

    def test_bow_score_empty_string_returns_zero():
        assert bow_score("", "hello") == 0.0
        assert bow_score("hello", "") == 0.0
        assert bow_score("", "") == 0.0

    def test_hybrid_score_equal_weight():
        # 0.5 * 0.8 + 0.5 * 0.2 = 0.5
        result = hybrid_score(0.8, 0.2)
        assert abs(result - 0.5) < 0.001

    def test_hybrid_score_custom_weight():
        # weight=0.7: 0.7 * 1.0 + 0.3 * 0.0 = 0.7
        result = hybrid_score(1.0, 0.0, weight=0.7)
        assert abs(result - 0.7) < 0.001

    def test_hybrid_score_both_max():
        assert hybrid_score(1.0, 1.0) == 1.0

Note: test numbering is informal (16 existing tests, new tests are 15-16 by group name). The actual count after adding is 22 tests.
  </action>
  <verify>
    <automated>cd /c/Users/raphg/Desktop/Xeter/Xeter && python -m pytest xeter/tests/worker/test_tool_call_analyzer.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All tests pass (16 existing + 7 new = 23 total); no import errors; bow_score and hybrid_score test cases all green</done>
</task>

</tasks>

<verification>
Run: python -m pytest xeter/tests/worker/test_tool_call_analyzer.py -v
Expected: 23 tests pass (16 original + 7 new hybrid utility tests)
Import check: from xeter.services.worker.base import bow_score, hybrid_score succeeds
</verification>

<success_criteria>
- bow_score() and hybrid_score() exist as module-level functions in xeter/services/worker/base.py
- Both are importable from xeter.services.worker.base
- bow_score uses Jaccard token overlap via set operations (no new deps)
- hybrid_score defaults to 50/50 blend but accepts custom weight
- 7 new unit tests all pass
- All 16 original tests still pass
- No changes to BaseAnalyzer class or existing functions
</success_criteria>

<output>
After completion, create .planning/phases/07-wrong-args-rewrite/07-02-SUMMARY.md
</output>
