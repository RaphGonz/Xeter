---
phase: 07-wrong-args-rewrite
plan: "03"
type: execute
wave: 2
depends_on:
  - "07-02"
files_modified:
  - xeter/services/worker/tool_call_analyzer.py
  - xeter/tests/worker/test_tool_call_analyzer.py
autonomous: true
requirements:
  - ARGS-01
  - ARGS-02
  - ARGS-03
  - ARGS-04
  - ARGS-05

must_haves:
  truths:
    - "A span with tool_output containing 'invalid argument' is flagged as wrong_tool_args with score=1.0 and no embed() call"
    - "A span with tool_output 'Email delivered' and semantically mismatched arguments is flagged using hybrid scoring"
    - "A span with all-numeric flattened values (e.g. '10000 * (1 + 0.05)') is NOT flagged (embedding skipped)"
    - "No wrong_tool_args flag detail contains the key low_confidence"
    - "All 10 wrong_tool_args fixture spans (semantic mismatch path) are processed without crash"
  artifacts:
    - path: "xeter/services/worker/tool_call_analyzer.py"
      provides: "Rewritten _check_wrong_args with two-path detection"
      contains: "_WRONG_ARGS_ERROR_PATTERNS"
    - path: "xeter/tests/worker/test_tool_call_analyzer.py"
      provides: "Updated test_7 and new tests for rewritten _check_wrong_args"
  key_links:
    - from: "tool_call_analyzer.py _check_wrong_args()"
      to: "base.py bow_score, hybrid_score"
      via: "from xeter.services.worker.base import bow_score, hybrid_score"
      pattern: "hybrid_score"
    - from: "_check_wrong_args() error path"
      to: "_WRONG_ARGS_ERROR_PATTERNS list"
      via: "any(p.search(span.tool_output) for p in _WRONG_ARGS_ERROR_PATTERNS)"
      pattern: "_WRONG_ARGS_ERROR_PATTERNS"
---

<objective>
Replace _check_wrong_args() in ToolCallAnalyzer with a two-path detector:

Path 1 (ARGS-01): Check tool_output against a compiled regex list. If any error pattern matches, return flag immediately with score=1.0 — no embedding.
Path 2 (ARGS-02/03/04): Flatten argument values to plain text (not raw JSON), apply empty/all-numeric guard, then score with hybrid scoring (50/50 cosine+BOW from 07-02).
ARGS-05: Remove low_confidence: True from all flag details.

Also update test_7 which currently asserts low_confidence is True — it must be updated to assert low_confidence is absent.

Purpose: The old implementation embeds the raw JSON string (keys + values together), making cosine similarity meaningless. The new implementation fixes the signal and removes the exclusion flag.

Output: Rewritten _check_wrong_args; updated test_7; new ARGS-01 and ARGS-04 unit tests; all 23 tests pass.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-wrong-args-rewrite/07-RESEARCH.md

Current _check_wrong_args (lines 180-207 of tool_call_analyzer.py) to be replaced:
  - Embeds span.tool_arguments raw JSON string (BUG: key noise)
  - Includes low_confidence: True in flag detail (removed by ARGS-05)
  - No error pattern check, no value flattening

Pitfalls from research:
  - Pitfall 2: test_wrong_args_flag_has_low_confidence (test 7) WILL fail — update it
  - Pitfall 3: fixture has no error-pattern examples — cover ARGS-01 with synthetic unit tests only
  - Pitfall 1: _check_wrong_args must never call _get_tool_embeddings() or embed any tool field
  - Anti-pattern: log_score() MUST be called BEFORE threshold comparison (invariant from base.py)

bow_score and hybrid_score from 07-02:
  from xeter.services.worker.base import bow_score, hybrid_score
  cosine = self.compare(prompt_vec, args_vec)
  bow = bow_score(span.prompt, flattened)
  score = hybrid_score(cosine, bow)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite _check_wrong_args in tool_call_analyzer.py</name>
  <files>xeter/services/worker/tool_call_analyzer.py</files>
  <action>
Make the following changes to xeter/services/worker/tool_call_analyzer.py. Read the file first.

CHANGE 1 - Add imports at top: Add "import re" and update the base import to include bow_score and hybrid_score:
  from xeter.services.worker.base import BaseAnalyzer, Flag, SpanData, bow_score, hybrid_score

CHANGE 2 - Add module-level constants after imports, before the ToolCallAnalyzer class:

    _WRONG_ARGS_ERROR_PATTERNS: list[re.Pattern] = [
        re.compile(r'invalid argument', re.IGNORECASE),
        re.compile(r'invalid param', re.IGNORECASE),
        re.compile(r'missing required', re.IGNORECASE),
        re.compile(r'missing param', re.IGNORECASE),
        re.compile(r'required field', re.IGNORECASE),
        re.compile(r'validation error', re.IGNORECASE),
        re.compile(r'type error', re.IGNORECASE),
        re.compile(r'value error', re.IGNORECASE),
        re.compile(r'parse error', re.IGNORECASE),
        re.compile(r'HTTP [4][0-9][0-9]', re.IGNORECASE),
        re.compile(r'status[ _]?code[: ]*4[0-9][0-9]', re.IGNORECASE),
        re.compile(r'400 bad request', re.IGNORECASE),
        re.compile(r'422 unprocessable', re.IGNORECASE),
    ]

    _NUMERIC_TOKEN_RE = re.compile(r'^[\d\.\-\+\*\/\(\)\^\%\s]+$')


    def _flatten_arg_values(args_str: str) -> str:
        """Extract leaf values from tool_arguments JSON and join as plain text.

        Parses the JSON string and joins only the values (not keys) so that
        embedding is not polluted by field names. Falls back to raw string
        if parsing fails.
        """
        import json as _json
        try:
            parsed = _json.loads(args_str)
            if not isinstance(parsed, dict):
                return args_str
            return " ".join(str(v) for v in parsed.values() if v is not None)
        except (ValueError, TypeError):
            return args_str


    def _should_skip_embedding(flattened: str) -> bool:
        """Return True if flattened values are empty or entirely numeric/operator tokens.

        Skips embedding when there is no meaningful text signal (ARGS-04).
        The regex requires the ENTIRE string to be numeric/operator — any letter
        character causes it to fail and embedding proceeds.
        """
        text = flattened.strip()
        if not text:
            return True
        return bool(_NUMERIC_TOKEN_RE.match(text))

CHANGE 3 - Replace the _check_wrong_args method (lines 180-207) with:

    def _check_wrong_args(self, span: SpanData) -> list[Flag]:
        """Detect when tool arguments are semantically unrelated to the prompt.

        Two-path detection (ARGS-01 takes priority):

        Path 1 — output-error priority (ARGS-01):
          If tool_output contains an error pattern (regex), flag immediately.
          Score=1.0. No embedding call.

        Path 2 — semantic mismatch via hybrid scoring (ARGS-02/03/04):
          Flatten argument values (not raw JSON), skip if empty or all-numeric,
          then compute 50/50 cosine+BOW hybrid score and flag if below threshold.

        low_confidence is NOT included in flag detail (ARGS-05).
        """
        if span.tool_arguments is None or span.prompt is None:
            return []

        # ARGS-01: output-error priority path (no embedding)
        if span.tool_output and any(
            p.search(span.tool_output) for p in _WRONG_ARGS_ERROR_PATTERNS
        ):
            self.log_score("wrong_args_output_error", 1.0)
            return [Flag(
                flag_type="wrong_tool_args",
                score=1.0,
                detail={"metric": "output_error_pattern", "source": "tool_output"},
            )]

        # ARGS-02/04: flatten argument values; skip if empty or all-numeric
        flattened = _flatten_arg_values(span.tool_arguments)
        if _should_skip_embedding(flattened):
            return []

        # ARGS-02/03: embed flattened values and compute hybrid score
        prompt_vec = self.embed(span.prompt)
        args_vec = self.embed(flattened)
        cosine = self.compare(prompt_vec, args_vec)
        bow = bow_score(span.prompt, flattened)
        score = hybrid_score(cosine, bow)

        self.log_score("prompt_vs_args_hybrid", score)  # log BEFORE threshold

        if score < self._thresholds["wrong_tool_args"]:
            return [Flag(
                flag_type="wrong_tool_args",
                score=score,
                detail={"metric": "prompt_vs_args_hybrid", "score": score},
                # ARGS-05: no low_confidence key
            )]
        return []

CRITICAL: Do NOT call _get_tool_embeddings() or embed any tool field inside this method. The wrong_args check is only about prompt vs argument values.
  </action>
  <verify>
    <automated>cd /c/Users/raphg/Desktop/Xeter/Xeter && python -c "from xeter.services.worker.tool_call_analyzer import ToolCallAnalyzer, _WRONG_ARGS_ERROR_PATTERNS, _flatten_arg_values, _should_skip_embedding; print('imports OK'); f=_flatten_arg_values('{\"to\": \"bob@x.com\", \"subject\": \"hello\"}'); assert 'to' not in f, f'keys must not appear in flattened: {f}'; assert 'bob@x.com' in f; print('flatten OK'); assert _should_skip_embedding('10000 * (1 + 0.05)'), 'numeric guard failed'; assert not _should_skip_embedding('sales@x.com hello'); print('guard OK'); print('PASS')"</automated>
  </verify>
  <done>_WRONG_ARGS_ERROR_PATTERNS, _flatten_arg_values, _should_skip_embedding all importable; flatten extracts values not keys; numeric guard works; _check_wrong_args uses hybrid_score</done>
</task>

<task type="auto">
  <name>Task 2: Update test_7 and add new _check_wrong_args tests</name>
  <files>xeter/tests/worker/test_tool_call_analyzer.py</files>
  <action>
Make two changes to xeter/tests/worker/test_tool_call_analyzer.py.

CHANGE 1 - Update test_wrong_args_flag_has_low_confidence (test 7). The current assertion checks that low_confidence is True. After ARGS-05, it must be absent. Change the test name and assertion:

  Old test name: test_wrong_args_flag_has_low_confidence
  New test name: test_wrong_args_flag_has_no_low_confidence

  Old assertion:
    assert wrong_args_flags[0].detail.get("low_confidence") is True
  New assertion:
    assert "low_confidence" not in wrong_args_flags[0].detail, (
        "ARGS-05: low_confidence must be absent from wrong_tool_args flag detail"
    )

  Also update the encode_side_effect in this test: the new _check_wrong_args embeds
  flattened values (not raw JSON). The flattened value of
  '{"query": "some unrelated text"}' is "some unrelated text". The mock needs to
  ensure the semantic path triggers a flag. Since the test uses alternating odd/even
  vectors (unit vs orthogonal), and the new code calls embed(prompt) then embed(flattened),
  the pattern still works — first call is prompt (unit), second is flattened args (orthogonal).
  Cosine similarity will be low (~0), BOW overlap between the prompt and args will be
  near 0 (different words), hybrid score will be low. The flag will fire.
  Keep the existing side_effect logic unchanged.

CHANGE 2 - Add new test functions at the bottom of the file to cover ARGS-01, ARGS-04:

    # ---------------------------------------------------------------------------
    # Tests for rewritten _check_wrong_args (ARGS-01, ARGS-04, ARGS-05)
    # ---------------------------------------------------------------------------

    def test_wrong_args_error_pattern_fires_without_embedding():
        """ARGS-01: tool_output with error pattern triggers flag; encode is never called."""
        embedder = make_mock_embedder()
        analyzer = make_analyzer(embedder)
        span = make_clean_span(
            tool_arguments='{"query": "python docs"}',
            prompt="Search for Python typing documentation",
            tool_output="invalid argument: expected string, got integer",
        )
        flags = analyzer._check_wrong_args(span)
        assert any(f.flag_type == "wrong_tool_args" for f in flags), (
            "Expected wrong_tool_args flag from error pattern"
        )
        wrong_args = [f for f in flags if f.flag_type == "wrong_tool_args"][0]
        assert wrong_args.score == 1.0
        assert wrong_args.detail.get("metric") == "output_error_pattern"
        # ARGS-01 must short-circuit before any embed() call
        assert embedder.encode.call_count == 0, (
            f"ARGS-01 must not call embed(); got {embedder.encode.call_count} calls"
        )

    def test_wrong_args_skips_all_numeric_flattened_values():
        """ARGS-04: all-numeric flattened values must not be embedded or flagged."""
        embedder = make_mock_embedder(_orthogonal_vec())  # low sim if embedded
        analyzer = make_analyzer(embedder)
        span = make_clean_span(
            tool_arguments='{"amount": "10000 * (1 + 0.05) ** 3"}',
            prompt="Calculate compound interest",
            tool_output="Success",
        )
        flags = analyzer._check_wrong_args(span)
        wrong_args_flags = [f for f in flags if f.flag_type == "wrong_tool_args"]
        assert len(wrong_args_flags) == 0, (
            "ARGS-04: all-numeric flattened values must not produce a flag"
        )

    def test_wrong_args_no_low_confidence_in_detail():
        """ARGS-05: flag detail must never contain low_confidence key."""
        embedder = make_mock_embedder()
        call_idx = [0]
        def encode_side_effect(text):
            call_idx[0] += 1
            return _unit_vec() if call_idx[0] % 2 == 1 else _orthogonal_vec()
        embedder.encode.side_effect = encode_side_effect
        analyzer = make_analyzer(embedder, thresholds={**DEFAULT_THRESHOLDS, "wrong_tool_args": 0.4})
        span = make_clean_span(
            tool_arguments='{"query": "some unrelated text"}',
            prompt="Calculate the square root of 144",
            tool_output="Success",
        )
        flags = analyzer._check_wrong_args(span)
        wrong_args_flags = [f for f in flags if f.flag_type == "wrong_tool_args"]
        if wrong_args_flags:
            assert "low_confidence" not in wrong_args_flags[0].detail, (
                "ARGS-05: low_confidence must be absent"
            )
  </action>
  <verify>
    <automated>cd /c/Users/raphg/Desktop/Xeter/Xeter && python -m pytest xeter/tests/worker/test_tool_call_analyzer.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <done>All tests pass; test_wrong_args_flag_has_no_low_confidence passes; test_wrong_args_error_pattern_fires_without_embedding passes; test_wrong_args_skips_all_numeric_flattened_values passes; total test count increased by 3</done>
</task>

</tasks>

<verification>
Run: python -m pytest xeter/tests/worker/test_tool_call_analyzer.py -v
Expected: All tests pass (26 total after additions).
Spot-check: python -c "from xeter.services.worker.tool_call_analyzer import _WRONG_ARGS_ERROR_PATTERNS; import re; assert any(p.search('invalid argument: bad type') for p in _WRONG_ARGS_ERROR_PATTERNS); print('regex OK')"
Spot-check: grep -n "low_confidence" xeter/services/worker/tool_call_analyzer.py — must only appear in comments (not in Flag detail dict).
</verification>

<success_criteria>
- _WRONG_ARGS_ERROR_PATTERNS: module-level compiled regex list, 13 patterns
- _flatten_arg_values(): extracts dict values not keys, falls back to raw string on parse error
- _should_skip_embedding(): returns True for empty or all-numeric/operator strings
- _check_wrong_args() error path: fires without embed(), score=1.0, no low_confidence key
- _check_wrong_args() semantic path: uses hybrid_score(cosine, bow_score(...))
- _check_wrong_args() logs score BEFORE threshold comparison
- test_7 renamed and updated to assert low_confidence is absent
- 3 new tests cover ARGS-01, ARGS-04, ARGS-05
- All tests pass (26 total)
</success_criteria>

<output>
After completion, create .planning/phases/07-wrong-args-rewrite/07-03-SUMMARY.md
</output>
