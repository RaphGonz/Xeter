---
phase: 07-wrong-args-rewrite
plan: "05"
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-03"
files_modified:
  - fixtures/calibrated_thresholds.json
  - deploy/docker-compose.yml
autonomous: false
requirements:
  - ARGS-06

must_haves:
  truths:
    - "python xeter/scripts/calibrate.py --flag-type wrong_tool_args completes without crashing"
    - "calibrated_thresholds.json is updated with a wrong_tool_args threshold from hill climbing"
    - "Reported precision for wrong_tool_args is >= 0.80 (P/R benchmark passed)"
    - "All 10 wrong_tool_args fixture spans exercised the semantic mismatch path (not the error path)"
  artifacts:
    - path: "fixtures/calibrated_thresholds.json"
      provides: "Updated thresholds with calibrated wrong_tool_args value"
      contains: "wrong_tool_args"
  key_links:
    - from: "calibrate.py hill_climb()"
      to: "fixtures/calibrated_thresholds.json"
      via: "json.dump at end of main()"
      pattern: "THRESHOLDS_PATH"
---

<objective>
Run the per-method calibration for wrong_tool_args using the updated calibrate.py (07-01) and rewritten _check_wrong_args (07-03). The hill-climb must converge to a threshold with >= 80% precision on the 10 labelled wrong_tool_args spans in fixtures/labelled_spans.jsonl.

This plan requires a live embedder container. The user must confirm precision >= 80% via checkpoint.

Purpose: ARGS-06 — calibration passes P/R benchmark before Phase 8 begins.

Output: Updated calibrated_thresholds.json with wrong_tool_args threshold; docker-compose.yml patched; benchmark confirmed.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-wrong-args-rewrite/07-RESEARCH.md

Fixture context (from research):
  All 10 wrong_tool_args spans have tool_output = "Email delivered" (no error pattern).
  The error-path (ARGS-01) will NOT fire on any fixture span — this is by design.
  All 10 are semantic mismatch cases exercising the hybrid scoring path (ARGS-02/03).

Calibration command (requires embedder at localhost:8002):
  python xeter/scripts/calibrate.py --flag-type wrong_tool_args

Pitfall from research: if P is suspiciously perfect (1.0/1.0), check whether
error-pattern spans leaked into the fixture. They should not have.

DEFAULT_THRESHOLDS has wrong_tool_args: 0.4 as the hill-climb starting point.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start embedder and run wrong_tool_args calibration</name>
  <files>fixtures/calibrated_thresholds.json</files>
  <action>
Run the following steps. Requires Docker and the embedder image.

Step 1 - Ensure embedder is running:
  docker compose -f deploy/docker-compose.yml up -d embedder

Step 2 - Wait for embedder health (retry up to 10 times with 2-second intervals):
  for i in $(seq 1 10); do
    response=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8002/health)
    if [ "$response" = "200" ]; then break; fi
    sleep 2
  done

Step 3 - Run per-method calibration:
  cd /c/Users/raphg/Desktop/Xeter/Xeter
  python xeter/scripts/calibrate.py --flag-type wrong_tool_args

Step 4 - Verify calibrated_thresholds.json was updated:
  python -c "import json; d=json.load(open('fixtures/calibrated_thresholds.json')); print('wrong_tool_args threshold:', d['thresholds'].get('wrong_tool_args', 'MISSING'))"

Note: If the embedder is already running from previous work, skip steps 1-2.
The calibration script prints a summary table. Record the precision/recall line for wrong_tool_args.
  </action>
  <verify>
    <automated>cd /c/Users/raphg/Desktop/Xeter/Xeter && python -c "import json; d=json.load(open('fixtures/calibrated_thresholds.json')); assert 'wrong_tool_args' in d['thresholds']; print('wrong_tool_args threshold:', d['thresholds']['wrong_tool_args'])"</automated>
  </verify>
  <done>Calibration script exited 0; calibrated_thresholds.json contains wrong_tool_args key; docker-compose.yml patched</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm wrong_tool_args calibration passed P/R benchmark</name>
  <action>Read calibration output from Task 1 and check precision reported for wrong_tool_args.</action>
  <what-built>
Ran: python xeter/scripts/calibrate.py --flag-type wrong_tool_args

The calibration script ran hill-climbing on 10 wrong_tool_args labelled spans. All 10 used the semantic mismatch path (ARGS-02/03) since their tool_output is "Email delivered" (no error pattern — correct by design per research Pitfall 3).

Results written to fixtures/calibrated_thresholds.json.
deploy/docker-compose.yml patched with calibrated WORKER_THRESHOLD_WRONG_ARGS.
  </what-built>
  <how-to-verify>
1. Check the terminal output from the calibration run — find the wrong_tool_args row in the summary table
2. Confirm precision >= 0.80 — look for [OK] status
3. If precision less than 0.80, note the actual values (e.g. "precision=0.71, recall=0.90")
4. Check fixtures/calibrated_thresholds.json to confirm wrong_tool_args key is written
  </how-to-verify>
  <resume-signal>Type "passed" if precision >= 0.80, or report the actual precision/recall if the benchmark was not met</resume-signal>
  <verify>User has confirmed precision >= 0.80 or reported failure values</verify>
  <done>P/R benchmark >= 80% confirmed; Phase 8 can begin</done>
</task>

</tasks>

<verification>
Automated: python -c "import json; d=json.load(open('fixtures/calibrated_thresholds.json')); assert 'wrong_tool_args' in d['thresholds']; print('threshold:', d['thresholds']['wrong_tool_args'])"
Manual: calibration summary table shows wrong_tool_args with [OK] status (precision >= 0.80)
</verification>

<success_criteria>
- calibrate.py --flag-type wrong_tool_args runs without crashing
- Hill-climb converges; calibrated_thresholds.json updated with wrong_tool_args threshold
- Reported precision >= 0.80 for wrong_tool_args
- User confirms benchmark passed via checkpoint
- Phase 7 complete; Phase 8 can begin
</success_criteria>

<output>
After completion, create .planning/phases/07-wrong-args-rewrite/07-05-SUMMARY.md
Include: calibrated wrong_tool_args threshold value, precision and recall, number of hill-climb steps.
</output>
