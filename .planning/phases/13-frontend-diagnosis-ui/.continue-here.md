---
phase: 13-frontend-diagnosis-ui
task: checkpoint-13-02
status: debugging
last_updated: 2026-04-24T21:10:17.272Z
---

<current_state>
Phase 13 plan 01 is complete (DiagnosisCard + auto-load shipped). We are mid-checkpoint 13-02 (visual E2E verification). The app is running but seeded spans do not appear in the dashboard. Debugging why.
</current_state>

<completed_work>

- Phase 13 plan 01: COMPLETE — api.ts updated (DiagnosisResponse type, getDiagnosis GET, diagnose POST without flags), FlagSection rewritten with DiagnosisCard (colored verdict/severity badges, MetaRow affected field + fix, timestamp, skeleton loading, always-enabled button, key={spanId} remount, silent 404 on auto-load)
- next.config.ts fix: PRESENTER_URL default changed from http://presenter:8000 to http://localhost:8000 so npm run dev works without .env.local
- seed_spans.py rewrite: replaced stale examples with 4 real fixture spans (clean-001, fwc-001, fwta-001, fnt-001) with S3 payload uploads (prompt, response, raw_response, available_tools via MINIO_* env vars), bucket auto-creation, flags and scores in PostgreSQL
- Todo captured: audit env var naming inconsistency (S3_* vs MINIO_*) before going live
- README updated: added frontend dev server instructions (cd services/view && npm run dev)
</completed_work>

<remaining_work>

- Debug: seeded spans not showing in dashboard despite successful seed log
  - ClickHouse not reachable from host on port 8123 (curl returned unreachable) — may need to check if ClickHouse port is exposed in docker-compose
  - Likely candidates: tenant_id mismatch between ClickHouse and JWT, ClickHouse port not exposed to host, or time_begin format issue
- Once spans visible: run 6 visual verification tests for 13-02 checkpoint
- Once checkpoint approved: phase 13 complete, v1.2 milestone done
</remaining_work>

<decisions_made>

- seed_spans.py uses MINIO_* env vars (not S3_*) — matches what's in .env
- S3Client instantiated directly in seed script (not via get_s3_client()) to use MINIO_* vars
- Bucket is auto-created if missing before uploads
- span_ids kept as fixture IDs (clean-001, fwc-001 etc.) — not UUIDs
</decisions_made>

<blockers>

- Seeded spans not visible in dashboard: ClickHouse not reachable on localhost:8123 from outside Docker. Need to verify port 8123 is exposed in deploy/docker-compose.yml and that the tenant_id stored in ClickHouse matches the logged-in user's tenant_id from JWT.
- Debug approach: check docker-compose ports for clickhouse, then query ClickHouse directly once reachable to confirm data is there with correct tenant_id.
</blockers>

<context>
The full pipeline works end-to-end for real spans (SDK → Analyser → Worker → Presenter). The seed script is a shortcut that bypasses the worker and writes directly to ClickHouse + PostgreSQL. The issue is likely one of:
1. ClickHouse port 8123 not exposed on host (curl failed) — check deploy/docker-compose.yml clickhouse ports
2. tenant_id format mismatch — seed stores UUID string, presenter filters by same UUID from JWT, should match
3. span_id format (clean-001 is not a UUID) — probably fine since it's a String field

First action: check docker-compose for clickhouse port exposure, then curl ClickHouse once confirmed, verify data with SELECT, check tenant_id matches what's in the JWT.
</context>

<next_action>
Check deploy/docker-compose.yml clickhouse service ports section. If port 8123 is exposed, query: curl "http://localhost:8123/?query=SELECT+span_id,tenant_id+FROM+spans+LIMIT+10+FORMAT+JSON" and compare tenant_id to what the presenter uses (grep JWT/tenant from presenter auth).
</next_action>
