---
phase: 13-frontend-diagnosis-ui
plan: "02"
type: execute
wave: 2
depends_on: ["13-01"]
files_modified: []
autonomous: false
requirements:
  - FRONTEND-UI-01

must_haves:
  truths:
    - "Opening a flagged span panel with a prior diagnosis shows the DiagnosisCard without a button click"
    - "Clicking Diagnose on a flagged span triggers a real LLM call and shows a structured result card"
    - "The Diagnose button remains clickable during and after loading"
    - "Opening a clean/pending span shows the disabled Diagnose button with explanatory text"
  artifacts:
    - path: "services/view/src/components/SpanDetailPanel.tsx"
      provides: "Working DiagnosisCard UI with correct color mapping"
      contains: "DiagnosisCard"
  key_links:
    - from: "FlagSection"
      to: "DiagnosisCard"
      via: "diagResult state rendered conditionally"
      pattern: "DiagnosisCard.*diagResult"
---

<objective>
Visual verification of the completed diagnosis UI. Confirm that auto-load, POST trigger, loading states, error handling, and the DiagnosisCard layout all work correctly in the running application.

Purpose: Phase 13 ships only when the UI works end-to-end, not just TypeScript-compiles.
Output: Human-verified working diagnosis flow.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-frontend-diagnosis-ui/13-CONTEXT.md
@.planning/phases/13-frontend-diagnosis-ui/13-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of diagnosis UI</name>
  <files>services/view/src/components/SpanDetailPanel.tsx</files>
  <action>Pause for human verification of the diagnosis UI built in plan 13-01. No code changes — this task verifies the implementation visually and interactively.</action>
  <what-built>
    Structured diagnosis UI in SpanDetailPanel:
    - api.ts updated with DiagnosisResponse type, getDiagnosis GET, diagnose POST (no flags)
    - FlagSection auto-loads prior diagnosis via GET on mount
    - Diagnose button always enabled (never disabled)
    - Skeleton + "Analyzing…" text during loading
    - DiagnosisCard with colored verdict/severity badges + MetaRow fields + timestamp
    - Inline red error box on failure (button stays enabled for retry)
    - key={spanId} prevents stale state across panel opens
  </what-built>
  <how-to-verify>
Start the app if not already running:
```
cd C:/Users/raphg/Desktop/Xeter/Xeter
make up
```
Then open http://localhost:3000 and verify each of the following:

**Test 1 — Auto-load (span with prior diagnosis):**
1. Open any flagged span that has already been diagnosed
2. Panel opens → DiagnosisCard appears immediately (no button click needed)
3. Card shows two colored badges on one row (verdict + severity)
4. Below badges: "Affected field" and "Fix" MetaRow rows
5. Timestamp row: "Diagnosed X minutes ago"

**Test 2 — POST (new or re-run diagnosis):**
1. Open any flagged span
2. Click "Diagnose"
3. Skeleton + "Analyzing… (this may take a few seconds)" appears
4. "Diagnose" button remains clickable during loading (not grayed out)
5. After 5–15s: DiagnosisCard replaces skeleton with real result

**Test 3 — Error state:**
1. Stop the Diagnosticer service (or set DIAGNOSTICER_URL to an invalid value temporarily)
2. Open a flagged span, click "Diagnose"
3. Inline red error box appears below the button
4. Button remains clickable — clicking again fires another attempt

**Test 4 — No prior diagnosis (first open):**
1. Open a flagged span that has never been diagnosed
2. Panel opens — no DiagnosisCard, no error, no skeleton (after GET 404 is silently swallowed)
3. "Diagnose" button is present and clickable

**Test 5 — Non-flagged span:**
1. Open a clean or pending span
2. Disabled "Diagnose" button appears with "Diagnostics are only available for flagged spans." text

**Test 6 — Layout inside FlagSection:**
Confirm the layout matches:
```
┌─ Flags ─────────────────────────────────────┐
│  [wrong_tool_called] score: 0.8712          │
│  { detail... }                              │
│                                             │
│  [Diagnose]                                 │
│  ┌─ Diagnosis ──────────────────────────┐   │
│  │ [model] [critical]                   │   │
│  │ Affected: tool_name                  │   │
│  │ Fix: Switch to a model...            │   │
│  └──────────────────────────────────────┘   │
└─────────────────────────────────────────────┘
```
  </how-to-verify>
  <verify>Human approves all 6 tests via resume-signal</verify>
  <done>All 6 tests pass: auto-load, POST flow, error state, silent 404, non-flagged disabled button, correct layout</done>
  <resume-signal>Type "approved" if all 6 tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 6 manual verification tests pass. The diagnosis flow works end-to-end in the running application.
</verification>

<success_criteria>
- DiagnosisCard displays correctly for spans with prior diagnosis (auto-load)
- POST diagnosis flow works: skeleton → result card
- Button is never disabled during or after loading
- 404 on panel open is silent (no error shown)
- Error state shows inline red box, button stays enabled
- Non-flagged span shows disabled button with explanatory text
- Layout matches the mockup from CONTEXT.md
</success_criteria>

<output>
After completion, create `.planning/phases/13-frontend-diagnosis-ui/13-02-SUMMARY.md`
</output>
