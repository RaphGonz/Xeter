---
phase: 22-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - xeter/services/worker/score_writer.py
  - xeter/migrations/versions/006_span_scores_nullable_span_id.py
autonomous: true
requirements:
  - INFRA-02

must_haves:
  truths:
    - "write_scores accepts None as span_id without a type error"
    - "The span_scores.span_id column allows NULL values after migration upgrade"
    - "Existing span-level score writes are unaffected (write_scores(span_id, ...) still works)"
  artifacts:
    - path: "xeter/migrations/versions/006_span_scores_nullable_span_id.py"
      provides: "ALTER TABLE span_scores ALTER COLUMN span_id DROP NOT NULL"
      contains: "revision: str = \"006\""
    - path: "xeter/services/worker/score_writer.py"
      provides: "Updated write_scores signature accepting str | None"
      contains: "span_id: str | None"
  key_links:
    - from: "xeter/services/worker/main.py (_flush_stale_traces)"
      to: "xeter/services/worker/score_writer.py (write_scores)"
      via: "write_scores(None, tenant_id_for_trace, trace_scores)"
      pattern: "write_scores\\(None,"
---

<objective>
Enable trace-level score writes by: (1) making span_scores.span_id nullable via a new Alembic migration, and (2) updating the write_scores signature from `span_id: str` to `span_id: str | None`.

Purpose: Plan 02 will call `write_scores(None, ...)` for trace-level scores. Without this migration the DB will reject the insert. Without the type update the call is a signature violation.

Output: Migration file 006, updated score_writer.py signature and docstring.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-bug-fixes/22-CONTEXT.md
@.planning/phases/22-bug-fixes/22-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add migration 006 — make span_scores.span_id nullable</name>
  <files>xeter/migrations/versions/006_span_scores_nullable_span_id.py</files>
  <read_first>
    - xeter/migrations/versions/005_trace_flags_schema.py (exact structural analog — copy this file's layout: module docstring, revision/down_revision vars, upgrade/downgrade using op.execute with raw SQL)
    - xeter/migrations/versions/002_span_scores.py (confirms span_id was created nullable=False; line 46: sa.Column("span_id", sa.String(), nullable=False))
  </read_first>
  <action>
    Create xeter/migrations/versions/006_span_scores_nullable_span_id.py modelled exactly on 005_trace_flags_schema.py structure.

    Required values:
    - Module docstring title: "span_scores: make span_id nullable for trace-level scores."
    - Changes line: "INFRA-02: span_id column made nullable — trace-level scores produced by TraceAnalyzer have no single span; span-level scores continue to populate span_id as before."
    - revision: str = "006"
    - down_revision: Union[str, None] = "005"
    - branch_labels: Union[str, Sequence[str], None] = None
    - depends_on: Union[str, Sequence[str], None] = None
    - upgrade(): op.execute("ALTER TABLE span_scores ALTER COLUMN span_id DROP NOT NULL;")
    - downgrade(): op.execute("ALTER TABLE span_scores ALTER COLUMN span_id SET NOT NULL;") with a comment: "WARNING: any trace-level score rows (span_id IS NULL) will block this."

    Imports: from typing import Sequence, Union and from alembic import op (same as 005; do NOT import sqlalchemy or op.create_table — this is a single ALTER, not a table creation).
  </action>
  <verify>
    <automated>python -c "import ast, sys; ast.parse(open('xeter/migrations/versions/006_span_scores_nullable_span_id.py').read()); print('syntax ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at xeter/migrations/versions/006_span_scores_nullable_span_id.py
    - revision = "006", down_revision = "005"
    - upgrade() body contains exactly: op.execute("ALTER TABLE span_scores ALTER COLUMN span_id DROP NOT NULL;")
    - downgrade() body contains exactly: op.execute("ALTER TABLE span_scores ALTER COLUMN span_id SET NOT NULL;")
    - No sqlalchemy imports — only typing, alembic.op
    - Python syntax is valid (python -c "import ast; ast.parse(...)" succeeds)
  </acceptance_criteria>
  <done>Migration 006 file exists, parses without error, has correct revision chain (005→006), and contains the expected ALTER TABLE statements.</done>
</task>

<task type="auto">
  <name>Task 2: Update write_scores signature to accept Optional span_id</name>
  <files>xeter/services/worker/score_writer.py</files>
  <read_first>
    - xeter/services/worker/score_writer.py (current file — read in full before editing; signature is at line 46–50, docstring Args section at lines 58–66, error log at line 86)
    - xeter/services/worker/flag_writer.py (exact analog — write_flags uses span_id: str | None at lines 51–56; docstring "or None for trace-level flags" at lines 63–66 is the wording to mirror)
  </read_first>
  <action>
    Make two targeted edits to xeter/services/worker/score_writer.py:

    1. Signature: change `span_id: str,` to `span_id: str | None,` on the first parameter line of write_scores (currently line 47).

    2. Docstring Args section: replace the existing `span_id:` line with:
       "span_id: The span being scored, or None for trace-level scores produced by TraceAnalyzer (psycopg2 maps None to SQL NULL automatically)."

    Do NOT change the rows tuple construction at line 70 — psycopg2 maps Python None to SQL NULL automatically, so (span_id, ...) tuple works unchanged. Do NOT change the error log line that references span_id — it formats None acceptably as the string "None".

    Do NOT add an Optional import or change any other function. The str | None union syntax (PEP 604) is already used throughout the codebase (see flag_writer.py line 52).
  </action>
  <verify>
    <automated>python -c "from xeter.services.worker.score_writer import write_scores; import inspect; sig = inspect.signature(write_scores); assert str(sig.parameters['span_id'].annotation) in ('str | None', 'typing.Optional[str]'), f'Got: {sig.parameters[\"span_id\"].annotation}'; print('signature ok')"</automated>
  </verify>
  <acceptance_criteria>
    - write_scores first parameter annotation is `str | None` (not `str`)
    - Docstring Args `span_id:` line contains the phrase "or None for trace-level scores"
    - No other lines in the file are changed
    - `python -c "from xeter.services.worker.score_writer import write_scores"` exits 0
    - `grep -n "span_id: str | None" xeter/services/worker/score_writer.py` returns a match on the parameter line
  </acceptance_criteria>
  <done>write_scores accepts span_id: str | None; docstring matches flag_writer.py pattern; existing callers with a string span_id are unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration script → PostgreSQL | DDL executed by alembic upgrade; runs with database credentials from DATABASE_URL |
| worker → PostgreSQL (score_writer) | psycopg2 INSERT with tenant RLS; span_id column now accepts NULL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | span_scores.span_id nullable | accept | NULL span_id is intentional for trace-level rows; existing ix_span_scores_span index remains for non-null rows; no data integrity loss |
| T-22-02 | Elevation of Privilege | downgrade() SET NOT NULL | accept | downgrade explicitly warns that NULL rows block it; operator must delete trace-level rows before downgrading; documented in migration comment |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | no new package installs in this plan |
</threat_model>

<verification>
1. `python -c "import ast; ast.parse(open('xeter/migrations/versions/006_span_scores_nullable_span_id.py').read()); print('ok')"` exits 0
2. `grep -n "revision.*006" xeter/migrations/versions/006_span_scores_nullable_span_id.py` returns a match
3. `grep -n "down_revision.*005" xeter/migrations/versions/006_span_scores_nullable_span_id.py` returns a match
4. `grep -n "span_id: str | None" xeter/services/worker/score_writer.py` returns a match
5. `python -c "from xeter.services.worker.score_writer import write_scores; print('import ok')"` exits 0
</verification>

<success_criteria>
- Migration 006 exists with revision="006", down_revision="005", correct upgrade/downgrade SQL
- write_scores signature is `span_id: str | None` with updated docstring
- No regressions: existing score_writer tests pass (`pytest xeter/tests/ -x -q` exits 0)
</success_criteria>

<output>
Create `.planning/phases/22-bug-fixes/22-01-SUMMARY.md` when done
</output>
