---
phase: 23-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - xeter/pyproject.toml
  - services/worker/Dockerfile
  - xeter/services/worker/base.py
  - xeter/services/worker/span_fetcher.py
  - xeter/scripts/calibrate.py
autonomous: true
requirements:
  - INFRA-04
  - INFRA-05
  - INFRA-06

must_haves:
  truths:
    - "Worker environment can import jsonschema, tiktoken, and rapidfuzz without ModuleNotFoundError"
    - "SpanData has expected_output_schema: Optional[str] = None field after available_tools"
    - "SpanData has parent_span_id: Optional[str] = None field after expected_output_schema"
    - "span_fetcher._FETCH_COLUMNS includes expected_output_schema and parent_span_id"
    - "_FETCH_QUERY SELECT clause includes expected_output_schema and parent_span_id"
    - "SpanData constructor in fetch_span() passes expected_output_schema and parent_span_id from the row"
    - "build_span_data() in calibrate.py passes expected_output_schema and parent_span_id kwargs to SpanData"
  artifacts:
    - path: "xeter/pyproject.toml"
      provides: "dependency declarations for worker"
      contains: "jsonschema==4.26.0"
    - path: "services/worker/Dockerfile"
      provides: "pip install line for worker image"
      contains: "jsonschema tiktoken rapidfuzz"
    - path: "xeter/services/worker/base.py"
      provides: "SpanData dataclass"
      contains: "expected_output_schema"
    - path: "xeter/services/worker/span_fetcher.py"
      provides: "span fetching from ClickHouse"
      contains: "expected_output_schema"
    - path: "xeter/scripts/calibrate.py"
      provides: "calibration harness SpanData builder"
      contains: "expected_output_schema=row.get"
  key_links:
    - from: "xeter/services/worker/base.py"
      to: "xeter/services/worker/span_fetcher.py"
      via: "SpanData constructor call in fetch_span()"
      pattern: "expected_output_schema=row"
    - from: "xeter/services/worker/base.py"
      to: "xeter/scripts/calibrate.py"
      via: "build_span_data() SpanData construction"
      pattern: "expected_output_schema=row.get"
---

<objective>
Add three new worker dependencies (jsonschema, tiktoken, rapidfuzz) to both
pyproject.toml and the worker Dockerfile. Extend SpanData with two new Optional[str]
fields (expected_output_schema, parent_span_id). Update span_fetcher to fetch both
columns from ClickHouse. Update calibrate.py build_span_data() to pass both new
fields through to SpanData.

Purpose: All v1.5 checks in phases 24-27 depend on SpanData carrying
expected_output_schema and parent_span_id. The three new deps are prerequisites
for the structural and semantic checks. These changes are foundational — nothing
else in phase 23 can proceed without them.

Output: Updated pyproject.toml, Dockerfile, base.py, span_fetcher.py, calibrate.py.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-infrastructure/23-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add jsonschema, tiktoken, rapidfuzz to pyproject.toml and Dockerfile</name>
  <files>xeter/pyproject.toml, services/worker/Dockerfile</files>
  <read_first>
    - xeter/pyproject.toml — read the full [project].dependencies list to find the insertion point; spacy is the reference entry to replicate the pattern
    - services/worker/Dockerfile — read the full file to see the exact pip install line (line 10) and the spacy pattern
  </read_first>
  <action>
    In xeter/pyproject.toml, add three entries to the dependencies list under [project] (alongside the existing spacy>=3.7 entry):
      "jsonschema==4.26.0",
      "tiktoken==0.13.0",
      "rapidfuzz==3.14.5",

    In services/worker/Dockerfile, extend line 10's pip install command. The current line is:
      RUN pip install --no-cache-dir psycopg2-binary boto3 redis httpx spacy && \
          python -m spacy download en_core_web_md

    Append jsonschema tiktoken rapidfuzz to the pip install invocation (before the &&), so all five non-spacy packages are installed in one RUN command. The spacy model download stays unchanged.

    Decision reference: D-09 (dual-location pattern matching spacy).
  </action>
  <verify>
    <automated>python -c "import tomllib; d=tomllib.load(open('xeter/pyproject.toml','rb')); deps=d['project']['dependencies']; assert any('jsonschema==4.26.0' in x for x in deps), 'jsonschema missing'; assert any('tiktoken==0.13.0' in x for x in deps), 'tiktoken missing'; assert any('rapidfuzz==3.14.5' in x for x in deps), 'rapidfuzz missing'; print('pyproject OK')"</automated>
  </verify>
  <acceptance_criteria>
    - xeter/pyproject.toml [project].dependencies contains "jsonschema==4.26.0"
    - xeter/pyproject.toml [project].dependencies contains "tiktoken==0.13.0"
    - xeter/pyproject.toml [project].dependencies contains "rapidfuzz==3.14.5"
    - services/worker/Dockerfile pip install line contains "jsonschema tiktoken rapidfuzz"
    - The spacy download line is unchanged
  </acceptance_criteria>
  <done>Both files updated; pyproject validation script exits 0; Dockerfile grep confirms all three packages present in pip install line.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend SpanData, span_fetcher, and calibrate.py build_span_data</name>
  <files>
    xeter/services/worker/base.py,
    xeter/services/worker/span_fetcher.py,
    xeter/scripts/calibrate.py,
    xeter/tests/test_span_data_fields.py
  </files>
  <read_first>
    - xeter/services/worker/base.py — read the full SpanData dataclass; the last field is available_tools: Optional[list[dict]]; new fields append after it
    - xeter/services/worker/span_fetcher.py — read _FETCH_COLUMNS list (lines 35-49), _FETCH_QUERY string (lines 51-56), and the SpanData(...) constructor call in fetch_span() (lines 164-178); both column representations and the constructor must be updated together
    - xeter/scripts/calibrate.py — read build_span_data() at line ~101; the current constructor call passes 14 kwargs; two more must be added
    - xeter/services/analyser/schemas.py — read SpanPayload for parent_span_id field pattern: Optional[str] = None; replicate exactly in SpanData
  </read_first>
  <behavior>
    - Test 1: SpanData() constructed with no args for new fields defaults expected_output_schema=None and parent_span_id=None
    - Test 2: SpanData constructed with expected_output_schema='{"type":"object"}' carries that string value on .expected_output_schema
    - Test 3: SpanData constructed with parent_span_id="abc-123" carries that string value on .parent_span_id
    - Test 4: build_span_data({"expected_output_schema": '{"type":"string"}', "parent_span_id": "p-1"}) returns SpanData with both fields set
    - Test 5: build_span_data({}) returns SpanData with expected_output_schema=None, parent_span_id=None (no KeyError)
    - Test 6: _FETCH_COLUMNS list contains "expected_output_schema" and "parent_span_id" (both strings present)
  </behavior>
  <action>
    In xeter/services/worker/base.py, add two fields at the END of the SpanData dataclass, after available_tools:
      expected_output_schema: Optional[str] = None   # inline JSON string, same pattern as tool_arguments
      parent_span_id: Optional[str] = None           # ClickHouse column already exists

    In xeter/services/worker/span_fetcher.py:
      1. Add "expected_output_schema" and "parent_span_id" to _FETCH_COLUMNS (append to the end of the list).
      2. Extend _FETCH_QUERY SELECT clause and FROM string to include both new column names. The query currently ends with "available_tools_ref"; append ", expected_output_schema, parent_span_id" before the FROM clause.
      3. In the SpanData(...) constructor call inside fetch_span(), add two kwargs:
           expected_output_schema=row.get("expected_output_schema") or None,
           parent_span_id=row.get("parent_span_id") or None,

    In xeter/scripts/calibrate.py, inside build_span_data(), add two kwargs to the SpanData(...) call after available_tools=row.get("available_tools"):
      expected_output_schema=row.get("expected_output_schema"),
      parent_span_id=row.get("parent_span_id"),

    Decision references: D-03 (Optional[str] storage pattern), D-10 (parent_span_id scope limited to SpanData + fetcher).

    Write xeter/tests/test_span_data_fields.py with tests covering all six behavior cases above. Import SpanData from xeter.services.worker.base and build_span_data from xeter.scripts.calibrate. Import _FETCH_COLUMNS from xeter.services.worker.span_fetcher.
  </action>
  <verify>
    <automated>python -m pytest xeter/tests/test_span_data_fields.py -v</automated>
  </verify>
  <acceptance_criteria>
    - xeter/services/worker/base.py SpanData dataclass contains "expected_output_schema: Optional[str] = None"
    - xeter/services/worker/base.py SpanData dataclass contains "parent_span_id: Optional[str] = None"
    - xeter/services/worker/span_fetcher.py _FETCH_COLUMNS list contains "expected_output_schema" and "parent_span_id"
    - xeter/services/worker/span_fetcher.py _FETCH_QUERY string contains "expected_output_schema" and "parent_span_id"
    - xeter/services/worker/span_fetcher.py SpanData constructor in fetch_span() passes expected_output_schema= and parent_span_id= kwargs
    - xeter/scripts/calibrate.py build_span_data() passes expected_output_schema=row.get("expected_output_schema") and parent_span_id=row.get("parent_span_id")
    - pytest xeter/tests/test_span_data_fields.py exits 0, all 6 tests pass
  </acceptance_criteria>
  <done>All six tests pass; SpanData, span_fetcher, and calibrate.py build_span_data are consistent with each other and with the new field types.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI packages | jsonschema, tiktoken, rapidfuzz installed from public registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | npm/pip installs (jsonschema, tiktoken, rapidfuzz) | mitigate | Verify exact pinned versions match PyPI before accepting; CONTEXT.md D-09 specifies exact versions |
| T-23-02 | Spoofing | SpanData.expected_output_schema from ClickHouse | accept | Data originates from SDK-controlled ingest path; no external injection vector at this layer |
</threat_model>

<verification>
- SpanData has exactly 15 fields total (13 original + expected_output_schema + parent_span_id)
- _FETCH_COLUMNS has 15 items (13 original + 2 new)
- _FETCH_QUERY includes both new column names in SELECT
- build_span_data() call in calibrate.py has 16 kwargs total (14 original + 2 new)
- pytest xeter/tests/test_span_data_fields.py -v exits 0
</verification>

<success_criteria>
1. Worker image pip install line contains jsonschema tiktoken rapidfuzz
2. pyproject.toml dependencies contains all three pinned packages
3. SpanData(expected_output_schema='{"type":"string"}', parent_span_id="x").expected_output_schema == '{"type":"string"}'
4. _FETCH_COLUMNS contains both "expected_output_schema" and "parent_span_id"
5. All 6 tests in test_span_data_fields.py pass
</success_criteria>

<output>
Create `.planning/phases/23-infrastructure/23-01-SUMMARY.md` when done
</output>
