---
phase: 23-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - xeter/services/analyser/schemas.py
  - xeter/services/analyser/batch.py
  - xeter/services/analyser/ingest.py
  - xeter/shared/db/clickhouse.py
  - xeter/services/analyser/main.py
  - sdk/xeter_sdk/decorator.py
  - xeter/tests/test_expected_output_schema_ingest.py
autonomous: true
requirements:
  - INFRA-05

must_haves:
  truths:
    - "SpanPayload accepts expected_output_schema: Optional[str] = None in POST /v1/spans body"
    - "SPAN_COLUMNS list contains expected_output_schema as item 9 (after tool_arguments)"
    - "ingest.py row construction includes span.expected_output_schema at the matching position"
    - "assert len(row) == len(SPAN_COLUMNS) still passes with 18 items"
    - "SPANS_TABLE_DDL in clickhouse.py contains expected_output_schema Nullable(String)"
    - "alter_spans_add_expected_output_schema() executes IF NOT EXISTS DDL — idempotent"
    - "analyser startup calls alter_spans_add_expected_output_schema immediately after create_spans_table"
    - "SDK trace() decorator accepts expected_output_schema: dict | None = None kwarg"
    - "SDK _dispatch() serializes expected_output_schema to JSON string and includes it in span dict"
  artifacts:
    - path: "xeter/services/analyser/schemas.py"
      provides: "SpanPayload Pydantic model"
      contains: "expected_output_schema"
    - path: "xeter/services/analyser/batch.py"
      provides: "SPAN_COLUMNS ordered list"
      contains: "expected_output_schema"
    - path: "xeter/services/analyser/ingest.py"
      provides: "row construction and assert guard"
      contains: "span.expected_output_schema"
    - path: "xeter/shared/db/clickhouse.py"
      provides: "DDL and idempotent alter function"
      contains: "alter_spans_add_expected_output_schema"
    - path: "xeter/services/analyser/main.py"
      provides: "startup lifespan sequence"
      contains: "alter_spans_add_expected_output_schema"
    - path: "sdk/xeter_sdk/decorator.py"
      provides: "trace() decorator with new kwarg"
      contains: "expected_output_schema"
  key_links:
    - from: "xeter/services/analyser/batch.py"
      to: "xeter/services/analyser/ingest.py"
      via: "SPAN_COLUMNS import + row length assert"
      pattern: "assert len\\(row\\) == len\\(SPAN_COLUMNS\\)"
    - from: "xeter/shared/db/clickhouse.py"
      to: "xeter/services/analyser/main.py"
      via: "alter_spans_add_expected_output_schema import + call"
      pattern: "alter_spans_add_expected_output_schema\\(ch_client\\)"
    - from: "sdk/xeter_sdk/decorator.py"
      to: "xeter/services/analyser/schemas.py"
      via: "span dict key expected_output_schema matches SpanPayload field"
      pattern: "expected_output_schema.*json.dumps"
---

<objective>
Wire the expected_output_schema field through the full ingest pipeline:
SpanPayload (API schema) -> SPAN_COLUMNS -> ingest row -> ClickHouse DDL (new column)
-> idempotent ALTER TABLE at startup -> SDK decorator kwarg.

After this plan, a span sent with expected_output_schema={"type":"object"} from the SDK
will arrive serialized as a JSON string in the ClickHouse spans table.

Purpose: Phases 24+ need expected_output_schema in ClickHouse to run schema-validation
checks. The SDK change enables SDK consumers to declare schemas on decorated functions.

Output: Updated schemas.py, batch.py, ingest.py, clickhouse.py, main.py, decorator.py
plus test_expected_output_schema_ingest.py.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-infrastructure/23-CONTEXT.md
@.planning/phases/23-infrastructure/23-01-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts from plan 23-01 output and existing files the executor needs. -->

From xeter/services/analyser/batch.py (AFTER plan 23-01, before this plan):
SPAN_COLUMNS = [
    "span_id", "trace_id", "parent_span_id", "tenant_id", "agent_name",
    "agent_model", "tool_name", "tool_description", "tool_arguments",
    "tool_output", "prompt_ref", "response_ref", "raw_response_ref",
    "available_tools_ref", "time_begin", "time_end", "schema_version",
]
# 17 items — expected_output_schema is NOT yet present; this plan inserts it at index 9 (after tool_arguments).

From xeter/services/analyser/ingest.py (current row construction comment, line ~106):
# SPAN_COLUMNS = [span_id, trace_id, parent_span_id, tenant_id, agent_name,
#                 agent_model, tool_name, tool_description, tool_arguments,
#                 tool_output, prompt_ref, ...]
# Row list items in same order; assert len(row) == len(SPAN_COLUMNS) at line 131.

From xeter/services/analyser/schemas.py (SpanPayload — current, no expected_output_schema):
class SpanPayload(BaseModel):
    span_id: str
    trace_id: Optional[str] = None
    parent_span_id: Optional[str] = None
    ...
    tool_arguments: Optional[str] = None  # JSON string
    tool_output: Optional[str] = None
    ...
    schema_version: str = Field(alias="xeter.schema.version", default="1.0")

From xeter/shared/db/clickhouse.py (SPANS_TABLE_DDL — does NOT yet have expected_output_schema):
SPANS_TABLE_DDL = """
CREATE TABLE IF NOT EXISTS spans (
    tenant_id ...
    tool_arguments      Nullable(String),
    tool_output         Nullable(String),
    available_tools_ref Nullable(String),
    ...
    schema_version      String DEFAULT '1.0'
)
"""

From xeter/services/analyser/main.py lifespan startup (current):
    ch_client = get_clickhouse_client()
    create_spans_table(ch_client)   # <- alter call goes immediately after this line
    app.state.batcher = SpanBatcher(ch_client)

From sdk/xeter_sdk/decorator.py trace() (current):
def trace(*, agent_name, agent_model, tool_name=None, tool_description=None,
          prompt_arg=None, tools_arg=None, trace_id=None, parent_span_id=None):
    ...
    def _dispatch(endpoint, api_key, kwargs, result, time_begin, time_end):
        ...
        span: dict = {
            "span_id": ..., "trace_id": trace_id, "parent_span_id": parent_span_id,
            "tool_arguments": tool_arguments,
            ...
        }
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add expected_output_schema to SpanPayload, SPAN_COLUMNS, and ingest row</name>
  <files>
    xeter/services/analyser/schemas.py,
    xeter/services/analyser/batch.py,
    xeter/services/analyser/ingest.py,
    xeter/tests/test_expected_output_schema_ingest.py
  </files>
  <read_first>
    - xeter/services/analyser/schemas.py — read SpanPayload; add expected_output_schema after tool_arguments; replicate Optional[str] = None pattern from parent_span_id (line 30)
    - xeter/services/analyser/batch.py — read SPAN_COLUMNS list (lines 38-56); insert "expected_output_schema" at index 9 (immediately after "tool_arguments", before "tool_output"); count total items to confirm 18
    - xeter/services/analyser/ingest.py — read the row construction list (lines 110-128) and the assert guard (line 131); insert span.expected_output_schema at the matching position in the row list (after span.tool_arguments, before span.tool_output); update the comment at line ~106-109 to include expected_output_schema
  </read_first>
  <behavior>
    - Test 1: SpanPayload(span_id="s", agent_name="a", agent_model="m", time_begin="t", time_end="t").expected_output_schema is None
    - Test 2: SpanPayload(..., expected_output_schema='{"type":"object"}').expected_output_schema == '{"type":"object"}'
    - Test 3: len(SPAN_COLUMNS) == 18
    - Test 4: SPAN_COLUMNS[9] == "expected_output_schema"  (index 9, after tool_arguments at index 8)
    - Test 5: Building a row list from a mock SpanPayload with expected_output_schema='{"type":"string"}' produces a list of length 18 that satisfies len(row) == len(SPAN_COLUMNS)
  </behavior>
  <action>
    In xeter/services/analyser/schemas.py, add to SpanPayload immediately after the tool_arguments line:
      expected_output_schema: Optional[str] = None  # inline JSON string, same pattern as tool_arguments

    In xeter/services/analyser/batch.py, insert "expected_output_schema" into SPAN_COLUMNS at index 9 (after "tool_arguments", before "tool_output"). Final SPAN_COLUMNS order at positions 8-10: tool_arguments, expected_output_schema, tool_output. Total count: 18 items.

    In xeter/services/analyser/ingest.py:
      1. Insert span.expected_output_schema into the row list immediately after span.tool_arguments and before span.tool_output. The row list will now have 18 items.
      2. Update the comment block above the row list to include expected_output_schema in the column listing.
      3. The assert at line 131 (assert len(row) == len(SPAN_COLUMNS)) requires no code change — it will enforce the new length automatically.

    Write xeter/tests/test_expected_output_schema_ingest.py with tests for all five behaviors above. Import SpanPayload from xeter.services.analyser.schemas and SPAN_COLUMNS from xeter.services.analyser.batch.

    CRITICAL: The three files (schemas.py, batch.py, ingest.py) must stay in sync — the column order in SPAN_COLUMNS, the field in SpanPayload, and the row list item in ingest.py must all align at position 9.
  </action>
  <verify>
    <automated>python -m pytest xeter/tests/test_expected_output_schema_ingest.py -v</automated>
  </verify>
  <acceptance_criteria>
    - xeter/services/analyser/schemas.py contains "expected_output_schema: Optional[str] = None"
    - xeter/services/analyser/batch.py SPAN_COLUMNS list has 18 items with "expected_output_schema" at index 9
    - xeter/services/analyser/ingest.py row list item for span.expected_output_schema is between span.tool_arguments and span.tool_output
    - xeter/services/analyser/ingest.py assert len(row) == len(SPAN_COLUMNS) is present and unchanged
    - pytest xeter/tests/test_expected_output_schema_ingest.py exits 0, all 5 tests pass
  </acceptance_criteria>
  <done>5 tests pass; SPAN_COLUMNS has 18 items; SpanPayload, SPAN_COLUMNS, and ingest row all have expected_output_schema in sync at position 9.</done>
</task>

<task type="auto">
  <name>Task 2: Add expected_output_schema DDL column and idempotent ALTER function; wire to analyser startup</name>
  <files>
    xeter/shared/db/clickhouse.py,
    xeter/services/analyser/main.py
  </files>
  <read_first>
    - xeter/shared/db/clickhouse.py — read the full SPANS_TABLE_DDL string (lines 58-83); find tool_arguments Nullable(String) column as the reference; also read create_spans_table() function (lines 91-101) for the pattern to replicate
    - xeter/services/analyser/main.py — read the lifespan function (lines 32-60); identify line 47 (create_spans_table(ch_client)) as the insertion point; also read the import block at the top (line 25: from xeter.shared.db.clickhouse import create_spans_table)
  </read_first>
  <action>
    In xeter/shared/db/clickhouse.py:
      1. In SPANS_TABLE_DDL, add a new column definition after the tool_arguments line:
           expected_output_schema  Nullable(String),
         The column must appear after "tool_arguments Nullable(String)," and before "tool_output Nullable(String),". Maintain the existing indentation style (4-space indent).
      2. Add a new function after create_spans_table():

         def alter_spans_add_expected_output_schema(client: clickhouse_connect.driver.Client) -> None:
             """Add expected_output_schema column to spans table if it does not exist.

             Idempotent — safe to call on every application startup (IF NOT EXISTS guard).
             Handles live deployments where the table was created before this column was added.

             Args:
                 client: A ClickHouse client returned by get_clickhouse_client().
             """
             client.command(
                 "ALTER TABLE spans ADD COLUMN IF NOT EXISTS "
                 "expected_output_schema Nullable(String)"
             )

    In xeter/services/analyser/main.py:
      1. Add alter_spans_add_expected_output_schema to the import on line 25:
           from xeter.shared.db.clickhouse import create_spans_table, alter_spans_add_expected_output_schema
      2. In the lifespan function, immediately after create_spans_table(ch_client) (line 47), add:
           alter_spans_add_expected_output_schema(ch_client)

    Decision references: D-04 (dual write: DDL for fresh deployments + ALTER for live deployments).
  </action>
  <verify>
    <automated>python -c "from xeter.shared.db.clickhouse import SPANS_TABLE_DDL, alter_spans_add_expected_output_schema; assert 'expected_output_schema' in SPANS_TABLE_DDL; print('DDL OK'); import inspect; src=inspect.getsource(alter_spans_add_expected_output_schema); assert 'IF NOT EXISTS' in src; print('ALTER OK')"</automated>
  </verify>
  <acceptance_criteria>
    - xeter/shared/db/clickhouse.py SPANS_TABLE_DDL contains "expected_output_schema  Nullable(String)"
    - xeter/shared/db/clickhouse.py has a function named alter_spans_add_expected_output_schema
    - alter_spans_add_expected_output_schema function body contains "IF NOT EXISTS"
    - xeter/services/analyser/main.py imports alter_spans_add_expected_output_schema from xeter.shared.db.clickhouse
    - xeter/services/analyser/main.py calls alter_spans_add_expected_output_schema(ch_client) on the line immediately after create_spans_table(ch_client)
  </acceptance_criteria>
  <done>DDL contains new column; alter function is idempotent (IF NOT EXISTS); analyser startup calls alter immediately after create_spans_table.</done>
</task>

<task type="auto">
  <name>Task 3: Add expected_output_schema kwarg to SDK trace() decorator</name>
  <files>
    sdk/xeter_sdk/decorator.py
  </files>
  <read_first>
    - sdk/xeter_sdk/decorator.py — read the full file; focus on trace() signature (line 44), the _dispatch() function (line 111), and the span dict construction (lines 134-151); parent_span_id is the exact pattern to replicate for expected_output_schema
  </read_first>
  <action>
    In sdk/xeter_sdk/decorator.py:

    1. Add expected_output_schema: dict | None = None as a keyword-only parameter to the trace() function signature, after parent_span_id: str | None = None. This is a decorator-level parameter, not call-time — it describes what the decorated function's output should always look like.

    2. In _dispatch(), before the span dict construction, add serialization of the kwarg:
         expected_output_schema_str = json.dumps(expected_output_schema) if expected_output_schema is not None else None

    3. In the span dict, add the key:
         "expected_output_schema": expected_output_schema_str,

    The key name must be "expected_output_schema" (matches SpanPayload field name).
    json is already imported at the top of the file.

    Decision reference: D-02 (decorator-level kwarg, serialized to JSON string by SDK).
  </action>
  <verify>
    <automated>python -c "
import inspect, json, sys
sys.path.insert(0, 'sdk')
from xeter_sdk.decorator import trace
sig = inspect.signature(trace)
assert 'expected_output_schema' in sig.parameters, 'kwarg missing from trace()'
print('trace() signature OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - sdk/xeter_sdk/decorator.py trace() signature includes "expected_output_schema: dict | None = None"
    - sdk/xeter_sdk/decorator.py _dispatch() contains "expected_output_schema_str = json.dumps(expected_output_schema) if expected_output_schema is not None else None"
    - sdk/xeter_sdk/decorator.py span dict contains key "expected_output_schema": expected_output_schema_str
    - Python import and signature inspection exits 0
  </acceptance_criteria>
  <done>SDK decorator accepts expected_output_schema kwarg; serializes dict to JSON string; includes in span dict sent to analyser.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | SpanPayload.expected_output_schema arrives from SDK over HTTP; stored as-is |
| analyser→ClickHouse | ALTER TABLE DDL executed at startup; must be idempotent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | Tampering | ingest.py row construction drift | mitigate | assert len(row) == len(SPAN_COLUMNS) at line 131 enforces sync at runtime |
| T-23-02-02 | Denial of Service | ALTER TABLE at every startup | accept | IF NOT EXISTS makes it a no-op after first run; ClickHouse alter is metadata-only |
| T-23-02-03 | Injection | expected_output_schema string from SDK user | accept | Stored as Nullable(String), not evaluated; schema validation is the consumer's job in Phase 24 |
</threat_model>

<verification>
- len(SPAN_COLUMNS) == 18 (was 17)
- SPAN_COLUMNS[9] == "expected_output_schema"
- SpanPayload has expected_output_schema field
- ingest.py row has 18 items with span.expected_output_schema at position 9
- SPANS_TABLE_DDL contains expected_output_schema Nullable(String)
- alter_spans_add_expected_output_schema body contains IF NOT EXISTS
- main.py calls alter immediately after create_spans_table
- trace() signature contains expected_output_schema: dict | None = None
- pytest xeter/tests/test_expected_output_schema_ingest.py -v exits 0
</verification>

<success_criteria>
1. POST /v1/spans with {"expected_output_schema": "{\"type\":\"object\"}"} accepted (200) and stored in ClickHouse
2. SPAN_COLUMNS has 18 items; assert in ingest.py does not fire
3. Analyser startup calls alter_spans_add_expected_output_schema(ch_client) — idempotent on repeat start
4. @xeter.trace(..., expected_output_schema={"type": "object"}) compiles and sends expected_output_schema as JSON string in span dict
5. All 5 tests in test_expected_output_schema_ingest.py pass
</success_criteria>

<output>
Create `.planning/phases/23-infrastructure/23-02-SUMMARY.md` when done
</output>
