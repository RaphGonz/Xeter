---
phase: 24-structural-span-checks
plan: 03
type: execute
wave: 3
depends_on:
  - 24-02
files_modified:
  - xeter/services/worker/main.py
  - xeter/scripts/calibrate.py
  - xeter/tests/test_calibrate_routing.py
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
  - SCHEMA-03
  - SCHEMA-04
  - CTX-01

must_haves:
  truths:
    - "Worker process loads OutputSchemaAnalyzer into the ANALYZERS list alongside ToolCallAnalyzer (main.py line ~177)"
    - "THRESHOLDS dict in main.py contains a context_overflow entry sourced from WORKER_THRESHOLD_CONTEXT_OVERFLOW env var with default 8000 (D-03)"
    - "calibrate.py FLAG_TYPES list contains 12 entries: 7 existing + 5 new (output_schema_violation, required_fields_missing, output_truncated, type_coercion_error, context_overflow)"
    - "calibrate.py FLAG_TYPE_TO_ANALYZER_CLASS contains 12 entries: 7 existing → ToolCallAnalyzer + 5 new → OutputSchemaAnalyzer"
    - "calibrate.py BINARY_FLAG_TYPES contains 7 entries: 3 existing + 4 new (output_schema_violation, required_fields_missing, output_truncated, type_coercion_error); context_overflow is NOT in this set (D-05)"
    - "calibrate.py DEFAULT_THRESHOLDS contains a context_overflow entry with value 8000 (D-03, D-05)"
    - "test_1_registry_has_exactly_7_keys is updated to assert == 12 (and renamed/replaced to match the new count); test_2_all_values_are_tool_call_analyzer_class is no longer universally true and must be updated to test only the 7 ToolCallAnalyzer entries"
    - "New tests in test_calibrate_routing.py verify the 5 new entries route to OutputSchemaAnalyzer, the 4 new binary entries are in BINARY_FLAG_TYPES, context_overflow is NOT in BINARY_FLAG_TYPES, and DEFAULT_THRESHOLDS['context_overflow'] == 8000"
    - "Full pytest run (python -m pytest xeter/tests/ -q) exits 0 — no regression in any existing test, all new tests pass"
  artifacts:
    - path: "xeter/services/worker/main.py"
      provides: "Worker entry point wired to OutputSchemaAnalyzer with context_overflow threshold"
      contains: "OutputSchemaAnalyzer(embedder, THRESHOLDS)"
    - path: "xeter/scripts/calibrate.py"
      provides: "Calibration harness routing 5 new flag types to OutputSchemaAnalyzer"
      contains: "OutputSchemaAnalyzer"
    - path: "xeter/tests/test_calibrate_routing.py"
      provides: "Updated registry tests for 12-key registry plus 5 new routing assertions"
      contains: "test_11_output_schema_checks_map_to_output_schema_analyzer"
  key_links:
    - from: "main.py THRESHOLDS dict"
      to: "OutputSchemaAnalyzer._check_context_overflow"
      via: "thresholds passed via OutputSchemaAnalyzer(embedder, THRESHOLDS) constructor"
      pattern: "context_overflow"
    - from: "calibrate.py FLAG_TYPE_TO_ANALYZER_CLASS"
      to: "evaluate_flag_type() analyzer instantiation"
      via: "registry lookup added in plan 23-03; new keys land here"
      pattern: "OutputSchemaAnalyzer"
    - from: "calibrate.py FLAG_TYPES list"
      to: "calibrate.py main loop iteration"
      via: "list iteration determines which flag types run hill_climb vs single-evaluation pass"
      pattern: "context_overflow"
---

<objective>
Wire OutputSchemaAnalyzer (created in plan 24-02) into the worker and calibration harness.
Three files change: main.py (worker bootstrap + THRESHOLDS), calibrate.py (4 registries),
test_calibrate_routing.py (assertion updates + 5 new tests). No analyzer logic changes —
this is pure registration so the existing class becomes reachable end-to-end.

Purpose: Without these wiring changes, the OutputSchemaAnalyzer class is dead code — the
worker never instantiates it, the calibration script does not know which class handles the
5 new flag types, and the existing routing test asserts a 7-key registry that this phase
extends to 12 keys. After this plan, every processed span is analyzed by both ToolCallAnalyzer
and OutputSchemaAnalyzer; every Phase 24 flag type is reachable from calibrate.py.

Output: Three modified files; one new test_1X_* set added to test_calibrate_routing.py.
Two existing tests (test_1, test_2) updated to reflect the 12-key expanded registry.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-structural-span-checks/24-CONTEXT.md
@.planning/phases/24-structural-span-checks/24-PATTERNS.md
@.planning/phases/24-structural-span-checks/24-02-SUMMARY.md
@.planning/phases/23-infrastructure/23-03-SUMMARY.md
</context>

<interfaces>
<!-- Current state of files to be modified. Read these before editing. -->

From xeter/services/worker/main.py (CURRENT — to be extended):
Line 44: from xeter.services.worker.tool_call_analyzer import ToolCallAnalyzer
Lines 52-58 (THRESHOLDS dict — 5 entries):
THRESHOLDS: dict[str, float] = {
    "tool_coherence_threshold": float(os.environ.get("WORKER_THRESHOLD_TOOL_COHERENCE", "0.15")),
    "unnecessary_tool_call":    float(os.environ.get("WORKER_THRESHOLD_UNNECESSARY_TOOL_CALL", "0.15")),
    "wrong_tool_args":          float(os.environ.get("WORKER_THRESHOLD_WRONG_TOOL_ARGS", "0.4")),
    "no_tool":                  float(os.environ.get("WORKER_THRESHOLD_NO_TOOL", "0.6")),
    "response_anomaly":         float(os.environ.get("WORKER_THRESHOLD_RESPONSE_ANOMALY", "0.4")),
}
Line 177 (ANALYZERS list — currently 1 entry):
analyzers = [ToolCallAnalyzer(embedder, THRESHOLDS)]

From xeter/scripts/calibrate.py (CURRENT — to be extended):
Line 26: from xeter.services.worker.tool_call_analyzer import ToolCallAnalyzer
Lines 47-55 (FLAG_TYPES list — 7 entries listed verbatim):
FLAG_TYPES = ["tool_not_available", "wrong_tool_choice", "unnecessary_tool_call", "wrong_tool_args", "no_tool", "parsing_error", "response_anomaly"]
Lines 65-73 (FLAG_TYPE_TO_ANALYZER_CLASS — 7 entries all → ToolCallAnalyzer)
Line 77 (BINARY_FLAG_TYPES — 3 entries):
BINARY_FLAG_TYPES: set[str] = {"tool_not_available", "wrong_tool_choice", "parsing_error"}
Lines 80-86 (DEFAULT_THRESHOLDS — 5 entries):
DEFAULT_THRESHOLDS: dict[str, float] = {
    "tool_coherence_threshold": 0.15,
    "unnecessary_tool_call":    0.15,
    "wrong_tool_args":          0.4,
    "no_tool":                  0.6,
    "response_anomaly":         0.4,
}

From xeter/tests/test_calibrate_routing.py (CURRENT — to be modified):
test_1_registry_has_exactly_7_keys (lines 19-24) — asserts len == 7
test_2_all_values_are_tool_call_analyzer_class (lines 27-35) — asserts all values are ToolCallAnalyzer
Tests 3-10 are unaffected by this phase
The file uses individual `def test_*()` functions (no class wrapper); add new tests at the bottom

New analyzer class to wire (already exists after plan 24-02):
from xeter.services.worker.output_schema_analyzer import OutputSchemaAnalyzer
Constructor signature: OutputSchemaAnalyzer(embedder, thresholds) — identical to ToolCallAnalyzer
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire OutputSchemaAnalyzer into worker main.py (ANALYZERS list + THRESHOLDS dict)</name>
  <files>xeter/services/worker/main.py</files>
  <read_first>
    - xeter/services/worker/main.py — lines 35-58 (imports + THRESHOLDS dict) and lines 170-185 (the analyzers list inside main()); these are the exact insertion points
    - xeter/services/worker/output_schema_analyzer.py — confirm the class exists and is importable from this path (plan 24-02 output)
    - .planning/phases/24-structural-span-checks/24-CONTEXT.md — D-03 (default 8000), D-05 (context_overflow is the only new threshold key — the 4 binary checks have no threshold)
    - .planning/phases/24-structural-span-checks/24-PATTERNS.md — section "xeter/services/worker/main.py (config/registry, modification)" lines 211-238 — exact pattern and comment style
  </read_first>
  <action>
    Modify xeter/services/worker/main.py in two places.

    Insertion 1 — add OutputSchemaAnalyzer import: After line 44 (the existing "from xeter.services.worker.tool_call_analyzer import ToolCallAnalyzer" line), add:
      from xeter.services.worker.output_schema_analyzer import OutputSchemaAnalyzer

    Insertion 2 — extend THRESHOLDS dict: Inside the THRESHOLDS dict declaration (currently lines 52-58, ending with the response_anomaly entry), add a new entry as the final dict entry:
      "context_overflow": float(os.environ.get("WORKER_THRESHOLD_CONTEXT_OVERFLOW", "8000")),  # [safe-default] calibration value; tune via calibration scripts

    The new entry MUST follow the exact existing pattern: dict key in double quotes, float(os.environ.get(...)) with env var name following the WORKER_THRESHOLD_* convention, default value as a string literal, trailing comma, trailing comment "# [safe-default] calibration value; tune via calibration scripts" matching all 5 existing entries.

    Env var name MUST be WORKER_THRESHOLD_CONTEXT_OVERFLOW (uppercase, matches the SCREAMING_SNAKE pattern of WORKER_THRESHOLD_TOOL_COHERENCE etc.).
    Default value MUST be the string "8000" (passed as string to os.environ.get, then float()-coerced — same pattern as existing entries).
    Decision reference: D-03 (default 8000).

    Insertion 3 — extend ANALYZERS list: Replace line 177 from:
      analyzers = [ToolCallAnalyzer(embedder, THRESHOLDS)]
    with:
      analyzers = [
          ToolCallAnalyzer(embedder, THRESHOLDS),
          OutputSchemaAnalyzer(embedder, THRESHOLDS),
      ]

    Both analyzers share the same THRESHOLDS dict — they read different keys from it. OutputSchemaAnalyzer reads only "context_overflow"; ToolCallAnalyzer reads the existing 5 keys. No key collision.

    Do NOT modify any other line in main.py. The BRPOP loop, signal handling, trace buffer, process_span function, and TraceAnalyzer wiring are unchanged.

    Decision references: D-03 (threshold default), D-05 (only context_overflow needs a threshold; the 4 binary checks have no threshold key).
  </action>
  <verify>
    <automated>python -c "import importlib; importlib.import_module('xeter.services.worker.main'); from xeter.services.worker.main import THRESHOLDS; assert 'context_overflow' in THRESHOLDS; assert THRESHOLDS['context_overflow'] == 8000.0, f'expected 8000.0 got {THRESHOLDS[\"context_overflow\"]}'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - xeter/services/worker/main.py contains the line "from xeter.services.worker.output_schema_analyzer import OutputSchemaAnalyzer" exactly once
    - THRESHOLDS dict has 6 entries (5 existing + context_overflow) — assert len(THRESHOLDS) == 6 when imported
    - THRESHOLDS["context_overflow"] equals 8000.0 when WORKER_THRESHOLD_CONTEXT_OVERFLOW env var is unset
    - The string "WORKER_THRESHOLD_CONTEXT_OVERFLOW" appears in main.py exactly once
    - main.py contains the literal "OutputSchemaAnalyzer(embedder, THRESHOLDS)" exactly once (inside the analyzers list)
    - main.py still contains the literal "ToolCallAnalyzer(embedder, THRESHOLDS)" exactly once (preserved)
    - The analyzers list in main() has length 2 — verifiable by reading the file with ast and inspecting the list literal at the assignment to "analyzers"
    - python -m pytest xeter/tests/worker/test_worker_loop.py -x exits 0 — worker bootstrap tests still pass with the new analyzer
    - The verify command above exits 0 with output "OK"
  </acceptance_criteria>
  <done>Worker bootstrap wires OutputSchemaAnalyzer with context_overflow=8000 default. Existing worker tests unchanged; new analyzer is instantiated on every worker start.</done>
</task>

<task type="auto">
  <name>Task 2: Extend calibrate.py registries (FLAG_TYPES, FLAG_TYPE_TO_ANALYZER_CLASS, BINARY_FLAG_TYPES, DEFAULT_THRESHOLDS)</name>
  <files>xeter/scripts/calibrate.py</files>
  <read_first>
    - xeter/scripts/calibrate.py — lines 26 (existing ToolCallAnalyzer import), 47-55 (FLAG_TYPES), 65-73 (FLAG_TYPE_TO_ANALYZER_CLASS), 77 (BINARY_FLAG_TYPES), 80-86 (DEFAULT_THRESHOLDS); these are the four extension points
    - .planning/phases/24-structural-span-checks/24-CONTEXT.md — D-05 (4 binary checks in BINARY_FLAG_TYPES; context_overflow has threshold in DEFAULT_THRESHOLDS)
    - .planning/phases/24-structural-span-checks/24-RESEARCH.md — Pitfall 5 (context_overflow MUST be in FLAG_TYPES list — otherwise hill climbing skips it)
    - .planning/phases/24-structural-span-checks/24-PATTERNS.md — section "xeter/scripts/calibrate.py (config/registry, modification)" lines 240-298 — exact additions
  </read_first>
  <action>
    Modify xeter/scripts/calibrate.py in four places. Do NOT modify any function bodies (evaluate_flag_type, hill_climb, _check_recall_floor, main); only edit the four module-level data structures.

    Insertion 1 — add OutputSchemaAnalyzer import: After line 26 (the existing "from xeter.services.worker.tool_call_analyzer import ToolCallAnalyzer" line), add:
      from xeter.services.worker.output_schema_analyzer import OutputSchemaAnalyzer

    Insertion 2 — extend FLAG_TYPES list (currently 7 entries at lines 47-55). Add 5 new string entries after the existing "response_anomaly" entry. Add an inline comment "# Phase 24 — OutputSchemaAnalyzer" above the new block. The 5 new entries in this exact order: "output_schema_violation", "required_fields_missing", "output_truncated", "type_coercion_error", "context_overflow". Final list length must be 12.

    Pitfall 5 reference: All 5 new entries MUST be in FLAG_TYPES (including the 4 binary ones) — the main() loop iterates over FLAG_TYPES to determine which types calibrate; binary types branch into single-evaluation pass via the BINARY_FLAG_TYPES check, but they must still appear in FLAG_TYPES to be considered. context_overflow runs through hill_climb (not in BINARY_FLAG_TYPES).

    Insertion 3 — extend FLAG_TYPE_TO_ANALYZER_CLASS dict (currently 7 entries at lines 65-73). Add 5 new key/value entries after the existing "response_anomaly" entry, all mapping to OutputSchemaAnalyzer. Keys in this exact order: "output_schema_violation", "required_fields_missing", "output_truncated", "type_coercion_error", "context_overflow". All values are the OutputSchemaAnalyzer class (the imported name, not a string). Final dict length must be 12. Add an inline comment "# Phase 24 — OutputSchemaAnalyzer routing" above the new block to mirror the FLAG_TYPES comment.

    Insertion 4 — extend BINARY_FLAG_TYPES set (currently 3 entries on line 77). Add the 4 binary Phase 24 checks. Final set must be exactly: {"tool_not_available", "wrong_tool_choice", "parsing_error", "output_schema_violation", "required_fields_missing", "output_truncated", "type_coercion_error"} — 7 entries. CRITICAL: context_overflow MUST NOT be in BINARY_FLAG_TYPES (D-05 — it is threshold-tunable, not binary).

    Insertion 5 — extend DEFAULT_THRESHOLDS dict (currently 5 entries at lines 80-86). Add one new entry: "context_overflow": 8000. Place it as the final dict entry. Final length must be 6. Value is the integer literal 8000 (matches the float comparison in the analyzer; jsonschema does not care; main.py wraps in float()). Decision reference: D-03 default; D-05 (context_overflow needs a threshold, binary checks do not).

    Do NOT touch FLAG_TYPE_ALIAS (still empty dict — no aliases needed). Do NOT touch hill_climb hyperparameters (HILL_CLIMB_START, HILL_CLIMB_STEP, HILL_CLIMB_MAX).
  </action>
  <verify>
    <automated>python -c "from xeter.scripts.calibrate import FLAG_TYPES, FLAG_TYPE_TO_ANALYZER_CLASS, BINARY_FLAG_TYPES, DEFAULT_THRESHOLDS; from xeter.services.worker.output_schema_analyzer import OutputSchemaAnalyzer; from xeter.services.worker.tool_call_analyzer import ToolCallAnalyzer; assert len(FLAG_TYPES) == 12, f'FLAG_TYPES len {len(FLAG_TYPES)}'; assert len(FLAG_TYPE_TO_ANALYZER_CLASS) == 12; assert set(FLAG_TYPE_TO_ANALYZER_CLASS.keys()) == set(FLAG_TYPES); new5 = {'output_schema_violation','required_fields_missing','output_truncated','type_coercion_error','context_overflow'}; assert all(FLAG_TYPE_TO_ANALYZER_CLASS[k] is OutputSchemaAnalyzer for k in new5); assert len(BINARY_FLAG_TYPES) == 7; assert 'context_overflow' not in BINARY_FLAG_TYPES; assert {'output_schema_violation','required_fields_missing','output_truncated','type_coercion_error'} <= BINARY_FLAG_TYPES; assert DEFAULT_THRESHOLDS['context_overflow'] == 8000; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - xeter/scripts/calibrate.py contains "from xeter.services.worker.output_schema_analyzer import OutputSchemaAnalyzer" exactly once
    - len(FLAG_TYPES) == 12 when imported; the 5 new strings are present in this exact set: output_schema_violation, required_fields_missing, output_truncated, type_coercion_error, context_overflow
    - len(FLAG_TYPE_TO_ANALYZER_CLASS) == 12; set(FLAG_TYPE_TO_ANALYZER_CLASS.keys()) == set(FLAG_TYPES) — invariant from plan 23-03 preserved
    - All 5 new keys in FLAG_TYPE_TO_ANALYZER_CLASS map to OutputSchemaAnalyzer (class object, not instance)
    - All 7 existing keys still map to ToolCallAnalyzer (no regression)
    - BINARY_FLAG_TYPES has exactly 7 entries; the 4 new binary entries are present; "context_overflow" is NOT present
    - DEFAULT_THRESHOLDS has 6 entries; DEFAULT_THRESHOLDS["context_overflow"] == 8000
    - python -c "import xeter.scripts.calibrate" exits 0 — module imports clean
    - The verify command above exits 0 with output "OK"
    - No body of any function in calibrate.py was modified (git diff shows only additions/edits in module-level data structures and one import line)
  </acceptance_criteria>
  <done>4 calibrate.py registries extended with 5 new flag types. All routing invariants from plan 23-03 preserved. context_overflow correctly classified as threshold-tunable (not binary).</done>
</task>

<task type="auto">
  <name>Task 3: Update test_calibrate_routing.py — fix superseded assertions and add 5 new Phase 24 routing tests</name>
  <files>xeter/tests/test_calibrate_routing.py</files>
  <read_first>
    - xeter/tests/test_calibrate_routing.py — lines 19-49 (the two tests that need updates: test_1_registry_has_exactly_7_keys and test_2_all_values_are_tool_call_analyzer_class); read the full file to confirm tests 3-10 do not need changes (they assert specific keys like "wrong_tool_args" still map to ToolCallAnalyzer, which remains true)
    - xeter/scripts/calibrate.py — confirm the post-Task 2 state contains 12 FLAG_TYPES, 12 FLAG_TYPE_TO_ANALYZER_CLASS entries, 7 BINARY_FLAG_TYPES entries, 6 DEFAULT_THRESHOLDS entries
    - .planning/phases/24-structural-span-checks/24-PATTERNS.md — section "xeter/tests/test_calibrate_routing.py (test, modification)" lines 414-451 — exact new tests to add
  </read_first>
  <action>
    Modify xeter/tests/test_calibrate_routing.py in two places: update two existing tests, append five new tests.

    Update 1 — rewrite test_1_registry_has_exactly_7_keys (lines 19-24) to test the new 12-key count. Rename to test_1_registry_has_exactly_12_keys. New body asserts:
      - FLAG_TYPE_TO_ANALYZER_CLASS is a dict
      - set(FLAG_TYPE_TO_ANALYZER_CLASS.keys()) == set(FLAG_TYPES)  (invariant from plan 23-03 preserved)
      - len(FLAG_TYPE_TO_ANALYZER_CLASS) == 12
    Update the docstring to read "FLAG_TYPE_TO_ANALYZER_CLASS is a dict with exactly 12 keys (7 ToolCallAnalyzer + 5 OutputSchemaAnalyzer) matching FLAG_TYPES."

    Update 2 — rewrite test_2_all_values_are_tool_call_analyzer_class (lines 27-35). The original assertion "all values are ToolCallAnalyzer" is no longer true after Phase 24. Rename to test_2_existing_7_keys_still_map_to_tool_call_analyzer. New body iterates over the 7 ToolCallAnalyzer-owned keys (the original FLAG_TYPES content) and asserts each maps to ToolCallAnalyzer:
      EXPECTED_TOOL_CALL_KEYS = {"tool_not_available", "wrong_tool_choice", "unnecessary_tool_call", "wrong_tool_args", "no_tool", "parsing_error", "response_anomaly"}
      for key in EXPECTED_TOOL_CALL_KEYS:
          assert FLAG_TYPE_TO_ANALYZER_CLASS[key] is ToolCallAnalyzer
    Update the docstring to read "All 7 pre-Phase-24 keys in FLAG_TYPE_TO_ANALYZER_CLASS still map to the ToolCallAnalyzer class (no regression)."

    Do NOT modify tests 3-10. They assert specific behaviors (test_3, test_4 about wrong_tool_args / response_anomaly mapping to ToolCallAnalyzer; test_5 about no inline import; test_6 about registry consultation; tests 7-10 about the recall floor) — all of these remain valid post-Phase-24.

    Append 5 new tests at the bottom of the file (after test_10_recall_floor_error_message_contains_flag_type_and_recall). Use sequential numbering test_11 through test_15.

    test_11_output_schema_checks_map_to_output_schema_analyzer:
      docstring: "All 5 Phase 24 flag types route to OutputSchemaAnalyzer via the registry."
      imports inside the function: from xeter.scripts.calibrate import FLAG_TYPE_TO_ANALYZER_CLASS; from xeter.services.worker.output_schema_analyzer import OutputSchemaAnalyzer
      body: for flag_type in ["output_schema_violation", "required_fields_missing", "output_truncated", "type_coercion_error", "context_overflow"]: assert FLAG_TYPE_TO_ANALYZER_CLASS[flag_type] is OutputSchemaAnalyzer

    test_12_binary_flag_types_includes_4_phase_24_schema_checks:
      docstring: "BINARY_FLAG_TYPES contains the 4 deterministic Phase 24 schema checks (excludes context_overflow)."
      body: from xeter.scripts.calibrate import BINARY_FLAG_TYPES; for flag_type in ["output_schema_violation", "required_fields_missing", "output_truncated", "type_coercion_error"]: assert flag_type in BINARY_FLAG_TYPES

    test_13_context_overflow_not_in_binary_flag_types:
      docstring: "context_overflow is threshold-tunable per D-05 — must NOT appear in BINARY_FLAG_TYPES."
      body: from xeter.scripts.calibrate import BINARY_FLAG_TYPES; assert "context_overflow" not in BINARY_FLAG_TYPES

    test_14_context_overflow_in_default_thresholds:
      docstring: "DEFAULT_THRESHOLDS contains context_overflow at the D-03 default value of 8000."
      body: from xeter.scripts.calibrate import DEFAULT_THRESHOLDS; assert "context_overflow" in DEFAULT_THRESHOLDS; assert DEFAULT_THRESHOLDS["context_overflow"] == 8000

    test_15_flag_types_list_has_12_entries:
      docstring: "FLAG_TYPES list has exactly 12 entries after Phase 24 — required so calibrate.py main() iterates over all new flag types."
      body: from xeter.scripts.calibrate import FLAG_TYPES; assert len(FLAG_TYPES) == 12; for flag_type in ["output_schema_violation", "required_fields_missing", "output_truncated", "type_coercion_error", "context_overflow"]: assert flag_type in FLAG_TYPES

    Decision references: D-03 (default threshold), D-05 (binary vs threshold-tunable), Pitfall 5 (FLAG_TYPES list must contain all 5 new entries including context_overflow).
  </action>
  <verify>
    <automated>python -m pytest xeter/tests/test_calibrate_routing.py -v</automated>
  </verify>
  <acceptance_criteria>
    - xeter/tests/test_calibrate_routing.py contains "def test_1_registry_has_exactly_12_keys" (the renamed test) and does NOT contain "def test_1_registry_has_exactly_7_keys"
    - xeter/tests/test_calibrate_routing.py contains "def test_2_existing_7_keys_still_map_to_tool_call_analyzer" and does NOT contain "def test_2_all_values_are_tool_call_analyzer_class"
    - Tests 3 through 10 are unchanged (grep for their function definitions returns the same lines as before)
    - xeter/tests/test_calibrate_routing.py contains all 5 new test functions: test_11_output_schema_checks_map_to_output_schema_analyzer, test_12_binary_flag_types_includes_4_phase_24_schema_checks, test_13_context_overflow_not_in_binary_flag_types, test_14_context_overflow_in_default_thresholds, test_15_flag_types_list_has_12_entries
    - python -m pytest xeter/tests/test_calibrate_routing.py -v exits 0 with exactly 15 tests passing (10 existing — 2 renamed in place — plus 5 new = 15 total)
    - Full suite python -m pytest xeter/tests/ -q exits 0 — Phase 24 introduces no regression
    - The phrase "test_1_registry_has_exactly_7_keys" appears nowhere in the codebase (grep returns 0 matches outside historical SUMMARY/PLAN docs)
  </acceptance_criteria>
  <done>test_calibrate_routing.py reflects the 12-key post-Phase-24 registry; 5 new routing tests cover all Phase 24 flag types; full test suite green. Phase 24 wiring complete end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| os.environ.get("WORKER_THRESHOLD_CONTEXT_OVERFLOW") → float() | Deployment-controlled env var; malformed string would raise ValueError on worker startup (fail-fast — acceptable) |
| calibrate.py FLAG_TYPE_TO_ANALYZER_CLASS lookup | Developer-controlled registry; KeyError on unknown flag type already guarded in main() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-10 | Tampering | WORKER_THRESHOLD_CONTEXT_OVERFLOW env var | mitigate via type-coercion | float() conversion of env string raises ValueError on malformed input, causing fail-fast worker startup (better than silent bad threshold) |
| T-24-11 | Denial of Service | Misconfigured threshold (e.g. set to 0) flags every span | accept | This is operator misconfiguration, not a security event; the same risk exists for the 5 pre-Phase-24 thresholds |
| T-24-12 | Tampering | Calibration script picks the wrong analyzer class via FLAG_TYPE_TO_ANALYZER_CLASS | mitigate | test_11 asserts all 5 new keys map to OutputSchemaAnalyzer; test_2 (renamed) asserts the 7 existing keys still map to ToolCallAnalyzer |
| T-24-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan; all deps installed in Phase 23 |
</threat_model>

<verification>
- python -c "import xeter.services.worker.main; import xeter.scripts.calibrate" exits 0 (both modules import after edits)
- THRESHOLDS dict in main.py has 6 entries with context_overflow == 8000 (default)
- All 4 calibrate.py registries (FLAG_TYPES, FLAG_TYPE_TO_ANALYZER_CLASS, BINARY_FLAG_TYPES, DEFAULT_THRESHOLDS) have the expected counts (12, 12, 7, 6 respectively)
- python -m pytest xeter/tests/test_calibrate_routing.py -v exits 0 with 15 passing tests
- python -m pytest xeter/tests/ -q exits 0 — full project suite is green
- All 5 phase requirement IDs (SCHEMA-01, SCHEMA-02, SCHEMA-03, SCHEMA-04, CTX-01) are now reachable end-to-end: worker instantiates OutputSchemaAnalyzer → analyzer fires the 5 flag types → calibrate.py knows which class evaluates each type
</verification>

<success_criteria>
- xeter/services/worker/main.py: 1 new import, 1 new THRESHOLDS entry, ANALYZERS list extended to 2 entries
- xeter/scripts/calibrate.py: 1 new import, 5 new FLAG_TYPES, 5 new FLAG_TYPE_TO_ANALYZER_CLASS entries, 4 new BINARY_FLAG_TYPES entries, 1 new DEFAULT_THRESHOLDS entry, context_overflow correctly excluded from BINARY_FLAG_TYPES
- xeter/tests/test_calibrate_routing.py: 2 tests updated, 5 new tests added, 15 total tests pass
- Full pytest suite (python -m pytest xeter/tests/ -q) exits 0 — Phase 24 ships
</success_criteria>

<output>
Create .planning/phases/24-structural-span-checks/24-03-SUMMARY.md when done.
</output>
