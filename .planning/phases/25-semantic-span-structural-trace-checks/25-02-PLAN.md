---
phase: 25-semantic-span-structural-trace-checks
plan: "02"
type: tdd
wave: 1
depends_on: []
files_modified:
  - xeter/tests/test_trace_analyzer.py
autonomous: true
requirements:
  - CTX-02
  - TRACE-01
  - TRACE-02
  - TRACE-03
  - TRACE-04

must_haves:
  truths:
    - "test_trace_analyzer.py exists with all test functions defined"
    - "Every test fails RED because TraceAnalyzer.analyze() returns [] (stub)"
    - "Test coverage spans all 5 check methods, their guard paths, and the log_score invariant per check"
  artifacts:
    - path: "xeter/tests/test_trace_analyzer.py"
      provides: "RED test scaffold for 5 TraceAnalyzer checks"
      min_lines: 160
  key_links:
    - from: "xeter/tests/test_trace_analyzer.py"
      to: "xeter/services/worker/trace_analyzer.py"
      via: "import TraceAnalyzer"
      pattern: "from xeter.services.worker.trace_analyzer import TraceAnalyzer"
---

<objective>
Write the RED test scaffold for all 5 TraceAnalyzer check methods (CTX-02 + TRACE-01 through TRACE-04).

Purpose: Establish the exact contract the TraceAnalyzer implementation must satisfy. Every flag-asserting test must fail because TraceAnalyzer.analyze() currently returns [] unconditionally (the stub from Phase 19).

Output: xeter/tests/test_trace_analyzer.py — 22 failing tests covering all 5 check methods, their guard paths, and key invariants.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/raphg/Desktop/Xeter/Xeter/.planning/PROJECT.md
@C:/Users/raphg/Desktop/Xeter/Xeter/.planning/ROADMAP.md
@C:/Users/raphg/Desktop/Xeter/Xeter/.planning/phases/25-semantic-span-structural-trace-checks/25-CONTEXT.md
@C:/Users/raphg/Desktop/Xeter/Xeter/.planning/phases/25-semantic-span-structural-trace-checks/25-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. All extracted from current codebase. -->

From xeter/services/worker/trace_analyzer.py (current stub — full file):
```python
class TraceAnalyzer(BaseTraceAnalyzer):
    def __init__(self, embedder: EmbedderClient, thresholds: dict[str, float]) -> None:
        super().__init__(embedder, thresholds)

    @property
    def name(self) -> str:
        return "trace_analyzer"

    def analyze(self, spans: list[SpanData]) -> list[Flag]:
        return []   # STUB — all tests that assert flags will fail RED
```

From xeter/services/worker/base.py:
```python
@dataclass
class SpanData:
    span_id: str
    tenant_id: str
    trace_id: str
    agent_name: str
    agent_model: str
    tool_name: Optional[str]
    tool_description: Optional[str]
    tool_arguments: Optional[str]
    tool_output: Optional[str]
    prompt: Optional[str]
    response: Optional[str]
    raw_response: Optional[str]
    available_tools: Optional[list[dict]]
    expected_output_schema: Optional[str] = None
    parent_span_id: Optional[str] = None

class BaseTraceAnalyzer(BaseAnalyzer):
    def analyze(self, spans: list[SpanData]) -> list[Flag]: ...

class BaseAnalyzer:
    def flush_scores(self) -> list[tuple[str, str, float]]: ...
    # tuple is (analyzer_name, metric_name, score)
```

CONTEXT.md decisions (enforced by tests):
- D-04: _check_stale_context compares span[i].prompt vs span[i-1].tool_output via fuzz.ratio
- D-05: stale_context uses fuzz.ratio (character-level), step_repetition uses fuzz.token_sort_ratio
- D-06: stale_context flag.detail has "low_confidence": True
- D-07: history_loss centroid = mean of ALL prior span prompts
- D-08: _check_history_loss guard: len(spans) < 3 → return []
- D-09: _check_context_propagation_failure compares span[i].prompt vs span[i-1].tool_output
- termination_loop: consecutive same tool_name count, not total; configurable as thresholds["termination_loop_n"]
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write RED tests for all 5 TraceAnalyzer check methods</name>
  <files>xeter/tests/test_trace_analyzer.py</files>

  <read_first>
    - xeter/services/worker/trace_analyzer.py — full file; confirm analyze() returns [] (stub state)
    - xeter/services/worker/base.py — SpanData dataclass all fields; Flag; BaseTraceAnalyzer; flush_scores tuple signature
    - xeter/tests/test_span_data_fields.py — _minimal_span_data factory to replicate as _make_spans helper
    - xeter/tests/test_calibrate_routing.py lines 73-119 — mock embedder and analyzer instantiation patterns
    - .planning/phases/25-semantic-span-structural-trace-checks/25-CONTEXT.md — D-04 through D-09, specifics section (termination_loop consecutive not total; span_id=None for trace flags)
    - .planning/phases/25-semantic-span-structural-trace-checks/25-PATTERNS.md — trace_analyzer section (full test structure patterns)
    - .planning/REQUIREMENTS.md — CTX-02, TRACE-01, TRACE-02, TRACE-03, TRACE-04 exact detection logic
  </read_first>

  <behavior>
    Guards (all should PASS even against the stub because the stub returns []):
    Test 1: test_single_span_returns_empty — ta.analyze([single_span]) == [] (all checks guard len<2, history_loss guards len<3)
    Test 2: test_history_loss_skips_two_span_trace — 2-span trace → no history_loss flag (len<3 guard)

    stale_context (CTX-02) — 4 tests:
    Test 3: test_stale_context_fires_when_prompt_copies_prior_tool_output — span[0].tool_output = text; span[1].prompt = text; threshold=50.0 → flag with flag_type=="stale_context"
    Test 4: test_stale_context_does_not_fire_on_fresh_prompt — span[0].tool_output = "weather in Tokyo"; span[1].prompt = "an entirely different topic"; threshold=85.0 → no stale_context flag
    Test 5: test_stale_context_detail_has_low_confidence_true — flag.detail["low_confidence"] is True
    Test 6: test_stale_context_skips_pair_when_prior_tool_output_is_none — span[0].tool_output=None; no crash, no flag

    step_repetition (TRACE-01) — 3 tests:
    Test 7: test_step_repetition_fires_on_near_duplicate_tool_calls — two spans with same tool_name and identical tool_arguments; threshold=80.0 → flag with flag_type=="step_repetition"
    Test 8: test_step_repetition_no_flag_on_distinct_tool_calls — two spans with different tool_name+tool_arguments; threshold=80.0 → no step_repetition flag
    Test 9: test_step_repetition_uses_token_sort_ratio — uses fuzz.token_sort_ratio semantics: "search city=Tokyo" vs "city=Tokyo search" should still fire (word-order invariant); threshold=80.0 → flag

    termination_loop (TRACE-02) — 3 tests:
    Test 10: test_termination_loop_fires_when_same_tool_called_n_times_consecutively — 4 spans with tool_name="search" consecutively; termination_loop_n=3 → flag with flag_type=="termination_loop"
    Test 11: test_termination_loop_no_flag_when_below_n — 2 consecutive same-tool spans; termination_loop_n=3 → no flag
    Test 12: test_termination_loop_resets_on_different_tool — spans: search, search, fetch, search, search; termination_loop_n=3 → no flag (consecutive run of search never reaches 3 after the reset)

    context_propagation_failure (TRACE-03) — 4 tests:
    Test 13: test_context_propagation_failure_fires_when_prompt_misses_prior_tool_output — span[0].tool_output has rich content; span[1].prompt is unrelated; embedder returns orthogonal vectors → flag with flag_type=="context_propagation_failure"
    Test 14: test_context_propagation_failure_no_flag_when_prompt_covers_tool_output — embedder returns identical vectors; prompt shares keywords with tool_output → no flag
    Test 15: test_context_propagation_failure_skips_when_prior_tool_output_none — span[0].tool_output=None → no crash, no flag
    Test 16: test_context_propagation_failure_logs_score — flush_scores() contains tuple with metric == "context_propagation_failure"

    history_loss (TRACE-04) — 4 tests:
    Test 17: test_history_loss_fires_when_prompt_disconnected_from_centroid — 3-span trace; embedder returns centroid vector far from span[2].prompt vector → flag with flag_type=="history_loss"
    Test 18: test_history_loss_no_flag_when_prompt_matches_centroid — embedder returns identical vectors for all prompts → score ~1.0 > threshold=0.4 → no flag
    Test 19: test_history_loss_skips_span_with_none_prompt — span[2].prompt=None → no crash, no flag for that span
    Test 20: test_history_loss_logs_score — flush_scores() contains tuple with metric == "history_loss"

    log_score invariants — 2 tests:
    Test 21: test_log_score_called_for_stale_context_on_valid_pair — valid 2-span pair → flush_scores() has entry with metric=="stale_context"
    Test 22: test_log_score_not_called_when_all_guards_trigger — single-span trace → flush_scores() returns [] (no scores because all checks returned early)
  </behavior>

  <action>
    Create xeter/tests/test_trace_analyzer.py. TraceAnalyzer already exists (Phase 19 stub) so the file will import without error — but tests that assert specific flag_type values will fail because analyze() returns [].

    File structure:
    1. Module docstring: "Tests for TraceAnalyzer — 5 trace-level checks. Plan: 25-02"
    2. Imports: from __future__ import annotations; from unittest.mock import MagicMock; import numpy as np; import pytest
    3. Helper _make_spans(n, **per_span_overrides): creates n SpanData objects with trace_id="tr1"; per_span_overrides values may be lists (one per span) or scalars (applied to all). Import SpanData inside helper body.
    4. Helper _make_trace_analyzer(thresholds=None): creates TraceAnalyzer with a MagicMock embedder. Default thresholds: {"stale_context": 85.0, "step_repetition": 85.0, "termination_loop_n": 3, "context_propagation_failure": 0.5, "history_loss": 0.4}. MagicMock embedder: encode returns np.ones(384); encode_batch returns [np.ones(384)].

    For Tests 1-2 (guard paths — will PASS against stub):
    - Confirm single-span and 2-span (for history_loss) return []. These PASS even in RED state.
    - NOTE: Tests 1-2 will PASS; all others (Tests 3-22) will fail against the stub. This is correct RED distribution.

    For stale_context tests (3-6):
    - Test 3: text = "the results from the previous search were: Paris 23°C, London 18°C". _make_spans(2, prompt=[None, text], tool_output=[text, None], tool_name=["step1", "step2"]). threshold={"stale_context": 50.0}. Assert any(f.flag_type == "stale_context" for f in flags).
    - Test 4: tool_output=["weather in Tokyo today", None], prompt=[None, "who invented the telephone"]. threshold=85.0. Assert not any(f.flag_type == "stale_context" for f in flags).
    - Test 5: Fire stale_context (test 3 setup) and assert flags[0].detail.get("low_confidence") is True.
    - Test 6: _make_spans(2, tool_output=[None, None], prompt=["a", "b"]). No crash; no stale_context flag.

    For step_repetition tests (7-9):
    - Test 7: _make_spans(2, tool_name=["search", "search"], tool_arguments=['{"q":"Paris"}', '{"q":"Paris"}']). threshold={"step_repetition": 80.0}. Assert step_repetition flag.
    - Test 8: _make_spans(2, tool_name=["search", "fetch"], tool_arguments=['{"q":"Paris"}', '{"url":"http://ex.com"}']). No step_repetition flag.
    - Test 9: _make_spans(2, tool_name=["search", "search"], tool_arguments=['{"q":"Paris"}', '{"q":"Paris"}']). Spans differ only in argument word order. threshold=80.0. Assert step_repetition flag. (token_sort_ratio is word-order invariant; this proves the implementation must use token_sort_ratio.)

    For termination_loop tests (10-12):
    - Test 10: _make_spans(4, tool_name=["search","search","search","search"]). threshold={"termination_loop_n": 3}. Assert termination_loop flag.
    - Test 11: _make_spans(2, tool_name=["search","search"]). threshold={"termination_loop_n": 3}. No termination_loop flag.
    - Test 12: _make_spans(5, tool_name=["search","search","fetch","search","search"]). threshold={"termination_loop_n": 3}. No termination_loop flag (consecutive run of search never hits 3 across the fetch break).

    For context_propagation_failure tests (13-16):
    - Tests 13-14: control embedder's encode output. For test 13: encode returns orthogonal vectors (cosine=0); tool_output="city=Paris population=2M"; prompt="unrelated topic". Assert flag. For test 14: encode returns identical vectors; prompt includes keywords from tool_output. No flag.
    - Test 15: _make_spans(2, tool_output=[None, None], prompt=["a", "b"]). No crash, no flag.
    - Test 16: valid pair with non-None tool_output and prompt. flush_scores() contains at least one tuple where tuple[1] == "context_propagation_failure".

    For history_loss tests (17-20):
    - Tests 17-18: control embedder. For test 17: encode_batch returns [np.ones(384), np.ones(384)] (centroid = np.ones(384)); encode returns np.zeros(384) for span[2].prompt → cosine=0 < threshold=0.4 → flag. For test 18: encode always returns np.ones(384) → cosine=1.0 > 0.4 → no flag.
    - Test 19: _make_spans(3, prompt=["a","b",None]). No crash.
    - Test 20: valid 3-span trace. flush_scores() tuple with metric=="history_loss".

    For log_score tests (21-22):
    - Test 21: 2-span trace with valid tool_output and prompt. flush_scores() has a tuple with metric=="stale_context".
    - Test 22: single-span trace. flush_scores() == [].

    Do NOT deferred-import TraceAnalyzer at the top level — it already exists as a stub so a top-level import is fine. Use top-level: from xeter.services.worker.trace_analyzer import TraceAnalyzer.
  </action>

  <verify>
    <automated>cd C:/Users/raphg/Desktop/Xeter/Xeter && python -m pytest xeter/tests/test_trace_analyzer.py --tb=no -q 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - File xeter/tests/test_trace_analyzer.py exists.
    - Running pytest on it: Tests 1-2 (guard tests against stub) PASS; Tests 3-22 FAIL with AssertionError (analyze() returns [] so no flags fire). Total: 2 passed, 20 failed.
    - All 22 test function names exist in the file matching the names in the behavior section above.
    - Test file imports TraceAnalyzer at module level (no deferred import needed — stub exists).
    - _make_spans helper accepts keyword lists-or-scalars per-span-override pattern as described.
    - No hardcoded float thresholds inside individual test assertions (thresholds always passed via _make_trace_analyzer(thresholds={...}) parameter).
    - Full existing suite shows no new failures: pytest xeter/tests/ --ignore=xeter/tests/test_trace_analyzer.py exits with same pre-existing failure count.
  </acceptance_criteria>

  <done>22 tests exist in test_trace_analyzer.py; 2 pass (guard tests), 20 fail RED (all flag-asserting tests).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → TraceAnalyzer stub | Test imports existing stub; behavior tests fail because stub returns [] |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02-SC | Tampering | No new packages installed in this plan | accept | No npm/pip/cargo installs in test scaffold plan |
</threat_model>

<verification>
pytest xeter/tests/test_trace_analyzer.py produces exactly 2 passed + 20 failed.
No regressions: full suite (excluding test_trace_analyzer.py) unchanged.
</verification>

<success_criteria>
22 tests exist covering all 5 TraceAnalyzer checks. 20 fail RED against the stub, 2 pass (correct guard behavior). Scaffold is complete and ready for GREEN implementation in Plan 25-04.
</success_criteria>

<output>
Create .planning/phases/25-semantic-span-structural-trace-checks/25-02-SUMMARY.md when done.
</output>
