---
phase: 27-calibration-pass
plan: 02
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - xeter/scripts/calibrate.py
  - fixtures/calibrated_thresholds.json
autonomous: false
requirements:
  - CAL-01

user_setup:
  - service: docker-embedder
    why: "calibrate.py requires a live embedder at http://localhost:8002 for embedding-based checks"
    env_vars: []
    dashboard_config:
      - task: "Start the embedder service before executing Task 1"
        location: "Terminal: docker compose -f deploy/docker-compose.yml up embedder"

must_haves:
  truths:
    - "calibrate.py --flag-type <type> completes without error for each of the 17 new types"
    - "No new flag type calibrates to R < 0.10 (recall floor not violated)"
    - "Each type is classified as binary or threshold-tunable based on calibration results"
    - "BINARY_FLAG_TYPES in calibrate.py is updated with Phase 26 binary classifications"
    - "calibrated_thresholds.json written with all new threshold values"
  artifacts:
    - path: "fixtures/calibrated_thresholds.json"
      provides: "Calibrated thresholds for all threshold-tunable new types"
      contains: "missing_details"
    - path: "xeter/scripts/calibrate.py"
      provides: "BINARY_FLAG_TYPES updated with any Phase 26 types classified as binary"
  key_links:
    - from: "xeter/scripts/calibrate.py"
      to: "fixtures/calibrated_thresholds.json"
      via: "THRESHOLDS_PATH.open('w')"
      pattern: "json.dump.*threshold_output"
---

<objective>
Run calibrate.py for each of the 17 new flag types against the extended fixture, fix any recall floor failures by improving fixture quality or adjusting thresholds, classify Phase 26 types as binary vs threshold-tunable, and produce calibrated_thresholds.json.

Purpose: After Plan 27-01, the fixture has coverage for all 17 new types and evaluate_flag_type() handles trace groups. This plan runs the actual calibration. If any type hits the recall floor (R < 0.10), the executor diagnoses and fixes the fixture or threshold before proceeding.

Output:
- calibrated_thresholds.json with all threshold-tunable new types calibrated
- BINARY_FLAG_TYPES in calibrate.py updated with Phase 26 binary classifications
- Each of 17 new types classified in the calibration report
</objective>

<execution_context>
@C:\Users\raphg\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\raphg\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\raphg\Desktop\Xeter\Xeter\.planning\PROJECT.md
@C:\Users\raphg\Desktop\Xeter\Xeter\.planning\ROADMAP.md
@C:\Users\raphg\Desktop\Xeter\Xeter\.planning\STATE.md

@C:\Users\raphg\Desktop\Xeter\Xeter\.planning\phases\27-calibration-pass\27-01-SUMMARY.md
</context>

<interfaces>
From calibrate.py (after Plan 27-01):

BINARY_FLAG_TYPES: set[str] = {
    "tool_not_available", "wrong_tool_choice", "parsing_error",
    "output_schema_violation", "required_fields_missing", "output_truncated",
    "type_coercion_error", "context_overflow",
}

DEFAULT_THRESHOLDS: dict[str, float] = {
    "tool_coherence_threshold": 0.15, "unnecessary_tool_call": 0.15, "wrong_tool_args": 0.4,
    "no_tool": 0.6, "response_anomaly": 0.4, "context_overflow": 8000,
    # Phase 25
    "missing_details": 0.6, "stale_context": 85.0, "context_propagation_failure": 0.5,
    "history_loss": 0.4, "step_repetition": 85.0, "termination_loop_n": 3,
    # Phase 26
    "conversation_reset": 0.25, "information_withholding": 0.5, "wrong_agent_handoff": 1.0,
    "clarification_skipped": 1.0, "no_verification": 1.0, "incomplete_verification": 0.7,
}

TRACE_LEVEL_TYPES: frozenset[str] (10 entries, added in Plan 27-01)
CALIBRATION_ROUTING_GRAPH: dict[str, list[str]] (added in Plan 27-01)

Recall floor: R >= 0.10 enforced by _check_recall_floor(); sys.exit(1) on violation.
Binary types: evaluated at their DEFAULT_THRESHOLDS value, no hill-climb.
Threshold-tunable: hill_climb() from 0.10 to 0.95 in 0.05 steps.
termination_loop: integer grid sweep [2,3,4,5] for termination_loop_n.

Phase 26 DEFAULT_THRESHOLDS starting values and their check logic:
  wrong_agent_handoff: 1.0 — binary-like (requires routing_graph; if graph absent always returns [])
  clarification_skipped: 1.0 — syntactic/deterministic (disjunctive marker + no ?)
  no_verification: 1.0 — binary (keyword scan, no continuous score)
  conversation_reset: 0.25 — cosine-based, threshold-tunable
  information_withholding: 0.5 — NE recall ratio, threshold-tunable
  incomplete_verification: 0.7 — entity coverage ratio, threshold-tunable
</interfaces>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: Run calibrate.py --flag-type for each new Phase 24/25 type and verify no recall floor failures</name>
  <read_first>
    - fixtures/labelled_spans.jsonl (verify the extended fixture exists and has new type rows)
    - xeter/scripts/calibrate.py (confirm TRACE_LEVEL_TYPES, CALIBRATION_ROUTING_GRAPH, group_spans_by_trace are present)
    - .planning/phases/27-calibration-pass/27-01-SUMMARY.md (confirm Task 1 and Task 2 complete)
  </read_first>
  <what-built>
    Plan 27-01 extended the fixture and updated evaluate_flag_type() for multi-span trace groups.
    Now run calibrate.py for each of the 12 Phase 24/25 types (span-level and trace-level):
      output_schema_violation, required_fields_missing, output_truncated, type_coercion_error,
      context_overflow, missing_details, stale_context, step_repetition, termination_loop,
      context_propagation_failure, history_loss

    Note: output_schema_violation, required_fields_missing, output_truncated, type_coercion_error,
    and context_overflow are already in BINARY_FLAG_TYPES — they use a single evaluation pass.
    missing_details, stale_context, step_repetition, context_propagation_failure, history_loss
    use hill_climb(); termination_loop uses integer grid sweep.

    Run each type individually first to catch recall floor failures early:
      docker compose -f deploy/docker-compose.yml up -d embedder
      python xeter/scripts/calibrate.py --flag-type output_schema_violation
      python xeter/scripts/calibrate.py --flag-type required_fields_missing
      python xeter/scripts/calibrate.py --flag-type output_truncated
      python xeter/scripts/calibrate.py --flag-type type_coercion_error
      python xeter/scripts/calibrate.py --flag-type context_overflow
      python xeter/scripts/calibrate.py --flag-type missing_details
      python xeter/scripts/calibrate.py --flag-type stale_context
      python xeter/scripts/calibrate.py --flag-type step_repetition
      python xeter/scripts/calibrate.py --flag-type termination_loop
      python xeter/scripts/calibrate.py --flag-type context_propagation_failure
      python xeter/scripts/calibrate.py --flag-type history_loss

    If any type exits with RECALL FLOOR ERROR:
    1. Run with --verbose to see false negatives: python xeter/scripts/calibrate.py --flag-type <type> --verbose
    2. Diagnose: are the fixture examples too ambiguous? Is the analyzer threshold starting point too high?
    3. Fix approach (choose one):
       a. Add more distinctive flagged examples to generate_labelled_fixture.py for that type, then re-run the generator
       b. Lower the DEFAULT_THRESHOLDS starting value for that type in calibrate.py if the issue is the starting threshold being too high
    4. Re-run the failing type until R >= 0.10

    After all 11 Phase 24/25 types pass, run the full Phase 24/25 subset:
      python xeter/scripts/calibrate.py --eval-only (to see current state without hill-climbing)
  </what-built>
  <how-to-verify>
    1. Start embedder: docker compose -f deploy/docker-compose.yml up -d embedder
    2. Wait for embedder health: curl http://localhost:8002/health
    3. Run each Phase 24/25 type individually and confirm no RECALL FLOOR ERROR in stdout
    4. Confirm each run prints "Calibration complete." or "P=X.XXX R=X.XXX" without sys.exit(1)
    5. If any type fails: apply fix from diagnose→fix steps above, re-run until it passes
  </how-to-verify>
  <resume-signal>Type "phase2425-ok" when all 11 Phase 24/25 types complete without recall floor errors</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Classify Phase 26 types as binary vs threshold-tunable and run Phase 26 calibration</name>
  <read_first>
    - xeter/scripts/calibrate.py (read BINARY_FLAG_TYPES, DEFAULT_THRESHOLDS, TRACE_LEVEL_TYPES)
    - xeter/services/worker/trace_analyzer.py (read _check_wrong_agent_handoff, _check_clarification_skipped, _check_no_verification implementations to confirm whether they produce a continuous score or a binary 0/1)
    - fixtures/labelled_spans.jsonl (confirm Phase 26 fixture rows are present)
    - xeter/tests/test_calibrate_routing.py (read current test names and count — Task 2 renames test_24 and adds test_28)
  </read_first>
  <action>
Classify each of the 6 Phase 26 types based on their check logic. Then update BINARY_FLAG_TYPES and run calibration.

Classification rules:
- A check is BINARY if its logic produces only 0.0 or 1.0 scores (no continuous threshold), or if it requires discrete config (routing_graph) that makes hill_climb meaningless.
- A check is THRESHOLD-TUNABLE if it produces a continuous score (cosine similarity, NE recall ratio, entity coverage ratio) that hill_climb can optimize.

Expected classification based on implementation:
  - wrong_agent_handoff: BINARY — check is topological (in routing_graph or not); DEFAULT 1.0 is already "fire or not"; hill_climb on [0.10,0.95] meaningless for a graph membership check. Add to BINARY_FLAG_TYPES.
  - clarification_skipped: BINARY — syntactic rule (disjunctive markers present + no ? in response); produces 0.0 or 1.0; DEFAULT 1.0 correct. Add to BINARY_FLAG_TYPES.
  - no_verification: BINARY — keyword scan (fire if no keyword in any span); produces 0.0 or 1.0; DEFAULT 1.0 correct. Add to BINARY_FLAG_TYPES.
  - conversation_reset: THRESHOLD-TUNABLE — cosine drop below threshold; hill_climb applies. Keep in hill_climb path.
  - information_withholding: THRESHOLD-TUNABLE — NE recall ratio below threshold; hill_climb applies. Keep in hill_climb path.
  - incomplete_verification: THRESHOLD-TUNABLE — entity coverage ratio below threshold; hill_climb applies. Keep in hill_climb path.

Read trace_analyzer.py to confirm these classifications match the actual implementation. If any implementation differs from the expected classification, use the implementation's actual behavior (not the expected classification above).

Update BINARY_FLAG_TYPES in calibrate.py to add the 3 binary Phase 26 types (wrong_agent_handoff, clarification_skipped, no_verification). The updated set has 11 entries total.

After updating BINARY_FLAG_TYPES, run each Phase 26 type individually (embedder must still be running):
  python xeter/scripts/calibrate.py --flag-type wrong_agent_handoff
  python xeter/scripts/calibrate.py --flag-type clarification_skipped
  python xeter/scripts/calibrate.py --flag-type no_verification
  python xeter/scripts/calibrate.py --flag-type conversation_reset
  python xeter/scripts/calibrate.py --flag-type information_withholding
  python xeter/scripts/calibrate.py --flag-type incomplete_verification

Apply the same recall-floor fix process as Task 1 if any type exits with RECALL FLOOR ERROR.

After all 6 types pass individually, run the full calibration suite:
  python xeter/scripts/calibrate.py

This writes calibrated_thresholds.json with all 24 flag types' results. The run succeeds when:
  - No RECALL FLOOR ERROR is printed
  - "Calibration complete." is printed
  - calibrated_thresholds.json is written to fixtures/

Do NOT update TERMINATION_LOOP_N_VALUES or BINARY_FLAG_TYPES beyond the Phase 26 classifications described above. The 8 pre-Phase-26 binary types are already correct.

test_24_phase26_types_not_in_binary_flag_types in test_calibrate_routing.py will now FAIL because wrong_agent_handoff, clarification_skipped, no_verification are being added to BINARY_FLAG_TYPES. Update this test:
- Rename test_24 to test_24_threshold_tunable_phase26_types_not_in_binary
- Assert only conversation_reset, information_withholding, incomplete_verification are NOT in BINARY_FLAG_TYPES
- Add test_28 asserting wrong_agent_handoff, clarification_skipped, no_verification ARE in BINARY_FLAG_TYPES
  </action>
  <verify>
    <automated>python -m pytest xeter/tests/test_calibrate_routing.py -x -q 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - BINARY_FLAG_TYPES has exactly 11 entries: `python -c "from xeter.scripts.calibrate import BINARY_FLAG_TYPES; assert len(BINARY_FLAG_TYPES)==11, len(BINARY_FLAG_TYPES); print('OK')"` exits 0
    - wrong_agent_handoff, clarification_skipped, no_verification are in BINARY_FLAG_TYPES: `python -c "from xeter.scripts.calibrate import BINARY_FLAG_TYPES; assert 'wrong_agent_handoff' in BINARY_FLAG_TYPES and 'clarification_skipped' in BINARY_FLAG_TYPES and 'no_verification' in BINARY_FLAG_TYPES; print('OK')"` exits 0
    - conversation_reset, information_withholding, incomplete_verification are NOT in BINARY_FLAG_TYPES: `python -c "from xeter.scripts.calibrate import BINARY_FLAG_TYPES; assert all(t not in BINARY_FLAG_TYPES for t in ['conversation_reset','information_withholding','incomplete_verification']); print('OK')"` exits 0
    - fixtures/calibrated_thresholds.json exists and contains at least 12 keys in its "thresholds" dict: `python -c "import json; d=json.load(open('fixtures/calibrated_thresholds.json')); assert len(d['thresholds'])>=12, d['thresholds'].keys(); print('OK')"` exits 0
    - calibrated_thresholds.json contains per_flag_type entries for each of the 17 new types: `python -c "import json; d=json.load(open('fixtures/calibrated_thresholds.json')); new=['output_schema_violation','required_fields_missing','output_truncated','type_coercion_error','context_overflow','missing_details','stale_context','step_repetition','termination_loop','context_propagation_failure','history_loss','wrong_agent_handoff','information_withholding','conversation_reset','clarification_skipped','no_verification','incomplete_verification']; missing=[t for t in new if t not in d.get('per_flag_type',{})]; assert not missing, missing; print('OK')"` exits 0
    - All routing tests pass: `python -m pytest xeter/tests/test_calibrate_routing.py -q` reports 28 passed (27 original + 1 new test_28), 0 failed
    - No RECALL FLOOR ERROR appears in any per-type calibration run (confirmed by user in Task 1 checkpoint + executor in Task 2 runs)
  </acceptance_criteria>
  <done>All 17 new flag types calibrated; 3 Phase 26 types correctly classified as binary; calibrated_thresholds.json written; 28 routing tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| embedder container → calibrate.py | HTTP embeddings from localhost:8002; local network only |
| calibrate.py → calibrated_thresholds.json | Output written to fixtures/; no external exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Spoofing | embedder health check (httpx.get) | accept | Localhost only; not an external service; no auth needed |
| T-27-02-02 | Information Disclosure | calibrated_thresholds.json | accept | Threshold values are not secrets; fixture artifacts in version control |
| T-27-02-03 | Denial of Service | context_overflow spans (9000-token prompts) in tiktoken evaluation | mitigate | tiktoken is local CPU-only; no HTTP calls; token count is bounded by fixture size |
| T-27-02-04 | Elevation of Privilege | docker compose up embedder | accept | Local development Docker only; embedder is read-only inference service |
| T-27-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies in this plan; slopcheck not triggered |
</threat_model>

<verification>
After both tasks complete:
- `python -m pytest xeter/tests/test_calibrate_routing.py -q` reports 28 passed, 0 failed
- `python -c "import json; d=json.load(open('fixtures/calibrated_thresholds.json')); ft=d['per_flag_type']; new=['output_schema_violation','required_fields_missing','output_truncated','type_coercion_error','context_overflow','missing_details','stale_context','step_repetition','termination_loop','context_propagation_failure','history_loss','wrong_agent_handoff','information_withholding','conversation_reset','clarification_skipped','no_verification','incomplete_verification']; missing=[t for t in new if t not in ft]; bad_r=[t for t in new if ft.get(t,{}).get('recall',1.0) is not None and ft[t].get('recall',1.0)<0.10]; print('missing:',missing,'bad recall:',bad_r); assert not missing and not bad_r"` exits 0
- `python -c "from xeter.scripts.calibrate import BINARY_FLAG_TYPES; assert len(BINARY_FLAG_TYPES)==11; print('BINARY_FLAG_TYPES has',len(BINARY_FLAG_TYPES),'entries')"` exits 0
</verification>

<success_criteria>
1. calibrate.py --flag-type <type> completes without error for all 17 new types (no RECALL FLOOR ERROR)
2. calibrated_thresholds.json contains per_flag_type entries for all 17 new types with recall >= 0.10
3. BINARY_FLAG_TYPES has 11 entries: 8 pre-Phase-26 + 3 Phase 26 binary types
4. 28 routing tests pass (test_28 added asserting Phase 26 binary classification)
</success_criteria>

<output>
Create .planning/phases/27-calibration-pass/27-02-SUMMARY.md when done
</output>
