---
phase: 27-calibration-pass
plan: 03
type: execute
wave: 3
depends_on:
  - 27-02
files_modified:
  - deploy/docker-compose.yml
  - xeter/scripts/calibrate.py
  - fixtures/calibrated_thresholds.json
autonomous: false
requirements:
  - CAL-01

must_haves:
  truths:
    - "Full-suite calibrate.py run completes with mean precision >= 95% across all flag types"
    - "docker-compose.yml WORKER_THRESHOLD_* env vars are patched for all threshold-tunable new types"
    - "patch_docker_compose() in calibrate.py covers all 18 threshold keys (13 existing + 5 new Phase 26)"
    - "calibrated_thresholds.json is the final artifact reflecting the full-suite run"
    - "All 28 routing tests pass after final calibrate.py changes"
  artifacts:
    - path: "deploy/docker-compose.yml"
      provides: "WORKER_THRESHOLD_* env vars patched for all threshold-tunable types"
      contains: "WORKER_THRESHOLD_CONVERSATION_RESET"
    - path: "fixtures/calibrated_thresholds.json"
      provides: "Final calibrated thresholds from full-suite run"
      contains: "per_flag_type"
    - path: "xeter/scripts/calibrate.py"
      provides: "patch_docker_compose key_to_env covers all 18 threshold keys"
      contains: "WORKER_THRESHOLD_CONVERSATION_RESET"
  key_links:
    - from: "xeter/scripts/calibrate.py patch_docker_compose"
      to: "deploy/docker-compose.yml"
      via: "DOCKER_COMPOSE_PATH.write_text(patched)"
      pattern: "DOCKER_COMPOSE_PATH.write_text"
    - from: "fixtures/calibrated_thresholds.json"
      to: "deploy/docker-compose.yml"
      via: "patch_docker_compose(calibrated)"
      pattern: "calibrated.get\\(key\\)"
---

<objective>
Complete the calibration pass: verify full-suite mean precision >= 95%, extend patch_docker_compose() to cover all threshold-tunable new types, apply the docker-compose patch, and commit all calibration artifacts.

Purpose: Plans 27-01 and 27-02 produced per-type calibration results. This plan confirms the full suite meets the v1.5 precision target, patches deploy/docker-compose.yml with the calibrated threshold values, and verifies the final state satisfies all CAL-01 success criteria.

Output:
- Full-suite calibrate.py run with all 24 flag types — mean precision reported >= 95%
- deploy/docker-compose.yml patched with WORKER_THRESHOLD_* values for all threshold-tunable new types
- calibrated_thresholds.json final artifact committed
- CAL-01 success criteria verified
</objective>

<execution_context>
@C:\Users\raphg\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\raphg\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\raphg\Desktop\Xeter\Xeter\.planning\PROJECT.md
@C:\Users\raphg\Desktop\Xeter\Xeter\.planning\ROADMAP.md
@C:\Users\raphg\Desktop\Xeter\Xeter\.planning\STATE.md

@C:\Users\raphg\Desktop\Xeter\Xeter\.planning\phases\27-calibration-pass\27-02-SUMMARY.md
</context>

<interfaces>
From deploy/docker-compose.yml (current state — only 4 WORKER_THRESHOLD_* entries):
  WORKER_THRESHOLD_WRONG_TOOL_ARGS: "0.1"
  WORKER_THRESHOLD_UNNECESSARY_TOOL_CALL: "0.25"
  WORKER_THRESHOLD_NO_TOOL: "0.15"
  WORKER_THRESHOLD_RESPONSE_ANOMALY: "0.1"

From calibrate.py patch_docker_compose() key_to_env (current 13 entries):
  tool_not_available → WORKER_THRESHOLD_TOOL_NOT_AVAILABLE
  wrong_tool_choice → WORKER_THRESHOLD_WRONG_TOOL_CHOICE
  unnecessary_tool_call → WORKER_THRESHOLD_UNNECESSARY_TOOL_CALL
  wrong_tool_args → WORKER_THRESHOLD_WRONG_TOOL_ARGS
  no_tool → WORKER_THRESHOLD_NO_TOOL
  response_anomaly → WORKER_THRESHOLD_RESPONSE_ANOMALY
  context_overflow → WORKER_THRESHOLD_CONTEXT_OVERFLOW
  missing_details → WORKER_THRESHOLD_MISSING_DETAILS
  stale_context → WORKER_THRESHOLD_STALE_CONTEXT
  context_propagation_failure → WORKER_THRESHOLD_CONTEXT_PROPAGATION_FAILURE
  history_loss → WORKER_THRESHOLD_HISTORY_LOSS
  step_repetition → WORKER_THRESHOLD_STEP_REPETITION
  termination_loop_n → WORKER_THRESHOLD_TERMINATION_LOOP_N

Missing from key_to_env (Phase 26 threshold-tunable types, need to be added):
  conversation_reset → WORKER_THRESHOLD_CONVERSATION_RESET
  information_withholding → WORKER_THRESHOLD_INFORMATION_WITHHOLDING
  incomplete_verification → WORKER_THRESHOLD_INCOMPLETE_VERIFICATION

Binary types (wrong_agent_handoff, clarification_skipped, no_verification) have
calibrated.get(key) returning None in patch_docker_compose — they are correctly skipped
when they appear in key_to_env. Do NOT add them to key_to_env (no threshold to patch).

From calibrate.py main() precision check (lines 620-646):
  - Binary types: status "OK" if precision >= 0.80
  - Threshold-tunable types: status "OK" if precision >= 0.80
  - all_pass is True only when all types have precision >= 0.80
  - Full-suite mean precision is NOT automatically computed — must be calculated manually
    from the results dict returned by main()

Full-suite mean precision formula:
  mean_precision = mean([res["best_precision"] for res in results.values()])
  Target: mean_precision >= 0.95
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 26 threshold env vars to patch_docker_compose() and docker-compose.yml</name>
  <read_first>
    - xeter/scripts/calibrate.py (read patch_docker_compose() key_to_env dict — lines 454-469)
    - deploy/docker-compose.yml (read full file to find the worker environment section and existing WORKER_THRESHOLD_* entries)
    - fixtures/calibrated_thresholds.json (read the "thresholds" dict to get calibrated values for conversation_reset, information_withholding, incomplete_verification)
  </read_first>
  <action>
Two changes in one task to keep them atomic:

1. Update patch_docker_compose() in calibrate.py: add 3 new entries to key_to_env dict:
   "conversation_reset":       "WORKER_THRESHOLD_CONVERSATION_RESET",
   "information_withholding":  "WORKER_THRESHOLD_INFORMATION_WITHHOLDING",
   "incomplete_verification":  "WORKER_THRESHOLD_INCOMPLETE_VERIFICATION",
   Key_to_env grows from 13 to 16 entries. Do NOT add wrong_agent_handoff, clarification_skipped, or no_verification — they are binary; calibrated.get(key) returns None for them and patch_docker_compose() already skips None values.

2. Add corresponding WORKER_THRESHOLD_* lines to deploy/docker-compose.yml in the worker environment section, adjacent to the existing WORKER_THRESHOLD_* vars. Use the calibrated values from fixtures/calibrated_thresholds.json. Format:
   WORKER_THRESHOLD_CONVERSATION_RESET: "<value>"
   WORKER_THRESHOLD_INFORMATION_WITHHOLDING: "<value>"
   WORKER_THRESHOLD_INCOMPLETE_VERIFICATION: "<value>"
   Also add all the other missing threshold env vars that are in key_to_env but not yet in docker-compose.yml:
   WORKER_THRESHOLD_TOOL_NOT_AVAILABLE: "<value>"
   WORKER_THRESHOLD_WRONG_TOOL_CHOICE: "<value>"
   WORKER_THRESHOLD_CONTEXT_OVERFLOW: "<value>"
   WORKER_THRESHOLD_MISSING_DETAILS: "<value>"
   WORKER_THRESHOLD_STALE_CONTEXT: "<value>"
   WORKER_THRESHOLD_CONTEXT_PROPAGATION_FAILURE: "<value>"
   WORKER_THRESHOLD_HISTORY_LOSS: "<value>"
   WORKER_THRESHOLD_STEP_REPETITION: "<value>"
   WORKER_THRESHOLD_TERMINATION_LOOP_N: "<value>"
   Use the calibrated values from fixtures/calibrated_thresholds.json["thresholds"] for all. For binary types that don't have a threshold key (tool_not_available, wrong_tool_choice have None in calibrated dict), use their DEFAULT_THRESHOLDS value (tool_not_available has no threshold key — omit it; wrong_tool_choice has no threshold — omit it). Only add entries for keys that exist in key_to_env and have a non-None calibrated value.
  </action>
  <verify>
    <automated>python -c "
import re
content = open('deploy/docker-compose.yml', encoding='utf-8').read()
required = ['WORKER_THRESHOLD_CONVERSATION_RESET','WORKER_THRESHOLD_INFORMATION_WITHHOLDING','WORKER_THRESHOLD_INCOMPLETE_VERIFICATION']
missing = [v for v in required if v not in content]
print('Missing from docker-compose.yml:', missing)
assert not missing, missing
print('PASS')
"</automated>
  </verify>
  <acceptance_criteria>
    - patch_docker_compose() key_to_env has 16 entries: `python -c "import inspect; from xeter.scripts.calibrate import patch_docker_compose; src=inspect.getsource(patch_docker_compose); count=src.count('WORKER_THRESHOLD_'); print('count:', count); assert count>=16"` exits 0
    - deploy/docker-compose.yml contains WORKER_THRESHOLD_CONVERSATION_RESET, WORKER_THRESHOLD_INFORMATION_WITHHOLDING, WORKER_THRESHOLD_INCOMPLETE_VERIFICATION
    - `python -c "import re; c=open('deploy/docker-compose.yml').read(); assert 'WORKER_THRESHOLD_CONVERSATION_RESET' in c and 'WORKER_THRESHOLD_INFORMATION_WITHHOLDING' in c and 'WORKER_THRESHOLD_INCOMPLETE_VERIFICATION' in c; print('OK')"` exits 0
    - All 28 routing tests still pass: `python -m pytest xeter/tests/test_calibrate_routing.py -q` reports 28 passed
  </acceptance_criteria>
  <done>patch_docker_compose() covers all 16 threshold-tunable keys; docker-compose.yml has all WORKER_THRESHOLD_* env vars</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Run full-suite calibrate.py and verify mean precision >= 95%</name>
  <read_first>
    - fixtures/calibrated_thresholds.json (current state after Plan 27-02 partial runs)
    - deploy/docker-compose.yml (confirm WORKER_THRESHOLD_* lines added in Task 1)
  </read_first>
  <what-built>
    All calibration infrastructure is in place. Run the full calibration suite and verify the precision target.

    Commands (embedder must be running):
      docker compose -f deploy/docker-compose.yml up -d embedder
      python xeter/scripts/calibrate.py

    This run:
    1. Evaluates all 24 flag types
    2. Writes the final calibrated_thresholds.json
    3. Patches deploy/docker-compose.yml WORKER_THRESHOLD_* values in-place
    4. Prints the per-flag-type summary table

    After the run, compute mean precision:
      python -c "
    import json
    d = json.load(open('fixtures/calibrated_thresholds.json'))
    precisions = [v['precision'] for v in d['per_flag_type'].values() if v.get('precision') is not None]
    mean_p = sum(precisions) / len(precisions) if precisions else 0
    print(f'Flag types evaluated: {len(precisions)}')
    print(f'Mean precision: {mean_p:.3f}')
    print('TARGET MET' if mean_p >= 0.95 else f'TARGET MISSED — mean={mean_p:.3f} < 0.95')
    "

    If mean precision < 0.95:
    1. Identify which flag types have precision < 0.90 from the summary table
    2. Add more clean fixture examples for types with high FP rates, or more distinctive flagged examples for types with low TP rates
    3. Re-run the generator: python xeter/scripts/generate_labelled_fixture.py
    4. Re-run calibration for the failing types: python xeter/scripts/calibrate.py --flag-type <type>
    5. Re-run full suite until mean precision >= 0.95

    After a successful full-suite run, also run the routing tests one final time:
      python -m pytest xeter/tests/test_calibrate_routing.py -q
  </what-built>
  <how-to-verify>
    1. docker compose -f deploy/docker-compose.yml up -d embedder
    2. python xeter/scripts/calibrate.py  (confirm "Calibration complete." at end)
    3. python -c "import json; d=json.load(open('fixtures/calibrated_thresholds.json')); precisions=[v['precision'] for v in d['per_flag_type'].values() if v.get('precision') is not None]; mp=sum(precisions)/len(precisions); print(f'Mean precision: {mp:.3f}'); assert mp>=0.95, f'{mp:.3f} < 0.95'"
    4. python -m pytest xeter/tests/test_calibrate_routing.py -q  (28 passed)
    5. Inspect deploy/docker-compose.yml to confirm WORKER_THRESHOLD_CONVERSATION_RESET etc. have the calibrated numeric values (not placeholder strings)
    6. Inspect fixtures/calibrated_thresholds.json to confirm per_flag_type has entries for all 24 flag types
  </how-to-verify>
  <resume-signal>Type "full-suite-ok" with the mean precision value (e.g., "full-suite-ok 0.97") when full-suite run passes and mean precision >= 0.95</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| calibrate.py → docker-compose.yml | patch_docker_compose writes threshold values into deploy config; values come from calibration results |
| embedder HTTP → calibrate.py | embeddings from localhost:8002; no network exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | Tampering | patch_docker_compose writes to deploy/docker-compose.yml | accept | Values are calibrated numeric thresholds; no secrets; version-controlled |
| T-27-03-02 | Information Disclosure | calibrated_thresholds.json contains precision/recall data | accept | Non-sensitive model behavior data; committed to repo for reproducibility |
| T-27-03-03 | Denial of Service | full-suite calibrate.py run (embedding calls for 24 types) | accept | Local Docker only; bounded by fixture size (~370 rows); acceptable runtime |
| T-27-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies in this plan; slopcheck not triggered |
</threat_model>

<verification>
After both tasks complete:

Source assertions:
- `python -c "import re; c=open('deploy/docker-compose.yml').read(); vars=['WORKER_THRESHOLD_CONVERSATION_RESET','WORKER_THRESHOLD_INFORMATION_WITHHOLDING','WORKER_THRESHOLD_INCOMPLETE_VERIFICATION']; missing=[v for v in vars if v not in c]; assert not missing, missing; print('docker-compose OK')"` exits 0
- `python -c "from xeter.scripts.calibrate import BINARY_FLAG_TYPES; assert len(BINARY_FLAG_TYPES)==11; print('BINARY_FLAG_TYPES OK')"` exits 0

Behavior assertions:
- `python -m pytest xeter/tests/test_calibrate_routing.py -q` reports 28 passed, 0 failed
- `python -c "import json; d=json.load(open('fixtures/calibrated_thresholds.json')); precisions=[v['precision'] for v in d['per_flag_type'].values() if v.get('precision') is not None]; mp=sum(precisions)/len(precisions); assert mp>=0.95 and len(d['per_flag_type'])==24, f'mean={mp:.3f}, count={len(d[\"per_flag_type\"])}'; print(f'Full-suite mean precision: {mp:.3f} — PASS')"` exits 0

CAL-01 success criteria cross-check:
1. Every new flag type has a key in THRESHOLDS or BINARY_FLAG_TYPES in calibrate.py — verified by routing tests + fixture coverage
2. No new flag type calibrates to R < 0.10 — enforced by _check_recall_floor() in calibrate.py; verified by full-suite run completing without sys.exit(1)
3. Full-suite mean precision >= 95% — verified by precision assertion above
4. Calibration report identifies binary vs threshold-tuned — verified by per_flag_type entries in calibrated_thresholds.json (binary=true field)
</verification>

<success_criteria>
1. Full-suite calibrate.py runs to completion without RECALL FLOOR ERROR
2. Mean precision across all 24 flag types >= 0.95 (computed from calibrated_thresholds.json)
3. deploy/docker-compose.yml has WORKER_THRESHOLD_* env vars for all threshold-tunable types including Phase 26
4. calibrated_thresholds.json is the definitive calibration artifact with per_flag_type entries for all 24 types
5. 28 routing tests pass
6. All CAL-01 success criteria are met
</success_criteria>

<output>
Create .planning/phases/27-calibration-pass/27-03-SUMMARY.md when done
</output>
