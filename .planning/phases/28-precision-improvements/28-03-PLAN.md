---
phase: 28-precision-improvements
plan: 03
type: execute
wave: 2
depends_on:
  - 28-01
  - 28-02
files_modified:
  - fixtures/calibrated_thresholds.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Each of the 14 fixed flag types has been re-calibrated individually"
    - "Each calibrated type shows precision improved vs Phase 27 baseline (or documented why not)"
    - "No RECALL FLOOR ERROR for any type (recall >= 0.10 for all 14 types)"
    - "calibrated_thresholds.json updated with new threshold values for all 14 types"
  artifacts:
    - path: "fixtures/calibrated_thresholds.json"
      provides: "Updated thresholds for all 14 fixed flag types"
      contains: "stale_context"
  key_links:
    - from: "python -m xeter.scripts.calibrate --flag-type <type>"
      to: "fixtures/calibrated_thresholds.json"
      via: "per-type merge write"
      pattern: "per_flag_type"
---

<objective>
Re-calibrate all 14 fixed flag types individually to verify precision improved and update calibrated_thresholds.json.

Purpose: Algorithm fixes in Plans 28-01 and 28-02 changed the score distributions for all 14 types. The thresholds from Phase 27 are now stale — stale_context and step_repetition in particular were calibrated against 0–100 scores and now need recalibration against 0.0–1.0 scores. Each per-type run runs the hill_climb or integer grid sweep and merges results into calibrated_thresholds.json.

Output: Updated fixtures/calibrated_thresholds.json with improved thresholds for all 14 types. No code changes.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-precision-improvements/28-CONTEXT.md
@.planning/phases/27-calibration-pass/27-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-calibrate Tier 1 and Tier 2 types (tool_not_available, stale_context, step_repetition, termination_loop)</name>
  <files>fixtures/calibrated_thresholds.json</files>
  <read_first>
    - fixtures/calibrated_thresholds.json — current per_flag_type entries for these 4 types (baseline to compare against)
    - xeter/scripts/calibrate.py — BINARY_FLAG_TYPES (tool_not_available is binary — uses eval-only path); termination_loop uses TERMINATION_LOOP_N_VALUES integer grid; stale_context and step_repetition use hill_climb on the 0.0–0.95 range (now valid after /100 fix)
    - .planning/phases/28-precision-improvements/28-CONTEXT.md — D-17 (per-type calibration requirement), calibration_context baselines
  </read_first>
  <action>
Run these four calibration commands IN ORDER. Each merges into calibrated_thresholds.json, so later runs build on earlier results.

Embedder must be running first. Verify with: `python -c "import httpx; r = httpx.get('http://localhost:8002/health', timeout=5); r.raise_for_status(); print('embedder OK')"`. If not running, start with `docker compose up embedder -d` and wait for health check.

Then run:
1. `python -m xeter.scripts.calibrate --flag-type tool_not_available`
2. `python -m xeter.scripts.calibrate --flag-type stale_context`
3. `python -m xeter.scripts.calibrate --flag-type step_repetition`
4. `python -m xeter.scripts.calibrate --flag-type termination_loop`

For each run, record the output precision and recall values.

Phase 27 baselines for comparison:
- tool_not_available: P=0.000, R=0.000 (target: P > 0 OR confirmed fixture-driven with documented root cause from Plan 28-02)
- stale_context: P=0.154, R=1.000 (target: P > 0.154 and R >= 0.10)
- step_repetition: P=0.104, R=1.000 (target: P > 0.104 and R >= 0.10)
- termination_loop: P=0.625, R=1.000 (target: P > 0.625 and R >= 0.10; or explain why unchanged)

If ANY run exits with RECALL FLOOR ERROR: stop, diagnose why the algorithm over-tightened, and report to the user before continuing. Do NOT suppress the error.

If tool_not_available still produces P=0 and R=0 after the Plan 28-02 fix: this confirms the diagnosis is fixture-driven (not a code bug). Accept and continue — the Plan 28-02 executor should have documented this.
  </action>
  <verify>
    <automated>python -c "
import json
data = json.load(open('fixtures/calibrated_thresholds.json'))
pft = data['per_flag_type']
sc = pft.get('stale_context', {})
sr = pft.get('step_repetition', {})
tl = pft.get('termination_loop', {})
assert sc.get('recall', 0) >= 0.10, f'stale_context recall floor violated: {sc.get(\"recall\")}'
assert sr.get('recall', 0) >= 0.10, f'step_repetition recall floor violated: {sr.get(\"recall\")}'
assert tl.get('recall', 0) >= 0.10, f'termination_loop recall floor violated: {tl.get(\"recall\")}'
print(f'stale_context: P={sc.get(\"precision\"):.4f} R={sc.get(\"recall\"):.4f} (baseline P=0.1538)')
print(f'step_repetition: P={sr.get(\"precision\"):.4f} R={sr.get(\"recall\"):.4f} (baseline P=0.1042)')
print(f'termination_loop: P={tl.get(\"precision\"):.4f} R={tl.get(\"recall\"):.4f} (baseline P=0.2083)')
print('OK: Tier 1/2 recall floors satisfied')
"
</automated>
  </verify>
  <acceptance_criteria>
    - `python -m xeter.scripts.calibrate --flag-type stale_context` completes without RECALL FLOOR ERROR; calibrated_thresholds.json updated with new stale_context threshold and P > 0.154
    - `python -m xeter.scripts.calibrate --flag-type step_repetition` completes without RECALL FLOOR ERROR; calibrated_thresholds.json updated with new step_repetition threshold and P > 0.104
    - `python -m xeter.scripts.calibrate --flag-type termination_loop` completes without RECALL FLOOR ERROR; integer grid result written; P >= 0.625 (or higher) and R >= 0.10
    - tool_not_available calibration result documented (P=0 confirmed fixture-driven, or fixed and improved)
    - All four types have updated entries in calibrated_thresholds.json per_flag_type section
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-calibrate Tier 3 types (missing_details, no_verification)</name>
  <files>fixtures/calibrated_thresholds.json</files>
  <read_first>
    - fixtures/calibrated_thresholds.json — current entries for missing_details (P=0.019, threshold=0.15) and no_verification (P=0.025, binary)
    - xeter/scripts/calibrate.py — no_verification is in BINARY_FLAG_TYPES (eval-only, no hill climb); missing_details uses hill_climb
    - .planning/phases/28-precision-improvements/28-CONTEXT.md — D-17, and the Tier 3 baseline descriptions
  </read_first>
  <action>
Run in order:
1. `python -m xeter.scripts.calibrate --flag-type missing_details`
2. `python -m xeter.scripts.calibrate --flag-type no_verification`

Phase 27 baselines:
- missing_details: P=0.019, R=0.125 (target: P > 0.019 and R >= 0.10)
- no_verification: P=0.025, R=1.000 (target: P > 0.025 and R >= 0.10)

missing_details is threshold-tunable. After the NE/lemma precondition fix in Plan 28-02, the hill_climb should find a meaningfully better threshold. If the hill_climb still converges at low precision (e.g., P < 0.10), check whether the --verbose flag reveals what kinds of spans are still generating false positives: `python -m xeter.scripts.calibrate --flag-type missing_details --verbose`.

no_verification is binary (keyword scan). After the write/mutate precondition fix in Plan 28-01, the binary evaluation should show P > 0.025. If precision is still near 0, add --verbose to diagnose: `python -m xeter.scripts.calibrate --flag-type no_verification --verbose`.

Record the precision and recall for both types and document in the SUMMARY.
  </action>
  <verify>
    <automated>python -c "
import json
data = json.load(open('fixtures/calibrated_thresholds.json'))
pft = data['per_flag_type']
md = pft.get('missing_details', {})
nv = pft.get('no_verification', {})
assert md.get('recall', 0) >= 0.10, f'missing_details recall floor violated: {md.get(\"recall\")}'
assert nv.get('recall', 0) >= 0.10, f'no_verification recall floor violated: {nv.get(\"recall\")}'
print(f'missing_details: P={md.get(\"precision\"):.4f} R={md.get(\"recall\"):.4f} (baseline P=0.0192)')
print(f'no_verification: P={nv.get(\"precision\"):.4f} R={nv.get(\"recall\"):.4f} (baseline P=0.0250)')
print('OK: Tier 3 recall floors satisfied')
"
</automated>
  </verify>
  <acceptance_criteria>
    - `python -m xeter.scripts.calibrate --flag-type missing_details` completes without RECALL FLOOR ERROR; P > 0.019 (improved vs baseline); R >= 0.10; new threshold written to calibrated_thresholds.json
    - `python -m xeter.scripts.calibrate --flag-type no_verification` completes without RECALL FLOOR ERROR; P > 0.025 (improved vs baseline); R >= 0.10; result written to calibrated_thresholds.json
    - If precision still low, --verbose diagnostic output is included in the SUMMARY to guide potential follow-up
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Re-calibrate Tier 4 types (context_propagation_failure, wrong_agent_handoff, wrong_tool_choice, history_loss, information_withholding, wrong_tool_args, response_anomaly, conversation_reset)</name>
  <files>fixtures/calibrated_thresholds.json</files>
  <read_first>
    - fixtures/calibrated_thresholds.json — current per_flag_type entries for these 8 types (baselines listed below)
    - xeter/scripts/calibrate.py — wrong_agent_handoff is in BINARY_FLAG_TYPES; wrong_tool_choice is in BINARY_FLAG_TYPES; all others use hill_climb
    - .planning/phases/28-precision-improvements/28-CONTEXT.md — D-17, and the Tier 4 baseline descriptions
  </read_first>
  <action>
Run these 8 calibration commands in order (each merges into calibrated_thresholds.json):

1. `python -m xeter.scripts.calibrate --flag-type context_propagation_failure`
2. `python -m xeter.scripts.calibrate --flag-type wrong_agent_handoff`
3. `python -m xeter.scripts.calibrate --flag-type wrong_tool_choice`
4. `python -m xeter.scripts.calibrate --flag-type history_loss`
5. `python -m xeter.scripts.calibrate --flag-type information_withholding`
6. `python -m xeter.scripts.calibrate --flag-type wrong_tool_args`
7. `python -m xeter.scripts.calibrate --flag-type response_anomaly`
8. `python -m xeter.scripts.calibrate --flag-type conversation_reset`

Phase 27 baselines for comparison (target: each P > baseline, R >= 0.10):
- context_propagation_failure: P=0.250, R=1.000
- wrong_agent_handoff: P=0.300, R=1.000 (binary)
- wrong_tool_choice: P=0.300 (shown as 0.3913 in JSON), R=0.818 (binary)
- history_loss: P=0.400, R=1.000
- information_withholding: P=0.444, R=1.000
- wrong_tool_args: P=0.455, R=1.000
- response_anomaly: P=0.458, R=1.000
- conversation_reset: P=0.600, R=1.000

If ANY run exits with RECALL FLOOR ERROR: stop, diagnose, and report before continuing. Do NOT continue to the next type.

For each binary type (wrong_agent_handoff, wrong_tool_choice): if precision is still low after the algorithm fix, add --verbose to diagnose FPs, and document which clean span types are being incorrectly flagged.

Record all results in the SUMMARY.
  </action>
  <verify>
    <automated>python -c "
import json
data = json.load(open('fixtures/calibrated_thresholds.json'))
pft = data['per_flag_type']
tier4 = ['context_propagation_failure', 'wrong_agent_handoff', 'wrong_tool_choice',
         'history_loss', 'information_withholding', 'wrong_tool_args',
         'response_anomaly', 'conversation_reset']
baselines = {
    'context_propagation_failure': 0.250,
    'wrong_agent_handoff': 0.300,
    'wrong_tool_choice': 0.300,
    'history_loss': 0.400,
    'information_withholding': 0.444,
    'wrong_tool_args': 0.455,
    'response_anomaly': 0.458,
    'conversation_reset': 0.600,
}
all_ok = True
for ft in tier4:
    entry = pft.get(ft, {})
    p = entry.get('precision', 0.0)
    r = entry.get('recall', 0.0)
    baseline = baselines[ft]
    recall_ok = r >= 0.10
    if not recall_ok:
        print(f'FAIL {ft}: recall {r:.4f} < 0.10 floor')
        all_ok = False
    direction = 'IMPROVED' if p > baseline else 'SAME/WORSE'
    print(f'  {ft}: P={p:.4f} R={r:.4f} (baseline P={baseline}) [{direction}]')
if all_ok:
    print('OK: all Tier 4 recall floors satisfied')
"
</automated>
  </verify>
  <acceptance_criteria>
    - All 8 types have updated entries in calibrated_thresholds.json per_flag_type section (timestamps or threshold values changed vs Phase 27 entries)
    - No RECALL FLOOR ERROR from any of the 8 calibration runs
    - R >= 0.10 for all 8 types (verify with the automated command above)
    - Precision improved vs Phase 27 baseline for at least 6 of the 8 types (2 may be unchanged if algorithm fix had limited effect — document these)
    - `python -m pytest xeter/tests/test_calibrate_routing.py -q` — 28 passed, 0 failed (calibration runs should not break routing tests)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| embedder HTTP → calibrate.py | Calibration calls the live embedder; responses are float arrays, no untrusted string data |
| calibrate.py → calibrated_thresholds.json | JSON write; file is a developer artifact in version control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03-01 | Information Disclosure | calibrated_thresholds.json | accept | Developer artifact committed to version control; contains no secrets, only numeric thresholds |
| T-28-03-02 | Denial of Service | embedder container | accept | Calibration requires live embedder; failure exits with a clear error message |
| T-28-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
After all three tasks complete:

1. `python -c "import json; d = json.load(open('fixtures/calibrated_thresholds.json')); pft = d['per_flag_type']; print(len(pft), 'types in per_flag_type')"` — shows at least 24 entries
2. All 14 fixed types appear in per_flag_type with updated values
3. `python -m pytest xeter/tests/test_calibrate_routing.py -q` — 28 passed, 0 failed
4. No RECALL FLOOR ERROR in any of the 14 calibration run outputs
5. The 12 non-broken types all show P > Phase 27 baseline (or documented exception for tool_not_available)
</verification>

<success_criteria>
- All 14 fixed flag types re-calibrated (tool_not_available, stale_context, step_repetition, termination_loop, missing_details, no_verification, context_propagation_failure, wrong_agent_handoff, wrong_tool_choice, history_loss, information_withholding, wrong_tool_args, response_anomaly, conversation_reset)
- calibrated_thresholds.json has updated per_flag_type entries for all 14 types
- No RECALL FLOOR ERROR for any type
- R >= 0.10 for all 14 types
- At least 12 of 14 types show precision improvement vs Phase 27 baseline
- 28 routing tests still pass
</success_criteria>

<output>
Create `.planning/phases/28-precision-improvements/28-03-SUMMARY.md` when done.
</output>
