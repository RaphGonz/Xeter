---
phase: 28-precision-improvements
plan: 04
type: execute
wave: 3
depends_on:
  - 28-03
files_modified:
  - fixtures/calibrated_thresholds.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Full-suite calibration run completes across all 24 flag types without RECALL FLOOR ERROR"
    - "Mean precision across all 24 flag types is >= 95%"
    - "calibrated_thresholds.json has 18 threshold keys and 24 per_flag_type entries after the run"
    - "28 routing tests pass after the full calibration run"
    - "Phase 28 is marked complete and Phase 27 plan 27-03 is unblocked"
  artifacts:
    - path: "fixtures/calibrated_thresholds.json"
      provides: "Final thresholds for all 24 flag types after full-suite run"
      contains: "per_flag_type"
  key_links:
    - from: "python -m xeter.scripts.calibrate (no --flag-type)"
      to: "fixtures/calibrated_thresholds.json"
      via: "full-suite write"
      pattern: "CALIBRATION RESULT"
    - from: "mean precision check"
      to: "Phase 27 plan 27-03"
      via: "unblock signal"
      pattern: "all_pass"
---

<objective>
Run the full-suite calibration (all 24 flag types) and verify mean precision >= 95% to unblock Phase 27 plan 27-03.

Purpose: Plans 28-01 through 28-03 fixed algorithms and per-type thresholds for 14 types. The full-suite run validates the complete picture: all 24 types calibrated together, the 10 non-fixed types (which were already P=1.0 or high-precision) retained, and the final mean precision target confirmed. This is the gate for Phase 27 plan 27-03 (full-suite calibration run + docker-compose WORKER_THRESHOLD_* patch).

Output: Final calibrated_thresholds.json with all 24 types. Phase 28 complete. Phase 27 plan 27-03 unblocked.
</objective>

<execution_context>
@C:/Users/raphg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/raphg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-precision-improvements/28-CONTEXT.md
@.planning/phases/27-calibration-pass/27-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Full-suite calibration run with timeout awareness</name>
  <files>fixtures/calibrated_thresholds.json</files>
  <read_first>
    - fixtures/calibrated_thresholds.json — current state after Plan 28-03 (all 14 fixed types have updated per_flag_type entries); the 10 non-fixed types should still be P=1.0 from their Phase 24/25/26 baselines
    - xeter/scripts/calibrate.py — main() function flow; note the full-suite run iterates FLAG_TYPES (24 types); BINARY_FLAG_TYPES gets eval-only pass; non-binary types use hill_climb; termination_loop uses integer grid; full-suite run re-writes calibrated_thresholds.json from scratch (not merge mode)
    - .planning/phases/27-calibration-pass/27-02-SUMMARY.md — Deviation 4: "Full calibration suite timed out — per-type runs used as equivalent" — the same timeout risk exists here; plan accordingly
    - .planning/phases/28-precision-improvements/28-CONTEXT.md — D-18 (final full-suite run requirement)
  </read_first>
  <action>
Attempt the full-suite calibration run with a generous timeout. The command is:
`python -m xeter.scripts.calibrate`

Note: Phase 27 Deviation 4 documented that the full-suite run can time out (~15+ minutes). The bash timeout is 120 seconds by default. To handle this:

**Approach 1 (preferred):** Run the full-suite command with explicit timeout in Bash (up to 600000ms allowed): use `timeout 600 python -m xeter.scripts.calibrate` (Linux/Mac) or run via Python subprocess with a timeout. If the shell allows, increase the Bash timeout to 600000ms.

**Approach 2 (fallback, if full-suite still times out):** The per-type calibration in Plan 28-03 already updated all 14 fixed types. The remaining 10 types (the ones that were P=1.0 in Phase 27) can be verified individually:
- `python -m xeter.scripts.calibrate --flag-type output_schema_violation`
- `python -m xeter.scripts.calibrate --flag-type required_fields_missing`
- `python -m xeter.scripts.calibrate --flag-type output_truncated`
- `python -m xeter.scripts.calibrate --flag-type type_coercion_error`
- `python -m xeter.scripts.calibrate --flag-type context_overflow`
- `python -m xeter.scripts.calibrate --flag-type parsing_error`
- `python -m xeter.scripts.calibrate --flag-type unnecessary_tool_call`
- `python -m xeter.scripts.calibrate --flag-type no_tool`
- `python -m xeter.scripts.calibrate --flag-type clarification_skipped`
- `python -m xeter.scripts.calibrate --flag-type incomplete_verification`

This produces the same calibrated_thresholds.json output as the full-suite run (via the merge logic confirmed in Phase 27 Deviation 4). Use Approach 2 if Approach 1 times out, and document the deviation in the SUMMARY.

After the calibration run(s) complete (either approach), verify calibrated_thresholds.json has the expected structure.
  </action>
  <verify>
    <automated>python -c "
import json
data = json.load(open('fixtures/calibrated_thresholds.json'))
n_thresholds = len(data.get('thresholds', {}))
n_per_flag = len(data.get('per_flag_type', {}))
print(f'thresholds keys: {n_thresholds} (expected 18)')
print(f'per_flag_type entries: {n_per_flag} (expected 24)')
assert n_per_flag >= 24, f'Expected 24 per_flag_type entries, got {n_per_flag}'
print('OK: calibrated_thresholds.json has correct shape')
"
</automated>
  </verify>
  <acceptance_criteria>
    - calibrated_thresholds.json has >= 18 keys in "thresholds" and exactly 24 entries in "per_flag_type"
    - No RECALL FLOOR ERROR during the calibration run(s)
    - If Approach 2 (per-type fallback) was used, all 24 types were individually calibrated and merged
    - The run approach (Approach 1 or 2) is documented in the SUMMARY
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify mean precision >= 95% and confirm routing tests pass</name>
  <files>fixtures/calibrated_thresholds.json</files>
  <read_first>
    - fixtures/calibrated_thresholds.json — per_flag_type section; all 24 entries with their precision values
    - xeter/tests/test_calibrate_routing.py — 28 routing tests; these test the calibration infrastructure wiring, not precision values; must remain green
    - .planning/phases/28-precision-improvements/28-CONTEXT.md — D-18: "confirm mean precision >= 95%"; calibration_context: "full-suite mean precision target (>= 95%) from current ~60-70% estimated mean across 24 types"
  </read_first>
  <action>
Compute the mean precision across all 24 flag types from calibrated_thresholds.json and verify the target is met.

Run the mean precision check:
```
python -c "
import json
data = json.load(open('fixtures/calibrated_thresholds.json'))
pft = data['per_flag_type']
precisions = [v['precision'] for v in pft.values() if v.get('precision') is not None]
mean_p = sum(precisions) / len(precisions)
print(f'Mean precision across {len(precisions)} types: {mean_p:.4f}')
print(f'Target: >= 0.95')
below = [(k, v['precision']) for k, v in pft.items() if v.get('precision', 1.0) < 0.95]
if below:
    print('Types below 0.95:')
    for ft, p in sorted(below, key=lambda x: x[1]):
        print(f'  {ft}: P={p:.4f}')
if mean_p >= 0.95:
    print('TARGET MET: mean precision >= 95%')
else:
    print(f'TARGET NOT MET: mean precision {mean_p:.4f} < 0.95')
"
```

Then run the routing tests:
`python -m pytest xeter/tests/test_calibrate_routing.py -q`

If mean precision is below 0.95: identify which types are pulling the mean down (the output above lists them). Check if any of the 14 fixed types still have very low precision (e.g., tool_not_available at P=0.0 is the known exception; missing_details and no_verification were the worst performers). Tool_not_available at P=0 is the confirmed fixture issue (not an algorithm failure) — if excluding it brings the mean to >= 0.95, document this in the SUMMARY.

The mean precision target (>= 95%) is the gate. If it is not met even after excluding tool_not_available (which was already P=0 before Phase 28), report the shortfall clearly with: the current mean, the types below 0.80, and a recommendation (additional algorithm tuning or fixture extension per the deferred ideas in CONTEXT.md).

Do NOT alter algorithm code in this plan — this plan is verification only. If the target is not met, create a clear summary section "REMAINING GAPS" listing types that need further work, so the user can decide whether to accept the result or start a follow-up phase.
  </action>
  <verify>
    <automated>python -m pytest xeter/tests/test_calibrate_routing.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest xeter/tests/test_calibrate_routing.py -q` exits 0 with 28 passed, 0 failed
    - The mean precision computation runs without error and produces a value
    - If mean_p >= 0.95: "TARGET MET" is printed and documented in SUMMARY — Phase 28 complete, Phase 27 plan 27-03 is unblocked
    - If mean_p < 0.95 but only tool_not_available (confirmed fixture-driven, P=0) is below 0.80: document this exception and report the mean excluding tool_not_available; if that mean is >= 0.95, mark the target as conditionally met pending fixture work
    - If mean_p < 0.95 for non-fixture reasons: SUMMARY includes a "REMAINING GAPS" section with types below 0.80 and recommended next actions
    - No new code changes are made in this task (verification only)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| embedder HTTP → calibrate.py | Same as Plan 28-03; calibration calls the live embedder; responses are float arrays |
| calibrated_thresholds.json → docker-compose.yml | calibrate.py's patch_docker_compose() updates WORKER_THRESHOLD_* env vars in deploy/docker-compose.yml |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04-01 | Information Disclosure | calibrated_thresholds.json | accept | Developer artifact in version control; numeric thresholds only, no secrets |
| T-28-04-02 | Tampering | deploy/docker-compose.yml | accept | calibrate.py's patch_docker_compose() rewrites threshold env vars in-place; reviewed in Plan 27-03 scope |
| T-28-04-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
Final verification for Phase 28:

1. `python -m pytest xeter/tests/test_calibrate_routing.py -q` — 28 passed, 0 failed
2. `python -c "import json; d = json.load(open('fixtures/calibrated_thresholds.json')); pft = d['per_flag_type']; ps = [v['precision'] for v in pft.values() if v.get('precision') is not None]; print(f'mean P={sum(ps)/len(ps):.4f} across {len(ps)} types')"` — prints mean precision >= 0.95 (or documents exception)
3. calibrated_thresholds.json has 24 per_flag_type entries
4. Phase 27 plan 27-03 is confirmed unblocked (mean precision target met or exception documented)
</verification>

<success_criteria>
- Full-suite calibration completes (either Approach 1 or Approach 2 per-type fallback)
- calibrated_thresholds.json has 24 per_flag_type entries
- 28 routing tests pass
- Mean precision across 24 types is >= 0.95, OR exception clearly documented (tool_not_available fixture gap) with the excluding-that-type mean also >= 0.95
- Phase 28 complete; Phase 27 plan 27-03 unblocked
</success_criteria>

<output>
Create `.planning/phases/28-precision-improvements/28-04-SUMMARY.md` when done.
</output>
