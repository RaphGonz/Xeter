---
phase: 30-diagnosticer-prompt
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - xeter/services/diagnosticer/prompt.md
  - xeter/tests/diagnosticer/test_context_assembly.py
autonomous: true
requirements: [DIAG-02]

must_haves:
  truths:
    - "prompt.md contains a system message section that frames the diagnosticer's role as root-cause analysis, not general Q&A"
    - "prompt.md contains a chain-of-thought scaffold that instructs the model to walk through each flag before reaching a verdict"
    - "prompt.md contains explicit decision criteria distinguishing all four verdicts: model, architecture, prompt, unknown"
    - "prompt.md contains severity calibration guidance for high, medium, and low"
    - "All nine span placeholders plus prompt_text, response_text, flags_section remain present so format_map substitution still succeeds with no KeyError"
    - "The existing /diagnose endpoint tests and context assembly tests still pass"
  artifacts:
    - path: "xeter/services/diagnosticer/prompt.md"
      provides: "Rewritten prompt: system message + CoT scaffold + four-verdict criteria + severity calibration"
      contains: "record_diagnosis"
    - path: "xeter/tests/diagnosticer/test_context_assembly.py"
      provides: "Content-presence assertions for the four required DIAG-02 sections"
      contains: "def test_prompt_content"
  key_links:
    - from: "xeter/services/diagnosticer/prompt.md"
      to: "anthropic.py:_DIAGNOSIS_TOOL verdict enum"
      via: "verdict names (model/architecture/prompt/unknown) and record_diagnosis tool reference must match"
      pattern: "record_diagnosis"
    - from: "xeter/tests/diagnosticer/test_context_assembly.py"
      to: "rendered _format_context output"
      via: "string-presence assertions on required sections"
      pattern: "_format_context"
---

<objective>
Rewrite the content of `prompt.md` (extracted in Plan 01) into a structured diagnostic prompt that satisfies DIAG-02: (a) a system message section framing the diagnosticer's role as root-cause analysis, (b) a chain-of-thought scaffold that walks through each flag before reaching a verdict, (c) explicit decision criteria for all four verdicts (`model` / `architecture` / `prompt` / `unknown`), and (d) severity calibration guidance.

Purpose: Give the LLM explicit reasoning guidance so it produces well-justified verdicts and fewer ambiguous `unknown` results, replacing the minimal "You are diagnosing a failing AI agent tool call" framing.
Output: Rewritten `prompt.md` content and new content-presence tests appended to `test_context_assembly.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-diagnosticer-prompt/30-RESEARCH.md

@xeter/services/diagnosticer/providers/anthropic.py

<interfaces>
<!-- Verdict and severity definitions the prompt content must align with. -->

From anthropic.py `_DIAGNOSIS_TOOL` (the structured-output tool the model is forced to call):
  Tool name: record_diagnosis
  verdict enum + meanings:
    - "model"        = LLM capability/knowledge issue
    - "architecture" = system design / tool schema issue
    - "prompt"       = instruction clarity / context issue
    - "unknown"      = insufficient signal
  severity enum: "low", "medium", "high"
  Other required fields the model emits: affected_field (str), fix (str)

The prompt is sent as the user-message `content` to all providers (Anthropic / OpenAI / Ollama).
There is NO provider-level `system` parameter — the system message section lives as literal
text at the top of prompt.md (per RESEARCH anti-pattern note A1).

RESEARCH "Prompt Content Requirements" (sections a–d) and "Prompt Template Structure"
skeleton (RESEARCH lines 290–356) define the expected content. Use it as a starting point;
expand decision criteria to reference the actual flag types Xeter emits.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite prompt.md content with system message, CoT scaffold, four-verdict criteria, and severity calibration</name>
  <files>xeter/services/diagnosticer/prompt.md</files>
  <read_first>
    - xeter/services/diagnosticer/prompt.md (the extracted template from Plan 01 — current placeholders)
    - 30-RESEARCH.md "Prompt Content Requirements" (sections a–d) and "Prompt Template Structure" skeleton (lines 290–356)
    - xeter/services/diagnosticer/providers/anthropic.py (verdict/severity definitions to stay consistent with)
  </read_first>
  <behavior>
    - Rendering _format_context(...) produces a string containing a system/role framing that uses the words "root-cause" (or "root cause") and references that this is not general Q&A.
    - The rendered string references the record_diagnosis tool/action.
    - The rendered string contains all four verdict names: model, architecture, prompt, unknown — each with distinguishing decision guidance.
    - The rendered string contains severity guidance covering high, medium, and low.
    - The rendered string contains a reasoning/chain-of-thought scaffold that instructs working through each flag before the verdict.
    - All Plan 01 placeholders still resolve: format_map produces no KeyError.
  </behavior>
  <action>
    Rewrite `prompt.md` content (preserving the placeholder names from Plan 01: span_id, trace_id, agent_name, agent_model, tool_name, tool_description, tool_arguments, tool_output, time_begin, prompt_text, response_text, flags_section). Structure the file as: (1) a SYSTEM MESSAGE section at the top framing the diagnosticer as a root-cause analysis engine for failing AI agent tool calls — explicitly stating it is not general Q&A or summarization, and that the output is a call to the `record_diagnosis` tool; (2) a VERDICT DECISION CRITERIA section with one labeled paragraph per verdict (model / architecture / prompt / unknown) giving distinguishing signals — use the RESEARCH section-c guidance and reference concrete flag types where helpful (e.g. wrong_args, wrong_tool_called, missing_tool, no_tool_used) without inventing flag types not in the codebase; (3) a SEVERITY CALIBRATION section defining high / medium / low; (4) the existing data sections (Span Information with the nine placeholders, Prompt Text, Response Text, Anomaly Flags with {flags_section}); (5) a REASONING STEPS / chain-of-thought scaffold that directs the model to enumerate each flag, interpret it, weigh whether multiple flags point to one cause, and only then call record_diagnosis.

    CRITICAL — curly brace escaping (Pitfall 1): because Plan 01 uses str.format_map, any literal `{` or `}` you add (for example a JSON example like `{"type": "object"}`) MUST be escaped as `{{` and `}}`. Prefer prose over JSON examples to avoid this entirely; if you must include literal braces, escape them. Do NOT add an SPDX header (Pitfall 3 — this is a template, not source). Keep every `{placeholder}` that the format_map dict in context_assembly.py supplies, and do NOT introduce any new `{placeholder}` whose key is absent from that dict (would cause KeyError per Pitfall 5).
  </action>
  <verify>
    <automated>cd xeter && python -c "from xeter.services.diagnosticer import context_assembly as c; out = c._format_context({'span_id':'s1'}, [], 'P', 'R').lower(); assert 'root' in out and 'record_diagnosis' in out and all(v in out for v in ['model','architecture','prompt','unknown']) and all(s in out for s in ['high','medium','low']); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - prompt.md renders (no KeyError) and the rendered text contains: root-cause framing, record_diagnosis reference, all four verdict names with criteria, severity high/medium/low guidance, and a per-flag reasoning scaffold.
    - No SPDX header in prompt.md; any literal braces are escaped.
  </acceptance_criteria>
  <done>prompt.md content rewritten with system message, four-verdict decision criteria, severity calibration, and a chain-of-thought scaffold; format_map substitution still succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add content-presence tests asserting the four DIAG-02 sections render</name>
  <files>xeter/tests/diagnosticer/test_context_assembly.py</files>
  <read_first>
    - xeter/tests/diagnosticer/test_context_assembly.py (existing tests from Plan 01 — append, don't rewrite)
  </read_first>
  <behavior>
    - test_prompt_content_system_message: rendered output frames root-cause analysis and references record_diagnosis.
    - test_prompt_content_four_verdicts: rendered output contains all four verdict names with surrounding decision guidance.
    - test_prompt_content_severity: rendered output contains high, medium, and low severity guidance.
    - test_prompt_content_cot_scaffold: rendered output contains a per-flag reasoning instruction (e.g. references working through each flag before the verdict).
  </behavior>
  <action>
    Append four content-presence tests to `test_context_assembly.py` (keep the existing Plan 01 tests intact). Each test calls `_format_context` with a minimal stub span and asserts (case-insensitively where appropriate) that the required DIAG-02 sections are present in the rendered string. Assert on stable, meaningful substrings (verdict names, "record_diagnosis", severity levels, a reasoning-scaffold keyword) rather than exact prose, so the tests survive minor wording edits but fail if a required section is removed.
  </action>
  <verify>
    <automated>cd xeter && python -m pytest tests/diagnosticer/test_context_assembly.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All tests in test_context_assembly.py pass (Plan 01 tests + four new content tests).
    - Content tests assert on durable substrings, not full-prose equality.
  </acceptance_criteria>
  <done>Four content-presence tests added and passing, locking in the four DIAG-02 required sections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt.md content → LLM API | Rewritten prose plus interpolated span data is sent verbatim to an external LLM as the user message |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | Tampering | format_map placeholders in rewritten content | mitigate | Content tests + the render smoke check ensure no placeholder is dropped or mistyped (would cause KeyError at request time). Escaping rule for literal braces enforced in the task action |
| T-30-05 | Information Disclosure | expanded prompt prose | accept | The added content is generic diagnostic instruction; it introduces no secrets and no new user-data fields beyond those already interpolated in Plan 01 |
| T-30-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase (content + test edits only). No legitimacy gate required |
</threat_model>

<verification>
Run, in order:
1. `cd xeter && python -m pytest tests/diagnosticer/test_context_assembly.py -x -q` — substitution + all content tests pass.
2. `cd xeter && python -m pytest tests/diagnosticer/test_diagnose_endpoint.py -x -q` — regression gate still green.
3. `cd xeter && python -m pytest tests/diagnosticer/ -x -q` — full diagnosticer suite green.
4. `cd xeter && python -m pytest tests/ -x -q` — full suite green (phase gate).
</verification>

<success_criteria>
- `prompt.md` contains a system message framing root-cause analysis (not general Q&A), a chain-of-thought scaffold walking through each flag before a verdict, explicit decision criteria for all four verdicts, and severity calibration guidance.
- The rewritten content keeps all Plan 01 placeholders; `format_map` substitution produces no KeyError.
- Content-presence tests lock in all four DIAG-02 sections and pass; `test_diagnose_endpoint.py` still passes.
- DIAG-02 satisfied.
</success_criteria>

<output>
Create `.planning/phases/30-diagnosticer-prompt/30-02-SUMMARY.md` when done.
</output>
